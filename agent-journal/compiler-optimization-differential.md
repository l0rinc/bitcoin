# Compiler, optimization, LTO, PGO, and BOLT differential

## Cycle 105 start

- Selected by the uber loop: exact `shuf -i 0-98 -n 1` -> `70` (`compiler-optimization-differential`) after Cycle 104 closed goal 7.
- Branch: `uber-cycle-105-compiler-optimization-differential-20260729`.
- Cycle-start HEAD: `aab05e8ff2715e34ac7edfc11344e85a29a54a16`; `origin/master`: `87bc4c74c4dff3e5e25abc294934a02f28027a45`; merge-base: `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `origin/master...HEAD` is `999 34`.
- Gate: tracked/index state was clean, `git diff --check` passed, catalog SHA256 values matched `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`, `10408ad01c000bba65c1fff135cf2d7d92508bf8a8549141e3d6880f7fe0d4ec`, and `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`, and known untracked artifacts were preserved. The unrelated PID `777094` wallet test remained untouched.
- Scope: compare current Bitcoin Core and libsecp256k1 production paths under available GCC/Clang compilers, `-O0/-O1/-O2/-O3/-Os`, Debug/Release-like settings, LTO/ThinLTO, PGO/SamplePGO, BOLT, and hardening/constant-time barriers. Require identical deterministic outputs and status/error behavior before interpreting performance, size, startup, or cache changes.
- Exclusions: Cycle 55/78 already validated SipHash across GCC/Clang optimization and LTO; Cycle 69/100 covered Bitcoin Core SHA256 dispatch and `SHA256D64`; Cycle 90 covered libsecp ECDH assembly/portable and GCC/Clang x86_64 combinations; Cycle 46/71 timing evidence covered selected ECDH/Schnorr/MuSig callers. Reopen those only with a distinct compiler transformation, current build-system mismatch, or minimized counterexample.
- Initial hypotheses: (1) a supported optimization/profile mode is advertised or wired but silently omits a target, flag, generated source, or runtime profile; (2) LTO/PGO changes a correctness, hardening, or constant-time boundary in a current production path; (3) BOLT/SamplePGO artifacts are accepted without held-out correctness or reproducible-profile validation; (4) compiler-specific warnings or UB under optimization expose a current source defect rather than a toolchain-only artifact.
- Evidence protocol: inventory tool versions and build options; choose separate scratch build directories under `/data/my_storage/tmp`; use fixed source, seeds, corpora, and workload; compare exit status, stdout/stderr, serialized outputs, checksums, symbols/sections, size, startup, and profiles; inspect IR/assembly only after a behavioral mismatch; and preserve raw logs plus rejected hypotheses below. No default datadir, wallet, key, or production database may be used.

## Cycle 105 evidence

- Tool inventory: GCC 12.2.0, Clang 14.0.6, Clang 19.1.7, `llvm-profdata-19` 19.1.7, and `perf` 6.1.164 were available. No `llvm-bolt` or `llvm-bolt-19` executable was present, and no BOLT or SamplePGO preset/integration was found in `CMakePresets.json`, `CMakeLists.txt`, `ci`, `doc`, or `depends`. CI does explicitly exercise `CMAKE_INTERPROCEDURAL_OPTIMIZATION=ON` in `ci/test/00_setup_env_native_chimera_lto.sh`; the developer notes document LLVM coverage/profile generation and merging but not a turnkey optimized PGO or BOLT build.
- Fresh GCC Release and IPO configurations both completed. `cmake --build ... --target bitcoin-util test_bitcoin -j4` exited 0 in both trees. The ordinary C++ Release path used `-O2`; the IPO path added `-flto=auto -fno-fat-lto-objects` to compile and link commands. Full unit runs in both trees exited 0 with 1,123 cases, 1,122 passed, and the same intentional filesystem-injection warning case. GCC Release versus IPO `test_bitcoin` loaded text was `28,282,818` versus `23,491,073` bytes; the larger IPO file (`67,511,696` versus `34,212,128` bytes) was explained by unstripped LTO DWARF sections, not loaded code. `bitcoin-util` loaded text fell from `3,158,335` to `358,978` bytes.
- Fresh Clang 19 Release and IPO configurations both completed all 454 build steps and linked `bitcoin-util` and `test_bitcoin` successfully. The fixed-seed selected slice `crypto_tests,rest_tests,net_tests,script_tests,serialize_tests --random=105` passed 100 cases in each tree. Full `test_bitcoin --random=105` runs both exited 0 with 1,122 passed cases and one existing warning for the skipped script-assets data. The assertion totals varied between repeated executions of the same binary, so they are not treated as a compiler differential; the pass/failure and case counts were stable.
- Clang 19 Release versus IPO ELF results preserved the same PIE/DYN x86-64 shape and deterministic behavior. `bitcoin-util` file sizes were `4,658,504` versus `656,368` bytes and loaded text was `3,176,200` versus `552,792` bytes. `test_bitcoin` file sizes were `33,809,592` versus `33,612,840` bytes and loaded text was `29,024,450` versus `29,724,777` bytes. The size changes were expected optimizer/linker layout effects. `bitcoin-util -chain=<main|testnet4|signet|regtest> getchainparams` produced matching SHA256 outputs in Release and IPO; the unsupported `testnet` name produced the same error in both.
- Profile generation followed the documented LLVM flags, after correcting two setup mistakes. The first configure used `WITH_IPC=OFF` and failed because the actual option is `ENABLE_IPC=OFF`; the corrected configure also used `ENABLE_WALLET=OFF` and `BUILD_GUI=OFF`. The clean instrumented Clang 19 `bitcoin-util` build completed 132 steps. Running help, all supported chain parameter paths, and an unknown-chain error path produced six raw `.profraw` files (433,080 bytes each). `llvm-profdata-19 merge -sparse` succeeded; the merged profile reported 409 functions.
- A clean Clang 19 profile-use `bitcoin-util` build with `-fprofile-instr-use=/data/my_storage/tmp/cycle105-pgo.profdata` completed and linked. It emitted many nonfatal `-Wprofile-instr-unprofiled` warnings because the deliberately narrow utility workload did not execute most source files. This is an expected partial-profile limitation and was retained in the evidence rather than hidden. The profile-generated and profile-used binaries produced identical chain-parameter hashes for main, testnet4, signet, and regtest. Profile-use loaded text was `3,077,961` bytes versus `3,583,086` in the instrumented generator; the corresponding files were `4,554,328` versus `6,363,176` bytes.

## Verdict

- No current source, CMake, optimizer, LTO, profile-use, or constant-time defect was independently confirmed. The IPO size differences are accounted for by loaded code and debug-section measurements; all tested compiler/mode pairs preserved unit-test status and deterministic utility outputs. No source or permanent test change is justified by this cycle.
- The profile-use warnings are a measurement limitation, not a production finding: the generated profile intentionally covers only `bitcoin-util`, while the use build compiles the complete utility dependency graph. A representative full `ctest`/functional profile would be required before making performance or profile-completeness claims. BOLT remains untestable until the tool is installed or a supported integration appears.
- Rejected candidates: GCC/Clang Release-versus-IPO output mismatch; LTO-only test failure; missing target or generated-source wiring; and an optimizer-specific warning/UB failure. All had successful builds, full unit status, and matching utility vectors. The earlier failed configure was a command-option mistake and is not evidence against the repository.

## Handoff

- Close this cycle with a journal-only handoff commit authored as `Lőrinc <pap.lorinc@gmail.com>`. Preserve `/data/my_storage/tmp/cycle105-*` artifacts and the raw profile data for a later held-out performance run; do not stage them. The next distinct compiler campaign should use a broad test/functional profile, a held-out workload, and repeated perf measurements before considering PGO adoption. Keep prior SipHash, SHA256, ECDH, and timing cells excluded unless a new transformation or mismatch appears.

## Cycle 247 start

- Selected by the uber loop: exact `shuf -i 0-98 -n 1` -> `70` (`compiler-optimization-differential`) after Cycle 246 closed Goal 54.
- Branch: `uber-cycle-247-compiler-optimization-differential-20260801`.
- Cycle-start HEAD: `0aa7bdaa7a18b55b713d523b2581941ba87315f8`; `origin/master`: `67efced1fc83a0b7215cc1513e7c4754fee0f12f`; merge-base: `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `origin/master...HEAD` is `42 1281`.
- Gate: tracked/index state was clean apart from known untracked agent artifacts, package files, crash file, and test cache; authoritative catalog, prompt, TSV, and uber-protocol hashes matched. All six protected long-running test processes remained alive and untouched. `/` is full; scratch work is limited to `/data/my_storage/tmp`, which has about 14 GB free.
- Prior closed cells: Cycle 105's broad GCC/Clang Release-versus-IPO matrix and narrow utility PGO; Cycles 55/78 SipHash LTO, 69/100 SHA256 dispatch, 90 libsecp ECDH backend/compiler, and 46/71 timing matrices. Reopen only with current-source behavior, a different transformation, or a minimized mismatch.
- New scope: build the current libsecp256k1 subtree with available GCC 12, Clang 14, and Clang 19 under `-O2`, `-O3`, and LTO where supported; compare deterministic module tests, public vectors, exact error/status outputs, assembly-sensitive hardening/constant-time markers, and object/runtime measurements. BOLT is not available (`llvm-bolt` and `llvm-bolt-19` are absent), so no BOLT claim is planned.
- Initial hypotheses: (1) a current optimized or LTO build changes libsecp256k1 field/scalar/group/signing behavior or invalid-input status; (2) compiler-specific UB or aliasing in a backend is exposed only at `-O3`/LTO; (3) optimization removes a compiler-visible secret cleanup or constant-time barrier; (4) a CMake/Autotools mode selects different generated/precomputed sources or hardening flags than the neighboring mode.
- Evidence protocol: use separate scratch trees and fixed seeds/vectors; record compiler commands, flags, source and binary hashes, test status, stdout/stderr, ELF sections/symbols, and assembly only for a falsifiable mismatch. Require a behavioral differential or a concrete build/configuration contract failure before changing source. Do not use default wallet, datadir, key, or production database state.

## Cycle 247 evidence

- Current-source CMake baseline: all builds used the same standalone libsecp256k1 tree, static library, x86_64 assembly, `ECMULT_WINDOW_SIZE=15`, `COMB_BLOCKS=43`, `COMB_TEETH=6`, ECDH, extrakeys, Schnorr, MuSig, ElligatorSwift, and Silent Payments enabled, with exhaustive and ctime tests disabled. Each build produced both `tests` and `noverify_tests`; CTest registered 222 cases, split evenly between the two binaries.
- Compiler matrix: GCC 12.2.0 (`/usr/bin/gcc`), Clang 14.0.6 (`/usr/bin/clang`), and Clang 19.1.7 (`/usr/bin/clang-19`) all configured and built the current source. GCC O2 and O3 completed 222/222 in 117.73 and 115.76 seconds. Clang 14 O2 and O3 completed 222/222 in 109.91 and 107.74 seconds. Clang 19 O2 and O3 completed 222/222 in 110.90 and 109.44 seconds. No failure, warning-as-error, or status/output differential was observed.
- LTO matrix: GCC 12 O2 `-flto` with `gcc-ar`/`gcc-ranlib` and Clang 19 O2 `-flto` with `llvm-ar-19`/`llvm-ranlib-19` both compiled and linked whole-program test executables. GCC emitted `lto-wrapper` during both links; the Clang Ninja link rules contained `LINK_FLAGS = -flto`. GCC LTO completed 222/222 in 117.22 seconds; Clang 19 LTO completed 222/222 in 110.02 seconds.
- PGO matrix: a GCC 12 `-fprofile-generate` build ran the complete 222-case suite and emitted two profile files for the embedded `tests.c` implementations. Reconfiguring the same build tree with `-fprofile-use` and `-fprofile-correction` consumed those profiles; the profile-use build emitted only the expected missing-profile warning for the separately compiled standalone library object, which the tests do not link for their embedded implementation. The profile-use suite completed 222/222 in 109.54 seconds. A separate use-tree experiment produced path-keyed `-Wmissing-profile` warnings and was rejected as invalid PGO evidence; it was not used for the verdict.
- Behavioral coverage: the matrices exercised field/scalar/group arithmetic, ecmult, ECDH and invalid-public-key handling, ECDSA parsing/signing, x-only keys and tweaks, Schnorr BIP vectors, MuSig vectors and state transitions, ElligatorSwift vectors and XDH, Silent Payments vectors, memory-clearing helpers, byte order, constant-time moves, VERIFY and no-VERIFY paths, and all registered invalid-input/status cases. All mode pairs retained the same pass/fail outcomes.
- Binary evidence: the differing code-generation outputs were expected and measurable. `tests` loaded sizes were GCC O2 49,022,440; GCC O3 49,109,022; Clang 14 O2 49,059,988; Clang 14 O3 49,198,268; Clang 19 O2 49,069,286; Clang 19 O3 49,227,809; GCC LTO 49,041,421; Clang 19 LTO 49,105,873; and GCC PGO-use 48,998,148 bytes. `noverify_tests` showed the same mode-dependent but internally consistent layout changes. Binary hashes differed across modes, with no behavioral mismatch.
- Constant-time inspection: `src/util.h` keeps `secp256k1_memczero`'s volatile flag access and constant-time contract. The GCC PGO-use disassembly retained flag-to-mask operations and branches only on public length/block structure; no branch on the selection flag was introduced. The dedicated `memczero`, `cmov`, and zero-array tests passed in every tested mode. This is supporting evidence, not a proof of constant-time behavior.
- Tooling and limitations: `perf` is available, but no performance change was adopted because these test binaries are not a representative production workload and the runs were not interleaved benchmark measurements. `llvm-bolt` and `llvm-bolt-19` are absent, so BOLT could not be exercised. Valgrind was not found by the standalone libsecp256k1 configurations. The GCC PGO profile was generated and consumed for the embedded test implementation; it is not evidence for production workload quality or a recommendation to enable PGO.

## Cycle 247 verdict

- No current source, CMake, compiler, optimization, LTO, PGO, generated-source, invalid-input-status, or constant-time-barrier defect was independently confirmed. All available compiler and optimization modes preserved the complete current libsecp256k1 test matrix and public vectors. No source or permanent test change is justified.
- Rejected candidates: GCC-versus-Clang arithmetic or status drift; O3-only UB/aliasing; GCC or Clang LTO-only behavior changes; profile-use regression; and optimizer removal of the volatile cleanup barrier. The separate PGO use-tree warning was a profile-path setup issue, then corrected by reusing the generation tree; it is recorded as a tooling limitation rather than a repository finding.

## Cycle 247 handoff

- Close with a journal-only handoff commit authored as `Lőrinc <pap.lorinc@gmail.com>`. Preserve `/data/my_storage/tmp/cycle247-*` artifacts and logs without staging them. A future compiler cycle should use a representative production workload with interleaved repeated measurements, held-out PGO validation, and an installed BOLT tool if BOLT remains in scope. Keep prior backend, timing, SHA256, SipHash, and ECDH cells excluded unless a new transformation or mismatch appears.

## Cycle 291 start

- Selected by the uber loop: exact shuf -i 0-98 -n 1 -> 70 (compiler-optimization-differential) after Cycle 290 closed Goal 59.
- Branch: uber-cycle-291-compiler-optimization-differential-20260802.
- Cycle-start HEAD: 18c2a3d36059e805403dd9729f28491d79280ac6; origin/master: 556988790a7f961693a8fd93f73725baea66476a; merge-base: a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b; start divergence origin/master...HEAD is 45 1372.
- Gate: origin was fetched; tracked and index state was clean apart from known untracked agent artifacts; catalog SHA256 values matched 5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8, 10408ad01c000bba65c1fff135cf2d7d92508bf8a8549141e3d6880f7fe0d4ec, and babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb; uber protocol SHA256 matched 954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0; all seven protected long-running processes remained alive and untouched. The root filesystem is full; /data/my_storage has about 5.1 GB available and all new scratch state used /data/my_storage/tmp.
- Prior cells excluded: Cycle 105's broad GCC/Clang Release-versus-IPO matrix and narrow bitcoin-util PGO; Cycle 247's current libsecp GCC/Clang O2/O3/LTO/PGO matrix; and earlier SipHash, SHA256, ECDH, and timing cells. The distinct queue item was a current production bitcoind path with a held-out replay, rather than another libsecp test or utility-only profile.
- Scope: compare current-source Clang 19 Release and instrumentation-guided PGO-use bitcoind/bitcoin-cli builds with wallet, IPC, GUI, and ZMQ disabled. Generate a fixed regtest profile, replay the same production path, exercise block-query paths omitted from training, compare output/state/restart behavior, and measure repeated timing without adopting a change from a noisy micro-workload.
- Initial hypotheses: (1) a current bitcoind production path changes correctness or persistent state under profile-use; (2) a profile-use build exposes a CMake append-flag or stale-profile wiring defect; (3) PGO materially changes code size or repeated workload cost on a held-out RPC path; (4) profile-use changes utility, validation, or chainstate test behavior. Require a behavioral/configuration defect or a repeated, attributable performance result before changing source.
- Evidence protocol: use separate scratch datadirs and build directories, fixed regtest address and block count, normalized JSON/state assertions, clean daemon shutdown/restart, raw LLVM profiles and merged counters, binary section measurements, focused C++ tests, and exact command/output records. The unprofiled Release tree is the control. Do not use default datadirs, wallets, keys, or production databases.

## Cycle 291 evidence

- Tool inventory: GCC 12.2.0, Clang 14.0.6, Clang 19.1.7, llvm-profdata-19 19.1.7, and perf 6.1.164 were available. llvm-bolt, llvm-bolt-19, and Valgrind were unavailable. The current-source Release and PGO trees used CMake Release, Clang 19, O2, BUILD_DAEMON=ON, BUILD_CLI=ON, BUILD_GUI=OFF, ENABLE_WALLET=OFF, ENABLE_IPC=OFF, and WITH_ZMQ=OFF.
- Release control configure/build: the existing non-protected Clang 19 Release tree was reconfigured from the current source and bitcoind plus bitcoin-cli rebuilt successfully. The binary identified as v31.99.0-18c2a3d36059 bitcoind.
- PGO generation: the current source was configured with fprofile-instr-generate on C/C++ compilation and executable linking, then bitcoind and bitcoin-cli rebuilt through 298/298 targets. A fresh regtest daemon generated 200 blocks using generatetoaddress with the fixed bcrt1qqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqq3xueyj address, then ran getblock and getblockstats for block 200 before clean shutdown. LLVM_PROFILE_FILE produced one raw profile, cycle291-pgo-gen-1527278.profraw, of 4,739,920 bytes. llvm-profdata-19 merge -sparse succeeded; the merged profile was 648,056 bytes, reported 4,023 functions and 422,959,292 total counter hits.
- PGO-use configuration initially exposed a stale cycle105 profile path inherited by the reused build directory's APPEND_CFLAGS, APPEND_CXXFLAGS, APPEND_LDFLAGS, SECP256K1_APPEND_CFLAGS, and SECP256K1_APPEND_LDFLAGS cache entries. The stale path was detected by searching generated rules, all append variables were explicitly reset to the Cycle 291 profile, and a second configure showed only the Cycle 291 path in the generated compile/link rules. This was a reusable-build setup hazard, not a repository defect. The corrected profile-use tree rebuilt bitcoind and bitcoin-cli through 298/298 with expected -Wprofile-instr-unprofiled warnings for functions absent from the short training run.
- Binary identity and section evidence: Release and PGO-use both reported v31.99.0-18c2a3d36059 bitcoind. GNU size reported Release bitcoind text/data/bss/dec as 11,876,608/77,568/38,424/11,992,600 and PGO-use as 11,379,139/77,592/38,424/11,495,155. readelf reported .text sizes of 0x71da1f and 0x6a6333 respectively. The PGO build reduced loaded code size, but the workload and profile scope are too narrow to treat that reduction as an adoption recommendation.
- Same-workload correctness: fresh Release and PGO-use daemons each reached height 200 with headers 200, initialblockdownload false, chainwork ending in 0192, txouts 200, and total_amount 8725. The first run measured generatetoaddress at 0.06 seconds for Release and 0.05 seconds for PGO-use. Three additional fresh runs measured Release at 0.07/0.07/0.06 seconds and PGO-use at 0.07/0.06/0.06 seconds. Independent datadirs produced different block hashes because regtest timestamps differed; normalized chain and UTXO results matched.
- Held-out and restart checks: each binary reloaded its 200-block datadir successfully with headers 200, initialblockdownload false, and the same chainwork. A 30-call getblock loop measured 0.17 seconds for Release and 0.10 seconds for PGO-use; a 30-call getblockstats loop measured 0.08 seconds and 0.09 seconds. Normalized block metadata matched: height 200, confirmations 1, nTx 1, size 261, strippedsize 225, weight 936, and one transaction. The RPC loop includes repeated bitcoin-cli process startup and is not a representative daemon benchmark; the single result is retained as evidence but not a speed claim.
- Focused test build and tests: the profile-use test_bitcoin target compiled and linked all 163/163 targets. With TMPDIR redirected to /data/my_storage/tmp, argsman_tests passed all 18 cases, including util_ReadConfigFiles_network_noincludeconf. The utility, validation, validation_chainstate, and validation_flush selection passed all 95 cases with no errors. An earlier combined attempt used the full root /tmp and hit the project's low-disk guard; a later attempt used a not-yet-created TMPDIR and failed with temp_directory_path, both environment setup failures corrected before the passing isolated runs.
- No source or permanent test file was changed in this cycle. The only evidence artifact is the selected journal section; raw profiles, merged profile data, build trees, and scratch datadirs remain outside Git.

## Cycle 291 verdict

- No current source, CMake, compiler, profile-use, persistent-state, RPC, utility, validation, chainstate, or restart defect was independently confirmed. The stale append-profile path was detected and corrected through explicit build-tree reconfiguration. Both binaries preserved normalized regtest state and focused test outcomes.
- Performance is inconclusive rather than positive: three same-workload generation repetitions were effectively tied at this scale, and the held-out RPC difference is dominated by short CLI/RPC process measurements. The PGO binary's smaller .text section is measurable, but the profile covers only a 200-block local run and omits wallet, network, functional, and long-running production workloads. No optimization commit is justified.
- Rejected candidates: profile-use behavioral drift; stale profile path as a repository build-system defect; PGO-induced test failure; and a reproducible held-out performance regression or win. The initial test failures were caused by the full root filesystem and a missing redirected temporary directory, not by the profile-use binary.

## Cycle 291 handoff

- Close with a journal-only handoff commit authored as Lőrinc <pap.lorinc@gmail.com>. Preserve the Cycle 291 profile/build artifacts without staging them. The next distinct compiler campaign should use a larger deterministic production or functional workload, held-out replay vectors, interleaved repeated measurements with perf counters where useful, and BOLT only if an executable or supported integration becomes available. Keep the current profile-use result as evidence, not as a recommended build configuration.

## Cycle 319 start

- Selected by the uber loop: exact `shuf -i 0-118 -n 1` -> `70` (`compiler-optimization-differential`) after Cycle 318 closed Goal 47.
- Branch: `uber-cycle-319-compiler-optimization-differential-20260802`.
- Cycle-start HEAD: `9297c44a83d4e684ab584a30c44e7b96eebb3da2`; origin/master: `556988790a7f961693a8fd93f73725baea66476a`; selection commit: `9f8d780825`; catalog SHA-256: `163eef986907fafe04cc3e27c87a892ed785fcc754f706cd4f318a521516deb4`.
- Prior cells excluded: Cycle 105's broad GCC/Clang Release-versus-IPO and utility PGO, Cycle 247's current libsecp GCC/Clang O2/O3/LTO/PGO matrix, and Cycle 291's current production bitcoind Release-versus-PGO-use replay. This cycle targets the newer libsecp source changes and verifies the build-mode contract rather than repeating those matrices.
- Current-source hypotheses: (1) recent ECDH, EllSwift, and MuSig failure-path changes behave differently under optimization or LTO; (2) compiler-specific UB or aliasing appears only in the current optimized paths; (3) recent hardening/declassification changes lose their intended generated-code behavior; (4) CMake's Release normalization and explicit optimization append flags select different code than their summaries imply.

## Cycle 319 evidence

- Tool inventory: GCC 12.2.0, Clang 14.0.6, Clang 19.1.7, and `perf` were available. No `llvm-bolt`, `llvm-bolt-19`, or Valgrind executable was available. Existing standalone trees `/data/my_storage/tmp/cycle247-secp-gcc-o2`, `cycle247-secp-gcc-lto`, and `cycle247-secp-clang19-o3` were reused to avoid a new full build while the filesystems remained full.
- Current-source GCC RelWithDebInfo `-O2` was reconfigured and rebuilt for `tests` and `noverify_tests`. GCC LTO was reconfigured with `-DCMAKE_INTERPROCEDURAL_OPTIMIZATION=ON`, `gcc-ar`, and `gcc-ranlib`; generated commands contained `-O2 -g -flto -flto=auto -fno-fat-lto-objects`. Clang 19 Release was rebuilt with `SECP256K1_APPEND_CFLAGS=-O3`; generated commands contained the project base `-O2` followed by the actual appended `-O3`.
- The focused current modules `ecdh`, `musig`, and `ellswift` passed under GCC O2, GCC LTO, and Clang 19 actual O3 with `tests` seed `319001` and `noverify_tests` seed `319002`, each at 32 iterations. The same tests had already been rebuilt from current source in each tree before execution.
- The full current-source `tests` suite passed under GCC O2 with seed `319010` and under Clang 19 actual O3 with seed `319010`. The full current-source `noverify_tests` suite passed under GCC O2 with seed `319011` and under Clang 19 actual O3 with seed `319011`. The four runs exited 0; execution times were 124.336 seconds, 116.709 seconds, 55.300 seconds, and 50.338 seconds respectively. The times are not a benchmark because the runs were not interleaved and compiler/build differences dominate.
- The generated-rule check was necessary: passing `-DCMAKE_C_FLAGS_RELEASE=-O3` alone left the nested libsecp compile commands at `-O2`, because `src/secp256k1/CMakeLists.txt` intentionally rewrites Release `-O3` to `-O2`. Passing `-DSECP256K1_APPEND_CFLAGS=-O3` produced compile commands ending in `-O3`. This is consistent with the documented project policy, but the configuration summary and generated behavior are easy to confuse.
- The current source delta included `1ec5c95460` generated-vector comment sanitization, MuSig public-nonce invalidation/declassification changes, invalid ECDH-public-key propagation and ctime coverage, the EllSwift auxiliary-input boundary fix, and recent hardening/toolchain commits. No test failure, output/status differential, optimizer warning, or generated-source mismatch appeared in the current matrix.

## Cycle 319 verdict

- No current source, CMake, compiler, optimization, LTO, hardening, constant-time, or invalid-input defect was independently confirmed. The available current libsecp paths preserved full test status under GCC O2, GCC LTO, and Clang 19 actual O3. No source or permanent test change is justified.
- Rejected candidates: recent ECDH/MuSig/EllSwift optimizer drift, GCC LTO-only behavior, Clang O3-only behavior, and a Release-summary-versus-generated-command mismatch. The last item is an important configuration lesson, not a defect: the nested project deliberately normalizes base Release O3 to O2, and the supported append variable is the explicit way to request an additional O3 flag.
- Limitations: no current full top-level Bitcoin build was created because storage is critically constrained; Clang 14 and current PGO were not rerun because their relevant source/matrix cells are already covered by Cycles 247 and 291; BOLT, Valgrind, and Alive2 were unavailable. Passing tests provide evidence, not a proof of constant-time behavior.

## Cycle 319 handoff

- Close with a journal-only handoff commit authored as `Lőrinc <pap.lorinc@gmail.com>`. Preserve the existing Cycle 247/291 build and profile artifacts without staging them. The next distinct compiler campaign should use a current top-level held-out workload or a newly changed transformation; do not repeat the focused ECDH/MuSig/EllSwift cells without a source or toolchain change.
- Learning seed for the next goal: compiler-mode audits must inspect generated compile/link commands and cache values, not only CMake summaries. A reusable build directory can retain stale append flags, and nested CMake projects can intentionally rewrite optimization levels.
