# Uber Goal State

This ledger is the authoritative handoff state for the continuing 99-goal investigation.

## Current Run

- Status: cycle 28 completed with a property-oracle audit and a dismissed invalid-domain serialization hypothesis. Existing transaction, block, Merkle, script, and block-filter properties were verified as stronger than the initial inventory suggested; no source change was justified. Ready for the next draw.
- Catalog: `agent-journal/reusable-continuous-agent-goals.md`
- Uber goal: `agent-journal/uber-goal.md`
- Worktree: `/data/my_storage/bitcoin`
- Branch: `fuzz-contract-cluster-oracles-20260709`
- Base: `origin/master` at `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`
- HEAD at initialization: `1dcc2da988ee625fbc5d7d55eb6f894c1103ec52`
- Current HEAD after cycle 16: `1926a4dbf612f3ce2fd43b61c0691360930a952f`
- Current HEAD after cycle 17: `ffda33a38f5fddab57e4618775d22ce31d8eda09`
- Current HEAD after cycle 18: `55eaf087c189ae871878692fb20a90ac3533084d`
- Current HEAD before cycle 19 journal handoff: `ec4401b816f132f2f35c1f2e64cf51e2046e8e32`
- Current HEAD after cycle 19 journal handoff: `238b809e9af1189c3ad0163f350162339d32cd07`
- Current HEAD after cycle 20 source/fuzz fix: `bbca305738343d518b179da413b3b4ebac0e2d5b`
- Current HEAD after cycle 21 source fix: `ee9ad3c5f9588bc0e228c61bc0b9b682517eeee1`
- Cycle 22 source changes: none; the cycle is recorded by a journal-only state update.
- Current HEAD after cycle 23 helper-reuse fix: `8ff6f9dd279ee73e4224bdb88630c1fdb72f9135`
- Current HEAD at cycle 24 gate: `91725b89ffac1e7b6875d4bd2344335277b76282`
- Current HEAD after cycle 24 journal handoff: `8b5df2d6f0140eae014c14d1f3a291a1aab6e98d`
- Current HEAD at cycle 25 gate: `8b5df2d6f0140eae014c14d1f3a291a1aab6e98d`
- Current HEAD after cycle 25 journal handoff: `eaf1b625e35b2e3b9d71dbd9f8a70426b651bbec`
- Current HEAD at cycle 26 gate: `eaf1b625e35b2e3b9d71dbd9f8a70426b651bbec`
- Current HEAD at cycle 27 gate: `ceb274f236881febe70aabeb534bde7f5d8babfd`
- Current HEAD after cycle 27 source fix: `c125061aa4`
- Current HEAD after cycle 27 journal handoff: `a49a0b851502138111a9007a9a86d2f4136573fe`
- Current HEAD at cycle 28 gate: `a49a0b851502138111a9007a9a86d2f4136573fe`
- Dirty state at initialization: only agent-owned catalog artifacts existed before authoritative files were added.

## Cycle Ledger

| Cycle | Draw/seed | Goal | Status | Evidence | Findings/commits | Next action |
|---|---|---|---|---|---|---|
| 1 | `shuf -i 0-98 -n 1` -> `53` | `statistical-timing` | inconclusive; no confirmed finding | Paired Clang 19 timing probes across AUTO/Debug and ASM-OFF/Release; official secp tests passed. See `statistical-timing.md`. | None | Reopen timing after MSan/Valgrind/compiler coverage; draw the next eligible goal |
| 2 | `shuf -i 0-98 -n 1` -> `66` | `backport-correctness` | dismissed; no confirmed finding | Compared the 31.x CVE-2024-52911 lifetime backport with master; `v31.1` compiled all 496 `test_bitcoin` actions and passed all 3 `validation_block_tests` cases with Boost seed `123456`. See `backport-correctness.md`. | None | Draw the next eligible goal; reopen release backports for other branches or batches |
| 3 | `shuf -i 0-98 -n 1` -> `69` | `backend-differential` | dismissed; no confirmed finding | Fresh current-source Clang 19 x86_64 assembly and ASM-OFF builds both passed the secp tests; a 2048-vector API probe produced identical `f50ba90e4d947dde` digests. See `backend-differential.md`. | None | Draw the next eligible goal; reopen for GCC/32-bit/ECDH/sanitizer coverage |
| 4 | `shuf -i 0-98 -n 1` -> `90` | `historical-knowledge-recipes` | recipe confirmed; no new defect | Extracted a reusable presence-vs-verification-before-assertion recipe from PR `#33014`; wallet-enabled focused functional regression passed with seed `12345`. See `historical-knowledge-recipes.md`. | None | Draw the next eligible goal; deduplicate this recipe against future boundary audits |
| 5 | `shuf -i 0-98 -n 1` -> `53` | `statistical-timing` (reopened) | inconclusive; no confirmed finding | Fresh current-source Clang 19 `-O2` Release assembly and ASM-OFF builds, CPU-pinned for 3 repetitions each; t-statistics changed direction and stayed below 1.8 in absolute value. See `statistical-timing.md`. | None | Draw the next eligible goal; reopen timing only with dudect/ctime/MSan/GCC/architecture or new caller evidence |
| 6 | `shuf -i 0-98 -n 1` -> `42` | `ci-bot-followup` | dismissed; no confirmed finding | All 22 workflow-referenced setup scripts existed; shell/Python entrypoints passed syntax checks; specialized gates matched runner branches. See `ci-bot-followup.md`. | None | Draw the next eligible goal; reopen only with live bot/check-run evidence, changed CI wiring, or a reproduced job failure |
| 7 | `shuf -i 0-98 -n 1` -> `81` | `spec-vector-drift` | dismissed; no confirmed finding | Pinned BIP340 vectors matched the repository after line-ending normalization; Core's fixed-`uint256` vector case passed. See `spec-vector-drift.md`. | None | Draw the next eligible goal; reopen for BIP341/342, MuSig, transport, or changed vector-source evidence |
| 8 | `shuf -i 0-98 -n 1` -> `80` | `fuzz-engine-differential` | dismissed; no confirmed finding | Same 75-file `bech32_roundtrip` corpus under libFuzzer, AFL++ 5.03a, and Honggfuzz 2.6 for 15 seconds each; all completed without crashes or hangs. FuzzTest was unavailable locally. See `fuzz-engine-differential.md`. | None | Draw the next eligible goal; reopen engine differentials only with a distinct target, sanitizer matrix, normalized coverage, or FuzzTest integration |
| 9 | `shuf -i 0-98 -n 1` -> `39` | `deterministic-artifacts` | dismissed; no confirmed finding | Two fresh Silent Payments vector generations matched the checked-in header byte-for-byte and each other (`8d88aead...9cedbbcc`); rebuilt current libsecp256k1 consumer tests passed all 7 Silent Payments tests, including `run_silentpayments_test_vectors`. See `deterministic-artifacts.md`. | None | Draw the next eligible goal; reopen generated-artifact audit only for a distinct generator, toolchain, locale, architecture, or stale-output hypothesis |
| 10 | `shuf -i 0-98 -n 1` -> `52` | `integer-overflow` | confirmed; fixed | Clean HEAD sanitizer run showed `4294968000` narrowing to `704` at both P2P buffer assignments. Range checks, uint64 conversion, focused node-init assertions, normal-build send/receive startup rejects, and repaired sanitizer run verified. See `integer-overflow.md`. | `ad2eb8be3afe4c0c0f1a254555dcda4262dda78b` (`init: reject overflowing P2P buffer arguments`) | Draw the next eligible goal; do not reopen these buffer arguments without new evidence |
| 11 | `shuf -i 0-98 -n 1` -> `67` | `release-version-differential` | dismissed; no confirmed finding | Fresh Clang 19 v31.1 and current HEAD regtest daemons received identical deterministic RPC/parser/genesis vectors. `getblockhash`, block/header serialization, script decoding, invalid-address/transaction errors, and `getblocktemplate` taproot rules matched. Observed deployment, protocol, transaction-rate, warning, and deprecated `fullrbf` field changes were each traced to source commits and release policy. See `release-version-differential.md`. | `673e2ca63f270d83d53b0bd9f0ec793e0e62bcb4` (`journal: record release differential cycle 11`) | Draw the next eligible goal; prefer a distinct release-branch, persistence, wallet, network transcript, or consensus-vector cell |
| 12 | `shuf -i 0-98 -n 1` -> `51` | `invariant-differential` | confirmed; fixed | `bitcoin-util getchainparams` emitted raw internal genesis bytes while RPC emitted the conventional public hash. A clean Clang 19 utility build, all six corrected chain fixtures, and a fresh scratch regtest daemon proved the before/after mismatch and exact post-fix equality. Alias metamorphic probes remained equal. See `invariant-differential.md`. | `c83969dae01c70eef95cf50904e95225404fc2ed` (`bitcoin-util: use public genesis hash format`) | Draw the next eligible goal; do not reopen this output-format issue without new consumer or contract evidence |
| 13 | `shuf -i 0-98 -n 1` -> `61` | `stateful-contract-fuzzer` | confirmed; test oracle fixed; no production defect | Extended the AddrMan operation-sequence fuzzer with bounded intermediate and final serialization byte-stream checkpoints, deterministic table equality, and lookup metamorphic checks. A 2,605-run qa-assets replay, bounded controls, and ASan/UBSan replay passed; an oversized control was transparently stopped at 230 executions for resource cost. See `stateful-contract-fuzzer.md`. | `bfa5a84f0ac8a18ce73bc24c0c280040c67e2a34` (`fuzz: round-trip addrman states during sequences`) | Draw the next eligible goal; do not reopen this exact AddrMan checkpoint without new state or serialization evidence |
| 14 | `shuf -i 0-98 -n 1` -> `21` | `rebuild-recovery-profile` | dismissed; no confirmed product defect or justified optimization | Built a current RelWithDebInfo wallet-disabled `bitcoind`; profiled fixed-time 20,000-block regtest chainstate reindex at 16 MB and 256 MB caches, rebuilt `txindex` plus `coinstatsindex`, and killed/restarted reindex at height 10,000. All completed runs converged to the exact source tip; cache differences were within one-run variance, and automatic restart replayed the partial state successfully. See `rebuild-recovery-profile.md`. | `fdceb1cb047512657a4d22b3023c0e5b59cf527c` (`journal: record rebuild recovery profile cycle 14`) | Draw the next eligible goal; revisit only with an input/output-heavy index workload, repeated samples, or a distinct crash/filesystem schedule |
| 15 | `shuf -i 0-98 -n 1` -> `89` | `bitcoin-p2p-accounting` | confirmed; fixed | `CConnman::StopNodes()` and `ConnmanTestMsg::ClearTestNodes()` left `m_network_conn_counts` populated after deleting peers. A focused regression failed before the fix, then passed after clearing the aggregate under `m_nodes_mutex`; the full `net_tests` suite passed 31 cases and 167,549 assertions, and rebuilt `p2p_handshake`, `cmpctblock`, `p2p_headers_presync`, and `connman` fuzz targets each completed 100 fixed-seed runs. See `bitcoin-p2p-accounting.md`. | `1554aa0f67421f265365fa2397dab43cf8d57e4f` (`net: reset connection counts when stopping peers`) | Draw the next eligible goal; revisit P2P accounting only with sanitized full-corpus, deterministic-coverage, transport v1/v2, or new lifecycle evidence |
| 16 | `shuf -i 0-98 -n 1` -> `8` | `locking-threading` | confirmed; fixed | Scheduler `DumpAddresses()` and shutdown `StopNodes()` could concurrently write `peers.dat`; AddrMan's internal lock did not cover temporary-file creation and final rename, allowing an older periodic snapshot to replace a newer shutdown snapshot. Added a function-local dump mutex. Focused normal and TSan `net_tests` passed; the existing StopNodes regression passed under TSan. Full suite reached completion with unrelated mempool, validation-signal, and external-signer failures. See `locking-threading.md`. | `bd50f35abb99fe15197cbde78e03e9bc2ac6c90d` (`net: serialize peer address dumps`) | Draw the next eligible goal; do not reopen without a distinct persistence schedule, filesystem fault, or scheduler-lifetime reproducer |
| 17 | `shuf -i 0-98 -n 1` -> `24` | `disk-io-amplification` | dismissed for production defect; benchmark gap confirmed | Audited BaseIndex, LevelDB, chainstate, txindex, txospenderindex, and blockfilterindex contracts. Built the open PR #35827 benchmark in a detached Clang 19 RelWithDebInfo worktree. Realistic disk/memory sync pairs showed expected I/O separation: TxIndex 47.3 ms vs 6.6 ms, TxoSpenderIndex 58.7 ms vs 8.0 ms, BlockFilterIndex 53.0 ms vs 13.5 ms; sequential perf counters showed 9,789 writes and 236 fdatasync calls versus 408 and zero in memory. All sync and lookup assertions passed; no persistence-growth or recovery defect was reproduced. See `disk-io-amplification.md`. | None; journal-only handoff, preserving PR #35827 as provenance rather than duplicating it | Draw the next distinct goal; revisit storage only with a larger graph, crash/filesystem schedule, or new source evidence |
| 18 | `shuf -i 0-98 -n 1` -> `27` | `error-path-state` | confirmed; fixed | `ChangeWalletPassphrase` ignored `WriteMasterKey` failure and mutated `mapMasterKeys` before persistence. An exact SQLite master-key-row abort made the old regression return success, reject the old passphrase, and accept the new one; the fixed test returned false, preserved the old passphrase and lock state, and the 18-case scriptpubkeyman suite passed. See `error-path-state.md`. | `55eaf087c189ae871878692fb20a90ac3533084d` (`wallet: preserve passphrase state on write failure`) | Draw the next distinct goal; reopen wallet passphrase handling only with new backend, restart, or encryption-failure evidence |
| 19 | `shuf -i 0-98 -n 1` -> `90` | `historical-knowledge-recipes` (reopened on new evidence) | recipe confirmed; no new production defect | Mined the relay-budget cluster (`68b745e2`, `a13956d2`, `81518c1d`, `dccab6b2`, `ade589fd`) for a reservation-conservation/deferred-eligibility recipe. Held-out `13da611b39` independently matched disconnect/removal accounting; `a39a6d7caf` failed-spend and `4ea3901a88` atomic-dump commits were negative controls. See `historical-knowledge-recipes.md`. | Journal-only handoff; no source change justified | Draw another eligible goal; if 90 repeats, choose a different historical bug-shape cluster |
| 20 | `shuf -i 0-98 -n 1` -> `37` | `build-dead-zones` | confirmed; fixed | Clean `BUILD_FOR_FUZZING=ON`, `ENABLE_IPC=ON`, wallet-enabled GCC configuration built the 510-step fuzz target and included generated IPC plus wallet fuzz sources. An empty `wallet_bdb_parser` input failed before the fix with UBSan at `AutoFile::write`/`fwrite`; empty and non-empty replays passed after the guard. Clang 19 plus installed Cap'n Proto 0.9.2 was an explicit dependency blocker. See `build-dead-zones.md`. | `bbca305738343d518b179da413b3b4ebac0e2d5b` (`fuzz: avoid empty wallet parser write`) | Draw the next distinct goal; revisit only with a different compiler/dependency/feature combination or new sanitizer evidence |
| 21 | `shuf -i 0-98 -n 1` -> `5` | `boundary-off-by-one` | confirmed; fixed | The direct-fetch walk used `<= 16`, admitting a 17th fetchable block and issuing a partial request. The failing-before functional control exposed `getdata` for 17 headers; the final loop continues over already-requested/available ancestors while stopping before the 17th fetchable entry. Exact 16/17 coverage, the full `p2p_sendheaders` scenario, a rebuilt unit target, and all 31 `net_tests` cases passed. See `boundary-off-by-one.md`. | `ee9ad3c5f9588bc0e228c61bc0b9b682517eeee1` (`net: stop direct fetch at peer block limit`) | Draw the next distinct goal; prioritize mixed eligibility, CompactSize max edges, and 32-bit-relevant offset cells |
| 22 | `shuf -i 0-98 -n 1` -> `27` | `error-path-state` (reopened on a distinct state cell) | dismissed; no new production defect | Transaction-download response/rejection/orphan/disconnect contracts and 14 focused unit tests passed. `CoinStatsIndex::CustomAppend` increments subsidy before a mismatch return, but `BaseIndex` contiguous-transition and initialization guards make the mismatch unreachable in supported production transitions. Isolated normal and ASan/UBSan replay of the same 128 qa-assets seeds completed 129 runs each with exit code 0; see `error-path-state.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; retain only the CoinStats internal-contract question and `GetNodeStateStats` failure semantics as lower-priority future hypotheses |
| 23 | `shuf -i 0-98 -n 1` -> `58` | `exact-helper-reuse` | confirmed; fixed | `TxIndex::CustomAppend` and `TxoSpenderIndex::BuildSpenderPositions` duplicated the exact witness-inclusive transaction-size expression already named by `CTransaction::ComputeTotalSize()`. Replaced both call sites; the rebuilt index tests passed all 6 cases and the combined transaction/index run passed all 24. See `exact-helper-reuse.md`. | `8ff6f9dd279ee73e4224bdb88630c1fdb72f9135` (`index: reuse transaction total-size helper`) | Draw the next distinct goal; do not reopen this offset expression unless the transaction-size contract or index backend changes |
| 24 | `shuf -i 0-98 -n 1` -> `82` | `secp-field-scalar-matrix` | dismissed; no confirmed finding | Clean standalone Clang 19 `SECP256K1_ASM=OFF` builds forced int128 and int64 wide multiplication, and all six regular/no-VERIFY/exhaustive binaries passed. A corrected internal-header probe exercised 13 field/scalar edge vectors, inverse/half/mul/add/normalization/reduction, and emitted 754 canonical records. Both outputs matched byte-for-byte with SHA256 `315b522a08b0f7c0ab9fbf21a02c8244143c50d8ee911645f54c8e888db92828`; the two earlier VERIFY failures were harness aliasing and lazy-initialization mistakes. See `secp-field-scalar-matrix.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; reopen representation parity only for GCC, cross-architecture, assembly, sanitizer, or new source evidence |
| 25 | `shuf -i 0-98 -n 1` -> `60` | `reviewer-preference-mining` | recipe confirmed; no new production defect | Mined seven PR review clusters with local history and public GitHub API evidence. Extracted general rules for contract/lifecycle tracing, public-input validation, exact behavioral oracles, fuzz reset/realism, async race-window narrowing, portable fixtures, and dependency hygiene; classified `Assert`/`Assume`, fuzz-target scope, `max/min`/`clamp`, hasher applicability, and style suggestions as contextual. Held-out commits `e9ed898a`, `6ee05c4b`, and `6aa5d8d` independently recovered the rules; `12269112` was a same-feature fixture control. See `reviewer-preference-mining.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; reopen only with new review data, a different subsystem cluster, or a concrete recurrence |
| 26 | `shuf -i 0-98 -n 1` -> `36` | `sanitizer-analysis-matrix` | dismissed; no confirmed production defect | Clang/GCC TSan unit slices passed 63/61 cases with 173,304/174,265 assertions. Clang ASan/UBSan passed 180 focused cases with 390,283 assertions; Clang/GCC ASan fuzz slices passed identical `bech32_roundtrip`, `addrman`, and `crypto` inputs. Clang `scan-build` produced zero plist reports. GCC `-fanalyzer` emitted 29 CWE-457 warnings, all clustered in inline assembly, standard-library/static initialization, assertions, filesystem wrappers, and unreachable code; cross-tool/runtime controls classified them as analyzer artifacts or bounded contract questions. See `sanitizer-analysis-matrix.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; reopen for MSan/instrumented dependencies, Valgrind, direct TokenPipe coverage, a new analyzer warning class, or a new source/configuration path |
| 27 | `shuf -i 0-98 -n 1` -> `15` | `public-object-validation` | confirmed; fixed | A syntactically shaped but off-curve compressed P2PK key was accepted by script inference as `pk(...)`, while descriptor parsing correctly rejected the same key as invalid. The failing-before round-trip test captured the exact parse error. Adding `IsFullyValid()` to `InferPubkey()` made inference fall back to `raw(...)`; the focused regression and normal/Clang ASan/LSan/UBSan descriptor suites passed. See `public-object-validation.md`. | `c125061aa4` (`descriptor: keep inferred invalid pubkeys parseable`) | Draw the next distinct goal; keep x-only/taproot inference as a separate unchecked validation cell |
| 28 | `shuf -i 0-98 -n 1` -> `48` | `property-oracle-expansion` | dismissed; no confirmed finding | Audited transaction, block/header, Merkle, script sigop, and block-filter property surfaces. Existing fuzzers and vectors already supplied independent round-trip, mutation, failure-state, and metamorphic contracts. A temporary empty-transaction witness probe showed only invalid-domain ambiguity; `CheckTransaction` rejects the object with `bad-txns-vin-empty`. Normal script/sigop, block-filter, and transaction suites passed 29, 8, and 18 cases; ASan fuzz smokes passed 1000/323/300 runs for script ops/block filter/block header. See `property-oracle-expansion.md`. | Journal-only handoff; no source change justified | Draw a new eligible goal; revisit serialization only for an accepted-transaction boundary or new witness format, and script sigops only with an independent P2SH/witness reference |

## Eligibility

- Pending goals: `0..98`, subject to the catalog validation and current risk map.
- Active goals this cycle: none after cycle 28 handoff.
- Reopened goals: `statistical-timing` (cycle 5).
- Exhausted goals: none recorded yet.

## Handoff

Cycle 28 used `/data/my_storage/tmp/property-oracle-expansion-cycle28/` for dedicated empty fuzz corpora and raw smoke outputs. The exact selector was `shuf -i 0-98 -n 1`, which drew `48` (`property-oracle-expansion`). The audit found existing independent contracts for transaction serialization and hashes, block/header bytes, partial Merkle trees, script sigop accounting, and block filters. A temporary probe of default empty transaction serialization disproved the expected witness parse failure, but the object is outside the accepted transaction domain and `CheckTransaction` rejects it as `bad-txns-vin-empty`; the probe was removed and no source/test change was justified. Normal script/sigop, block-filter, and transaction suites passed 29, 8, and 18 cases. Clang ASan fuzz smokes completed 1000 `script_ops`, 323 `blockfilter`, and 300 `block_header` runs with no diagnostics. No source, test, fuzz, sanitizer, daemon, or profiling process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 27 used `/data/my_storage/tmp/public-object-validation-cycle27/` for a fixed malformed public-object corpus and `/data/my_storage/tmp/sanitizer-analysis-matrix-cycle26/asan-unit-clang/` for the rebuilt Clang sanitizer test target. The exact selector was `shuf -i 0-98 -n 1`, which drew `15` (`public-object-validation`). The old inference path produced `pk(020000...0000)#rxgq8p4q` for an off-curve P2PK key, and `Parse()` rejected it with `pk(): Pubkey '020000...0000' is invalid`. Commit `c125061aa4` added the full curve-validity check and the regression now requires a parseable `raw(...)` fallback. Normal and sanitizer descriptor suites each passed 11 cases and 30,415 assertions; the focused regression passed 4 assertions in both configurations. Clang and GCC parser fuzz slices passed their fixed inputs before the source edit; no sanitizer diagnostic appeared after the edit. No source, test, fuzz, sanitizer, daemon, or profiling process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 26 used `/data/my_storage/tmp/sanitizer-analysis-matrix-cycle26/`. Clang and GCC TSan unit slices passed 63/61 cases and 173,304/174,265 assertions; a fresh Clang ASan/UBSan unit build passed 180 focused cases and 390,283 assertions; and matched Clang/GCC ASan/UBSan fuzz inputs passed under their respective driver interfaces. Clang `scan-build` analyzed the core crypto/util targets with zero plist reports. GCC `-fanalyzer` completed the same targets but emitted 29 CWE-457 warnings; all were cross-tool/runtime-classified as analyzer artifacts or bounded contract questions, with no source change justified. The initial Clang IPC configure was rejected by the explicit Cap'n Proto 0.9.2/Clang 19.1.7 compatibility check, so the static and ASan unit builds used IPC off; MSan, Valgrind, clang-tidy, cppcheck, Semgrep, and CodeQL were unavailable. Raw logs and build trees remain under `/data/my_storage/tmp/sanitizer-analysis-matrix-cycle26/`; no process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 25 used local history plus unauthenticated public GitHub API review endpoints for PRs `#35692`, `#35320`, `#35639`, `#35090`, `#35681`, `#35215`, `#35616`, and `#35767`. The exact API method and source tips are recorded in `reviewer-preference-mining.md`; the extracted recipe was checked against held-out commits `e9ed898a`, `6ee05c4b`, and `6aa5d8d`, with `12269112` retained as a same-feature fixture control. Remote review visibility is incomplete and acceptance alone was not treated as a universal preference. No production source changed, no tests were needed for this history-only cycle, and no process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 16 left no fuzz, sanitizer, daemon, profiling, or test process running. Raw timing artifacts remain under `/data/my_storage/tmp/statistical-timing/` and `/data/my_storage/tmp/statistical-timing-cycle5/`; backend differential artifacts remain under `/data/my_storage/tmp/backend-differential/`; the focused PSBT log is under `/data/my_storage/tmp/rpc-psbt-recipe-wallet.log`; fuzz-engine artifacts and logs remain under `/data/my_storage/tmp/fuzz-engine-differential/`; deterministic-artifact outputs remain under `/data/my_storage/tmp/deterministic-artifacts/`; integer-overflow build and startup logs remain under `/data/my_storage/tmp/integer-overflow-*/`; cycle 11 release-differential builds, raw RPC responses, and daemon logs remain under `/data/my_storage/tmp/release-version-differential/`; cycle 12 invariant-differential builds, fixtures, and scratch daemon artifacts remain under `/data/my_storage/tmp/invariant-differential-cycle12/`; cycle 13 fuzz builds and scratch corpora remain under `/data/my_storage/tmp/stateful-contract-fuzzer-cycle13/`, with project fuzz binaries under `build_fuzz_libfuzzer_clang19/` and `build_fuzz_asan_clang19/`; cycle 14 optimized daemon, deterministic chain, profile copies, crash-recovery copies, and raw counters remain under `/data/my_storage/tmp/rebuild-recovery-profile-cycle14/`; cycle 15 rebuilt fuzz binary, four eight-input smoke corpora, and logs remain under `/data/my_storage/tmp/bitcoin-p2p-accounting-cycle15/`. Cycle 16 used the existing unit and TSan build trees; the full unit run ended with the residual failures recorded in `locking-threading.md`. Cycle 17's detached benchmark worktree, build, raw testdatadirs, and perf runs remain under `/data/my_storage/tmp/disk-io-amplification-cycle17/`. Cycle 18 used `build_unit_wallet_clang19`; the wallet unit target and selected scriptpubkeyman suite passed, and the controlled pre-fix test failure is recorded in `error-path-state.md`. Cycle 19 used history-only evidence; the relay-budget and held-out private-broadcast commits include their exact clean-master controls and mutation proofs in `historical-knowledge-recipes.md`. Cycle 20 used `/data/my_storage/tmp/build-dead-zones-cycle20/`; the GCC IPC/wallet fuzz target built all 510 steps and the post-fix empty/non-empty wallet parser and IPC smokes passed. The Clang 19 attempt stopped at the repository's explicit Cap'n Proto 0.9.2 compatibility check. Cycle 21 used `/data/my_storage/tmp/p2p-sendheaders-cycle21-before2/` for the failing-before assertion and `/data/my_storage/tmp/p2p-sendheaders-cycle21-after3/` for the passing full functional run; `build_func_clang19` rebuilt `bitcoind`, `build_unit_clang19` rebuilt `test_bitcoin`, and `net_tests` completed all 31 cases with no errors. Cycle 22 used `/data/my_storage/tmp/cycle22-fuzz-work/` and `/data/my_storage/tmp/cycle22-fuzz-asan/` for isolated 128-seed normal and ASan/UBSan replays; both completed 129 runs with exit code 0. The earlier shared-checkout parallel replay was discarded as non-authoritative after log collisions and interruption. No source process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.
Cycle 23 used the existing `build_unit_clang19` tree; rebuilding `test_bitcoin` compiled both changed index translation units. The focused `txindex_tests` plus `txospenderindex_tests` run passed all 6 cases before and after, and the combined `transaction_tests,txindex_tests,txospenderindex_tests` run passed all 24 after the fix. No source process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.
Cycle 24 used `/data/my_storage/tmp/secp-field-matrix-int128/` and `/data/my_storage/tmp/secp-field-matrix-int64/`, both with assembly disabled and explicit int128/int64 overrides. Their builds and regular/no-VERIFY/exhaustive binaries passed. The scratch probe source and outputs remain under `/data/my_storage/tmp/`; the corrected runs exited 0, emitted 754 records each, and compared with identical SHA256 `315b522a08b0f7c0ab9fbf21a02c8244143c50d8ee911645f54c8e888db92828`. The first probe failures were discarded as harness violations: restricted field-multiply aliasing and lazy reads of uninitialized pairwise entries. No production source changed and no test, fuzz, sanitizer, daemon, or profiling process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.
