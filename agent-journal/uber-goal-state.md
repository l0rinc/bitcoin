# Uber Goal State

This ledger is the authoritative handoff state for the continuing 99-goal investigation.

## Current Run

- Cycle 88 is active on goal 74 (`memory-pressure-allocator`), selected by exact `shuf -i 0-98 -n 1` -> `74` after Cycle 87 closed goal 94.
- Current branch: `uber-cycle-88-memory-pressure-allocator-20260729`; start HEAD is `0ab48d44a6e17500b05bf76a781254451192f65e`; `origin/master...HEAD` is `2 964`; merge-base is `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`.
- No relevant process remains running. Preserved unrelated untracked artifacts remain excluded from all cycle commits.

## Latest authoritative checkpoint

- Cycle 88 is active on goal 74 (`memory-pressure-allocator`). The dedicated branch is `uber-cycle-88-memory-pressure-allocator-20260729`; cycle-start HEAD is `0ab48d44a6e17500b05bf76a781254451192f65e`, `origin/master...HEAD` is `2 965` after the cycle-start journal commit, and merge-base is `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`.
- Cycle 87 is complete on goal 94 (`bindings-ffi-parity`), after rejecting the exact draw `71` and accepting reroll `94`. Its confirmed source commit is `215c89a391` and its close snapshot is `0ab48d44a6`.
- Cycle 85 is complete on goal 63 (`loupe-pipeline`) with source/fix commit `468e4ef6fd` and close snapshot `1d1db50f3f`; its fail-closed release-verifier evidence is recorded in `agent-journal/loupe-pipeline.md`.
- Cycle 84 is complete with confirmed commit `89b320fe37` and close snapshot `eb9a35d139`; its deterministic failed-start lifecycle evidence is recorded in `agent-journal/deterministic-simulation.md`.
- Cycle 87 journal: `agent-journal/bindings-ffi-parity.md`. The source-level contract and history confirmed that `contrib/tracing/mempool_monitor.py` dropped the eighth replacement-kind field; the fix and limitations are recorded there. Live BCC execution was unavailable because `bcc` is not installed, and the functional runner could not start because `test/functional/config.ini` is absent. No relevant process remains running.

## Cycle 88 Active State

- Exact selector: `shuf -i 0-98 -n 1` -> `74` (`memory-pressure-allocator`).
- Branch: `uber-cycle-88-memory-pressure-allocator-20260729`.
- Start HEAD: `0ab48d44a6e17500b05bf76a781254451192f65e`; base `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `2 964`.
- Journal: `agent-journal/memory-pressure-allocator.md`; scratch root `/data/my_storage/tmp`.
- Distinct scope: network receive/send queue ownership, configured bounds, pause/disconnect behavior, accounting versus retained bytes, fragmented/stalled/duplicate/reconnect schedules, and cleanup. Cycle 53's prevector OOM policy cell is excluded.
- Status: cycle 88 is active; first action is to map the transport buffers and accounting contract, then run a fixed loopback workload with independent byte and RSS/heap measurements.

## Cycle 87 Completion

- Initial draw: exact `shuf -i 0-98 -n 1` -> `71` (`deterministic-simulation`), rejected as the just-closed campaign with no new schedule evidence.
- Accepted reroll: exact `shuf -i 0-98 -n 1` -> `94` (`bindings-ffi-parity`).
- Branch: `uber-cycle-87-bindings-ffi-parity-20260729`.
- Start HEAD: `a38b4da22fcb0df5411bfd2e023be4cee9689278`; base `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `2 961`.
- Scope: compare maintained language wrappers and FFI boundaries with native C/C++ declarations, including ABI widths, ownership/lifetime, callbacks, error mapping, buffers, secret cleanup, flags, and malformed input.
- Journal: `agent-journal/bindings-ffi-parity.md`; scratch root `/data/my_storage/tmp`.
- Confirmed finding: `contrib/tracing/mempool_monitor.py` omitted the eighth `mempool:replaced` argument, `replaced_by_transaction`, after commit `5736d1ddacc4` made the tracepoint distinguish transaction IDs from package hashes. Package RBF makes the ambiguity reachable. The demo now mirrors the documented/functional event layout and renders `transaction` or `package` before the replacement hash.
- Source/journal commit: `215c89a391` (`tracing: preserve mempool replacement kind`), authored as `Lőrinc <pap.lorinc@gmail.com>`.
- Validation: `python3 -m py_compile contrib/tracing/mempool_monitor.py` and `git diff --check` passed. The functional runner was attempted but stopped before test setup because `test/functional/../config.ini` is absent. `python3 -c 'import bcc'` reported `ModuleNotFoundError`, so live BCC validation remains an explicit environment limitation.
- No relevant process remains running. The next run must re-check the gate, worktree, process state, catalog/protocol/TSV hashes, existing journals, history, and review precedent before drawing a distinct eligible goal.

## Cycle 86 Completion

- Initial draw: exact `shuf -i 0-98 -n 1` -> `71` (`deterministic-simulation`), rejected as the just-closed campaign with no new schedule evidence.
- Accepted reroll: exact `shuf -i 0-98 -n 1` -> `25` (`performance-regression-bisect`).
- Branch: `uber-cycle-86-performance-regression-bisect-20260729`.
- Start HEAD: `1d1db50f3f5fe1002c0701a7b1a8fe78ec9338aa`; base `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `2 959`.
- Scope: recent performance regression bisecting across stable local workloads, benchmark integrity, build mode, cache state, profiles, I/O, CPU, allocation, and correctness controls.
- Journal: `agent-journal/performance-regression-bisect.md`; scratch root `/data/my_storage/tmp`.
- Status: cycle 86 is complete with a journal-only handoff; first action was inventory of performance journals, benchmark recipes, recent performance-sensitive history, and stable workload candidates.
- Candidate boundary: merge `c0e91efdb3` (`validation: fetch block input prevouts in parallel during ConnectBlock`) versus first parent `f0da26cfc8`. Both historical worktrees built successfully with the same GCC 12.2.0 `RelWithDebInfo` configuration and `/data`-backed scratch/ccache settings.
- Evidence: three interleaved parent/child `feature_block.py` runs all passed. Parent wall samples were `57.25`, `56.62`, and `56.72` seconds; child samples were `56.52`, `59.64`, and `58.41` seconds. Parent mean was `56.863` seconds and child mean `58.190` seconds, but the child test forces eight prefetch workers while the parent test uses the framework default of one, and the child sample spread was much larger. No causal profile, failure, or correctness regression was found.
- Verdict: inconclusive workload signal; no source change justified. The direct `connectblock` benchmark was excluded because it bypasses `ConnectTip` prefetch. Next evidence should use identical worker settings and a deterministic cold/evicted-chainstate transaction workload before treating any effect as a regression.
- Close action: commit this journal and state update as a single state-only handoff, then draw the next exact selector with `shuf -i 0-98 -n 1` and start a distinct branch/journal cycle.

## Cycle 85 Completion

- Selector: exact `shuf -i 0-98 -n 1` -> `63` (`loupe-pipeline`).
- Branch: `uber-cycle-85-loupe-pipeline-20260729`.
- Start HEAD: `eb9a35d139`; base `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `2 956`.
- Scope: separate scout, verifier, fixer, and reporter stages with leases, PoCs, applicability checks, semantic/hash deduplication, and final review evidence.
- Journal: `agent-journal/loupe-pipeline.md`; scratch root `/data/my_storage/tmp`.
- Confirmed finding: `contrib/verify-commits/verify-commits.py` treated every nonzero `git merge-base --is-ancestor` status as “predates the trusted root,” so an invalid/missing revision could return status 0 before signature or tree verification. The trusted-root and trusted-SHA512-root checks now accept only statuses 0 and 1; operational errors exit 1.
- Scout evidence: `doc/security/codex-security-bitcoin-round2/shard_00.jsonl` and `overview.md` rows 11 and 83-98. Deduplication found no earlier journal finding or separate fingerprint for this path/bug shape.
- Source/test/journal commit: `468e4ef6fdf68c566f31e6a8c4cd0d39004baec3` (`contrib: fail closed on verify-commits ancestry errors`), authored and committed as `Lőrinc <pap.lorinc@gmail.com>`.
- Validation: `python3 contrib/verify-commits/test.py` passed 3 tests; `python3 -m py_compile contrib/verify-commits/verify-commits.py contrib/verify-commits/test.py` passed; the fixed missing-revision command returned status 1; `git diff --check` passed. `test/lint/lint-files.py` reported five pre-existing unrelated shebang-permission failures and `lint-python.py` skipped for missing `lief`; neither was masked or changed.
- Limitation: practical impact depends on an invalid/unresolved candidate ref or incomplete Git object database reaching the release/CI verifier; it is not a remote P2P finding. The next cycle must recheck the gate, then draw a distinct goal.

## Previous authoritative checkpoint (superseded)

- Cycle 84 (`deterministic-simulation`) is complete with confirmed finding commit `89b320fe3709b3d96d91a5a596fae40f3d71e0a5` (`net: reject conflicting start options before threads`). The invalid public `CConnman::Start` option combination now returns before any network thread or other startup side effect; the before/after regression and full `net_tests` evidence are recorded in `agent-journal/deterministic-simulation.md`.
- The next selector draw is cycle 85: exact `shuf -i 0-98 -n 1` -> `63` (`loupe-pipeline`). Its branch and start snapshot are pending initialization below. Current branch HEAD is `89b320fe3709b3d96d91a5a596fae40f3d71e0a5`; `origin/master...HEAD` is `2 955`; merge-base is `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`.
- No relevant process remains running. Unrelated tracked commits and untracked files were preserved.

## Historical pre-close checkpoint (superseded)

<!-- Cycle 84 has verified a source finding; this checkpoint is updated before the finding commit. -->

- Current verified cycle: 84, goal 71 (`deterministic-simulation`). `CConnman::Start` now rejects conflicting outgoing options before any startup side effect; the focused before/after regression and full `net_tests` suite provide the evidence. The source/test/journal close is pending one self-contained commit.
- Cycle-start HEAD was `c79c8b401b`. Three pre-existing Codex Security documentation commits advanced this branch to `55cfa7a448` before the cycle finding; they were preserved. At the experiment point, `origin/master...HEAD` was `2 954`, with merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`.
- Current branch: `uber-cycle-84-deterministic-simulation-20260728`; no relevant process remains running. Known unrelated untracked files remain preserved.

## Historical Cycle 84 start checkpoint (superseded)

- Cycle 84 is active. Goal 71 (`deterministic-simulation`) is exploring replayable production schedules across task ordering, time, randomness, network, disk, retries, and shutdown, with explicit state, progress, durability, and resource invariants.
- Cycle 83 is complete. Goal 11 (`sanitizer-valgrind`) found no confirmed current Bitcoin Core or libsecp256k1 source defect and made no production source change; its ASan/TSan/MSan evidence and tool blockers are recorded in `agent-journal/sanitizer-valgrind.md`.
- Current branch: `uber-cycle-84-deterministic-simulation-20260728`; current HEAD before the start snapshot: `c79c8b401b`. `origin/master...HEAD` is `2 950`; no relevant process remains running.

## Historical Cycle 84 Active State (superseded)

- Selector: exact `shuf -i 0-98 -n 1` -> `71` (`deterministic-simulation`).
- Branch: `uber-cycle-84-deterministic-simulation-20260728`.
- Start HEAD: `c79c8b401b`; base `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `2 950`.
- Scope: replayable production schedules for task ordering, time, randomness, network, disk, retries, and shutdown, with explicit final-state, progress, durability, cleanup, and resource invariants.
- Exclusions: Cycle 9 generated-artifact determinism and prior full-sync, sanitizer, P2P accounting, filesystem crash, database fault, and scheduler cells without a new boundary or schedule.
- Journal: `agent-journal/deterministic-simulation.md`. Scratch root: `/data/my_storage/tmp` because `/` is full.
- Status: cycle 84 is active; first action is inventory of existing production simulation seams, deterministic hooks, and prior schedule evidence.

## Cycle 84 Verified Finding

- Finding: `CConnman::Start` returned `false` for `m_use_addrman_outgoing=true` plus nonempty `m_specified_outgoing` only after starting network thread handles. This violated the failed-start no-thread lifecycle invariant for direct public callers.
- Before evidence: the focused regression against the old source exited `201`; `!started` passed and `!threads_started` failed at `src/test/net_tests.cpp:391`.
- Repair: move the existing conflict check immediately after `Init`; add `ConnmanTestMsg::AnyThreadJoinablePublic()` and `net_tests/connman_start_rejects_conflicting_options_before_threads`.
- After evidence: the focused test exited `0`; full `net_tests` passed 34/34 cases and 150,899/150,899 assertions. The same build command and `/data`-backed `TMPDIR` were used; `git diff --check` passed.
- Cycle journal: `agent-journal/deterministic-simulation.md`; exact mechanism, reachability, history, limitations, and commands are recorded there. The commit is pending and must be authored as `Lőrinc <pap.lorinc@gmail.com>`.

## Cycle 83 Completion

- Selector: exact `shuf -i 0-98 -n 1` -> `11` (`sanitizer-valgrind`).
- Branch: `uber-cycle-83-sanitizer-valgrind-20260728`.
- Start HEAD: `acc8a0388d`; base `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`.
- Scope: fresh ASan, UBSan, TSan, MSan, LeakSanitizer, Valgrind/Memcheck, sanitized fuzz, recovery, and suppression/exclusion cells. Closed TokenPipe, prior sanitizer-matrix, LevelDB leak, compact-block read-failure, PSBT, and locator cells are excluded without distinct evidence.
- Journal: `agent-journal/sanitizer-valgrind.md`. Scratch root: `/data/my_storage/tmp` because `/` is full.
- Status: cycle 83 is complete with a journal-only close; no source patch or regression test was justified.

## Cycle 82 Completion

- Selected by exact `shuf -i 0-98 -n 1` -> `7` (`resource-exhaustion-variants`), excluding cycle 57's cfilters/BIP35 response-watermark cells and previously closed relay-backlog, integer-option, and receive-buffer cells.
- Branch: `uber-cycle-82-resource-exhaustion-variants-20260728`; start HEAD `d86caff0a23f0ba042ced945520dffb1ee224164`; base `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `2 945`.
- Confirmed finding: an unauthenticated peer could advertise an oversized locator count in `getblocks` or `getheaders`; generic vector deserialization reserved about 5 MiB before the later 101-entry check. `ReadBlockLocator` now checks the CompactSize count first. The source/test regression and exact resource equation are recorded in `agent-journal/resource-exhaustion-variants.md`.
- Source/test/journal commit: `1e0091464ceb4c13c2b5d33eaedbefc0b6d57452` (`net: reject oversized locators before deserialization`).
- Validation: normal focused before/after regression, normal full `net_tests` (33 cases), Clang 19 TSan focused regression, and `git diff --check`. An initial test launch without `TMPDIR` was discarded because the root filesystem was full before fixture setup; the authoritative reruns used `/data/my_storage/tmp`.
- Gate: tracked source is clean after the source commit; known untracked agent artifacts and `test/cache` remain preserved and excluded; catalog/protocol/TSV hashes remain recorded; no relevant process is running.

## Cycle 82 Active State

- Selector: exact `shuf -i 0-98 -n 1` -> `7` (`resource-exhaustion-variants`).
- Branch: `uber-cycle-82-resource-exhaustion-variants-20260728`.
- Start HEAD: `d86caff0a23f0ba042ced945520dffb1ee224164`; base `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `2 945`.
- Scope: distinct CPU/memory/disk/network/descriptor/queue/retry/retained-state variants at P2P, RPC/REST, wallet, persistence, and parser boundaries. Cycle 57's cfilters/BIP35 response-watermark cells and prior relay-backlog, integer-option, and receive-buffer cells are excluded without new evidence.
- Gate: catalog/protocol/TSV hashes match; known untracked artifacts are preserved; no relevant process is running. Cycle 82 journal: `agent-journal/resource-exhaustion-variants.md`.

- Status: cycle 81 in progress; goal 6 (`serialization-untrusted-input`) is tracing untrusted byte lengths, counts, encodings, casts, loops, and failure-state mutation across network, RPC, object, wallet, database, and fuzz boundaries. Cycle 80 found no new current-tree advisory variant; A1-A4 were dismissed after source/history tracing and focused suites, while A6 and A7 were already fixed upstream with current regression evidence. The missing previous-release migration binaries and native 32-bit execution are recorded limitations, not passes. Cycle 79's distinct `parse_numbers` engine comparison found no new defect. Cycle 78 confirmed and fixed the TokenPipe EPIPE status-contract defect while the remaining MSan daemon runtime boundary was classified as an uninstrumented system dependency. Cycle 77's fresh relationship/state-domain cells found no new source defect. Cycle 76 confirmed and fixed a distinct compact-block announcement read-failure path. Cycle 75 fixed the nested Taproot compressed-key private-lookup mismatch. Cycle 74 found no new source defect in the conditional-build matrix. Cycle 73 confirmed and fixed a reachable LevelDB ownership leak during `CDBWrapper` construction failure. Cycle 72's secp nonce/state cell and cycle 71's timing cell found no new source defect and remain closed. Cycle 70's kernel-wrapper pointer-array fix, cycle 69's `setlabel` RPC fix, and cycle 68's `GETBLOCKTXN` assertion fix remain closed.
- Catalog: `agent-journal/reusable-continuous-agent-goals.md`
- Uber goal: `agent-journal/uber-goal.md`
- Worktree: `/data/my_storage/bitcoin`
- Branch: `uber-cycle-81-serialization-untrusted-input-20260728`
- Base: `origin/master` at `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`
- HEAD at initialization: `1dcc2da988ee625fbc5d7d55eb6f894c1103ec52`
- Current HEAD after cycle 16: `1926a4dbf612f3ce2fd43b61c0691360930a952f`
- Current cycle-73 source/test/journal HEAD: `548d8cc8f8` (`dbwrapper: clean up resources on constructor failure`). The next state-only close commit records this cycle as complete.
- Current cycle-75 source/test/journal HEAD: `d02e22867b` (`descriptor: use x-only lookup for Taproot compressed keys`).
- Current cycle-76 source/test/journal HEAD: `ddbb88ed12` (`net: disconnect on compact block announcement read failure`).
- Current cycle-77 start HEAD: `76a2240168` (`journal: close cross-subsystem bug-shapes cycle 76`).
- Current cycle-77 pre-close HEAD: `fc42eb9f32` (`journal: start local-reasoning-domain cycle 77`).
- Current cycle-78 start HEAD: `b9bb184352` (`journal: close local-reasoning-domain cycle 77`).
- Current cycle-78 source/test/probe/journal HEAD: `c28bbc9580` (`util: report broken token pipe as end-of-stream`).
- Current cycle-79 start HEAD: `4230363290` (`journal: close sanitizer-analysis cycle 78`).
- Current cycle-79 journal/state close HEAD before the close snapshot: `0635279aed` (`journal: start fuzz-engine cycle 79`); no production source change was made.
- Current cycle-80 start HEAD: `d2379d8892` (`journal: close fuzz-engine cycle 79`); cycle 80 closed with a journal/state-only snapshot and no production source change.
- Current cycle-81 start HEAD: `7918b638fd` (`journal: close external advisory cycle 80`); cycle 81 is active on a fresh branch.
- Cycle 76 gate: HEAD `02e0a92ddc33af203f4204848eb6095312f052af`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 930`; tracked source clean except known untracked agent artifacts and `test/cache`; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 76 selector: initial `shuf -i 0-98 -n 1` -> `37` (`build-dead-zones`) was rejected as the just-closed cell; retry `shuf -i 0-98 -n 1` -> `26` (`cross-subsystem-bug-shapes`). Cycles 48 and 62's wallet-rescan and chainstate candidate-set cells are excluded; this cycle targets a distinct historical failure-publication or lifecycle-asymmetry shape.
- Cycle 77 selector: exact `shuf -i 0-98 -n 1` -> `57` (`local-reasoning-domain`). Cycle 65's AddrMan `GetNetwork()`/`GetNetClass()` relationship and linked-IPv4 cells are excluded; this cycle targets a distinct local relationship or state-domain assumption.
- Cycle 78 selector: initial `shuf -i 0-98 -n 1` -> `2` (`assertion-invariant-audit`) was rejected because cycle 68 closed that GETBLOCKTXN assertion cell; retry `shuf -i 0-98 -n 1` -> `36` (`sanitizer-analysis-matrix`) was accepted for the distinct queued MSan/instrumented-dependency, Valgrind, direct TokenPipe, new-warning, and new source/configuration cells.
- Cycle 79 selector: exact `shuf -i 0-98 -n 1` -> `80` (`fuzz-engine-differential`). Cycle 8's `bech32_roundtrip` engine/corpus comparison is excluded; this cycle uses the distinct `parse_numbers` parser target and current-HEAD toolchain matrix.
- Cycle 80 selector: exact `shuf -i 0-98 -n 1` -> `33` (`external-vulnerability-variants`). The current campaign mines authoritative advisory and related-project fix seeds; closed local cells are excluded unless a seed provides a distinct caller, trust boundary, or lifecycle path.
- Cycle 81 selector: exact `shuf -i 0-98 -n 1` -> `6` (`serialization-untrusted-input`). The current campaign audits untrusted byte lengths, counts, canonical encodings, output state, and persisted/network parser parity; closed serialization cells are excluded unless a distinct boundary or caller is proven.
- Cycle 74 gate: HEAD `ddde67072a`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 925`; tracked source clean except known untracked agent artifacts and `test/cache`; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 74 selector: exact `shuf -i 0-98 -n 1` -> `37` (`build-dead-zones`). Cycle 20's GCC `BUILD_FOR_FUZZING=ON`, `ENABLE_IPC=ON`, wallet-enabled parser/fuzz cell is excluded; this cycle targets wallet-off, IPC-off/monolithic, test/bench, and generated-source parity.
- Cycle 74 close: current source/journal HEAD before the state-only close commit is `8063cae3aa`; no source change was justified. The wallet-off/IPC-off Clang, wallet-on/IPC-off Clang, wallet-off/IPC-on GCC, fuzz-only wallet-off Clang, and GUI wallet-off Clang matrices all configured and built as expected. Focused unit, IPC, Qt, target-registration, and `/data`-backed fuzz controls passed; the only negative results were a malformed full-root temp setup and the documented release-build fuzz refusal. See `build-dead-zones.md`.
- Cycle 75 selector: exact `shuf -i 0-98 -n 1` -> `15` (`public-object-validation`). Cycles 27 and 51's P2PK and x-only/taproot inference cells are excluded; this cycle targets nested `multi_a`/multisig/miniscript public-key parsing, serialization, failure-state outputs, and wrapper parity. Branch `uber-cycle-75-public-object-validation-20260728` starts at `df9c8d76c7`.
- Cycle 75 source/test/journal completion: `d02e22867b` (`descriptor: use x-only lookup for Taproot compressed keys`). A full compressed key accepted in P2TR retained its descriptor spelling but used parity-specific private-key lookup, so an equivalent opposite-parity provider key was not found. The fix separates spelling from lookup mode and uses `GetKeyByXOnly()` for full compressed P2TR keys. Normal descriptor/miniscript, TSan, ASan/UBSan fuzz smoke, and the status-201 old-source mutation passed as documented in `public-object-validation.md`.
- Cycle 73 completion: the old source leaked 4064 bytes in 19 allocations when a filesystem exception interrupted `CDBWrapper` construction. The fixed `LevelDBContext` destructor and exception-safe option allocation passed the dedicated normal and ASan/LSan dbwrapper suites (14 cases, 2475 assertions each), the standalone fixed probe, and the negative-control mutation reproduced the old leak. A broader block/index/flush suite was blocked by the host root filesystem being 100% full and was terminated after cascading fixture assertions; no process remains running. See `raai-resource-leaks.md`.
- Cycle 71 gate: fetched `origin/master`; HEAD `9eb6b4ad1b66f030f0019b759d0c8017993cf63b`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 917`; tracked/staged state clean except known untracked agent artifacts and `test/cache`; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 71 selector: exact `shuf -i 0-98 -n 1` -> `53` (`statistical-timing`). Cycles 1, 44, and 46 timing cells are excluded; this run targets the remaining ElligatorSwift XDH and Silent Payments secret-bearing callers.
- Cycle 71 completion: no new source defect. Clang 19 AUTO/OFF probes found no stable class effect for Ellswift XDH, Silent Payments sender output creation, or label creation. A repeatable scan-only difference was explained by the documented declassification of the hash-derived public-output tweak; the fixed nonmatching-output probe is not a payment-recognition oracle. AUTO/OFF module suites passed, MSan ctime passed, and removing only that declassification reproduced a status-86 use-of-uninitialized-value report at the variable-time public-key tweak before restoration returned ctime to status 0. See `statistical-timing.md`.
- Cycle 72 gate: fetched `origin/master`; HEAD `480832aabf6e86a9750dd0a788cccc7f74d0b4ec`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `919 2`; tracked/staged state clean except known untracked agent artifacts and `test/cache`; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 72 selector: exact `shuf -i 0-98 -n 1` -> `84` (`secp-nonce-session`). Cycle 60's MuSig invalid-argument ordering fix is excluded; this run targets post-failure state, nonce consumption, malformed session/nonce binding, duplicate participants/nonces, infinity aggregation, and Schnorr callback failure.
- Cycle 58 gate: fetched `origin/master`; HEAD `75b1f55d251ea4cab3ebd827ece57eb6a8c41969`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 884`; tracked/staged state clean with only known untracked agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 58 selector: exact `shuf -i 0-98 -n 1` -> `32` (`history-incomplete-fixes`). Scope excludes the cycle-43 wallet migration write-return cell, cycle-56 database output-on-decode-failure cell, and cycle-57 compact-filter range-output/resource cell; this run mines the distinct current output-contract follow-up cluster.
- Current HEAD after cycle 58 source/test fix: `3e4ec4e7ef0f216c09c10b1d577fc1517a043434` (`blockstorage: publish undo data after checksum verification`). `BlockManager::ReadBlockUndo` now publishes decoded undo data only after checksum verification; `blockmanager_readblockundo_preserves_output_on_checksum_failure` reproduced the old partial-output behavior and passed after the fix. The focused regression passed with 11 assertions, the full `blockmanager_tests` suite passed 12 cases and 128 assertions, `git diff --check` passed, and ASan was unavailable in the local build environment. No relevant process remains running.
- Cycle 59 gate: fetched `origin/master`; HEAD `3b4c458d37861c084d074becaae27e4228fd07d0`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 887`; tracked/staged state clean with only known untracked agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 59 selector: exact `shuf -i 0-98 -n 1` -> `45` (`constant-time-boundary`). The cycle-40 EllSwift auxiliary-input boundary is closed. This run selects the distinct MuSig session/keypair declassification boundary and will not modify EllSwift code without new independent evidence.
- Cycle 59 completion: no source defect found. The live MSan ctime baseline, regular libsecp256k1 tests, and exhaustive order-13 tests passed. Removing keypair public declassification, secnonce invalidation-status declassification, or invalid-session-randomness status declassification each produced a first-invalid-operation MSan trace; all temporary mutations were restored. Valgrind was unavailable, `git diff --check` passed, and no relevant process remains running.
- Cycle 60 gate: fetched `origin/master`; HEAD `a064c61c3e77cbf08e9a68cd466f12cd50654ec3`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 889`; tracked/staged state clean with only known untracked agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 60 selector: exact `shuf -i 0-98 -n 1` -> `84` (`secp-nonce-session`). Earlier MuSig ctime and secret-lifetime cells are closed. This run audits invalid-order, single-use, duplicate/replay, malformed-object, callback, and output-on-failure transitions, starting with MuSig public output state.
- Cycle 60 completion: confirmed and fixed a MuSig API validation-ordering gap. `secp256k1_musig_nonce_gen` returned early for all-zero session randomness before validating required `pubnonce`, bypassing the illegal-argument callback when both were invalid. Removing the new precondition made `musig_api_tests` fail at `_calls_to_callback == 1` with exit 134; restoring it passed the focused test and the full libsecp test binary (16 iterations, 76.329 seconds, exit 0). See `secp-nonce-session.md`.
- Current HEAD after cycle 60 source/test fix: `765e82e8ab` (`secp256k1: validate MuSig nonce output argument`). No relevant process remains running.
- Cycle 61 gate: fetched `origin/master`; HEAD `3d67541b54019711263e742be691e83da57cd0ec`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 892`; tracked/staged state clean with only known untracked agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 61 selector: exact `shuf -i 0-98 -n 1` -> `22` (`full-sync-ibd-profile`). Prior rebuild, disk, perf, and regression-profile cells are closed; this run measures a distinct end-to-end local import/IBD phase and separates validation, chainstate, block I/O, compaction, logging, and harness time.
- Cycle 61 completion: no source defect found. A fresh optimized Clang 19 local-peer IBD matrix imported 20,000 deterministic regtest blocks into five empty sinks. The exact tip and 20,001 transaction count matched in every run. Regtest's intentional default `-checkblockindex=1` consumed about 55% of sampled cycles and made the run about 130 seconds; `-checkblockindex=100` took 4.545 seconds and `=0` took 2.899 seconds. Database-cache variation (16/64/256 MB) was non-monotonic within observed noise. See `full-sync-ibd-profile.md`.
- Cycle 61 artifacts: `/data/my_storage/tmp/full-sync-ibd-profile-cycle61/` contains the release-like build, source/sink datadirs, raw `perf stat` outputs, and a 54,683,552-byte DWARF call-stack profile with zero lost samples. No relevant process remains running.
- Cycle 62 gate: fetched `origin/master`; HEAD `387f5a122f1c85655253cd35b47d49140317e8a9`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 894`; tracked/staged state clean with only known untracked agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 62 selector: exact `shuf -i 0-98 -n 1` -> `26` (`cross-subsystem-bug-shapes`). The cycle-48 wallet-rescan reservation-ordering cell is closed; this run must mine a distinct structural fix shape and prove an analogous reachable site.
- Cycle 62 completion: dismissed. The `b0ce659a98` shared-candidate mutation fix was compared with `Chainstate::PreciousBlock`, but a block eligible for active reprioritization cannot be a valid background candidate under the current historical `TargetBlock` contract. The controlled active-child setup showed background candidate counts of zero before reprioritization; injecting membership would violate the production invariant. No source change was made. See `cross-subsystem-bug-shapes.md`; `git diff --check` passed and no relevant process remains running.
- Cycle 63 gate: fetched `origin/master`; HEAD `70caa034b9aa598e10bfd5091f2e6358f843a610`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 896`; tracked/staged state clean with known untracked agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 63 selector: exact `shuf -i 0-98 -n 1` -> `59` (`cpp-supply-chain`). Reopen only the compiler/tool acquisition, dependency-cache, generated-input, license, and provenance cells; do not repeat cycle 41's trusted-signature threshold fixes.
- Current HEAD after cycle 63 source fix: `2ac7b583af3a8935962d75b8125fd1c67675268b` (`guix: verify cached Git archives before extraction`). Existing source and detached-signature archives are now compared against a fresh `git archive` digest before extraction; stale and byte-tampered controls fail closed, while matching archives pass. `guix` and `shellcheck` were unavailable, so full containerized build and shell lint execution remain limitations. No relevant process remains running.
- Cycle 64 gate: fetched `origin/master`; HEAD `fda626b39ebae557a7b628f300b764aa58f84249`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 899`; tracked/staged state clean with known untracked agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 64 selector: exact `shuf -i 0-98 -n 1` -> `18` (`exhaustive-algebraic`). The prior GCS `MatchAny` identity is closed. This run audits compact-target canonicalization across exponent, mantissa, sign, overflow, and 256-bit boundary cells, with the proof-of-work path as the independent consumer.
- Cycle 65 gate: fetched `origin/master`; HEAD `d7109ee6a310bbfeac419e3f0833910ee2454570`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 901`; tracked/staged state clean with only known agent-owned untracked artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 65 selector: exact `shuf -i 0-98 -n 1` -> `57` (`local-reasoning-domain`). The selected cell is the relationship between AddrMan's `GetNetwork()` accounting/filtering and its public `GetNetClass()` result contract for valid linked-IPv4 addresses. See `local-reasoning-domain.md`.
- Current HEAD after cycle 65 source/test fix: `b2d858ae4e` (`addrman: match Select contract to transport network`). `Select` now asserts the transport-level `GetNetwork()` relationship used by its filtering implementation; the deterministic test and fuzzer oracles match it, and the retained regression covers RFC6145, RFC6052, RFC3964, and RFC4380 linked-IPv4 forms. The focused test passed 29 assertions, the full AddrMan suite passed 28 cases and 14,346 assertions, the deliberate old-classifier mutation failed, the rebuilt fuzzer target passed a 100-run empty-corpus smoke, and the interrupted large-corpus replay is recorded as an execution-boundary limitation. No relevant process remains running.
- Cycle 66 gate: fetched `origin/master`; HEAD `4f68ee5f8455f0279b929e1d04b986b72c8bc104`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 904`; tracked/staged state clean with only known agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 66 selector: exact `shuf -i 0-98 -n 1` -> `40` (`multi-agent-adjudication`). The cycle-54 duplicate global-backlog and cycle-38 `-txsendrate` metadata cells are excluded. The selected cell is whether `ProcessInvBacklog`'s no-recipient refund/clear behavior can lose a ForceRelay delivery across a later permission, handshake, filter, or peer-connection transition. See `multi-agent-adjudication.md`.
- Cycle 66 completion: dismissed; no source defect proven. Investigator A's production trace identified the observed loss of a remote ForceRelay event when no current recipient is eligible, while Investigator B's independent history/contract pass found the original global-queue commit explicitly clears work for future peers and no durable future-recipient promise. The current-recipient `p2p_permissions.py`, known/filtered relay-rate controls, 200-run `process_messages` replay, restored live schedule, temporary durability mutation, and `util_tests` passed. See `multi-agent-adjudication.md`; no relevant process remains running.
- Cycle 67 gate: fetched `origin/master`; HEAD `f08d9749c2949a3ff6b91d87525d42486a483c36`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 906`; tracked/staged state clean with only known agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 67 selector: exact `shuf -i 0-98 -n 1` -> `66` (`backport-correctness`). Cycle 36's `#35331` 31.x batch and cycle 66's earlier 31.x CVE lifetime cell are excluded. The selected distinct cell is the 29.x `#34370` Berkeley wallet file-ownership/cleanup backport, compared with the later rebased form on `origin/28.x`; see `backport-correctness.md`. The original and rebased patches have identical patch-id and semantic hunks. A BDB-enabled v29.4 build, focused wallet/database/filesystem tests, and corrected legacy-wallet functional run passed; the migration suite skipped only because previous-release binaries were unavailable. No production source has been changed; cycle 67 is dismissed.
- Cycle 68 gate: fetched `origin/master`; HEAD `0c8c5ad2d8ffcf1468d46339608efd42d37fb1c1`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `908 2`; tracked/staged state clean with only known agent artifacts; catalog/protocol/TSV hashes matched; no relevant process was running.
- Cycle 68 selector: exact `shuf -i 0-98 -n 1` -> `2` (`assertion-invariant-audit`). The AddrMan `Select` postcondition, MuSig argument-ordering, and earlier lock/lifetime cells are excluded. The distinct scope is release-reachable assumptions at RPC/configuration, network, persisted-state, fuzz, and optional-module boundaries; see `assertion-invariant-audit.md`. Cycle 68 confirmed that `GETBLOCKTXN` asserted on a local `ReadBlock` failure after `cs_main` was released; the source now logs and disconnects the requesting peer while keeping the daemon alive.
- Current HEAD after cycle 68 source/test fix: `6fbcd16491` (`net: handle GETBLOCKTXN block read failures`). The fixed P2P path handles local block-read failure without terminating the daemon, and `p2p_compactblocks.py` contains the corruption regression.
- Current HEAD after cycle 17: `ffda33a38f5fddab57e4618775d22ce31d8eda09`
- Current HEAD after cycle 18: `55eaf087c189ae871878692fb20a90ac3533084d`
- Current HEAD before cycle 19 journal handoff: `ec4401b816f132f2f35c1f2e64cf51e2046e8e32`
- Current HEAD after cycle 19 journal handoff: `238b809e9af1189c3ad0163f350162339d32cd07`
- Current HEAD after cycle 20 source/fuzz fix: `bbca305738343d518b179da413b3b4ebac0e2d5b`
- Current HEAD after cycle 21 source fix: `ee9ad3c5f9588bc0e228c61bc0b9b682517eeee1`
- Cycle 22 source changes: none; the cycle is recorded by a journal-only state update.
- Current HEAD after cycle 23 helper-reuse fix: `8ff6f9dd279ee73e4224bdb88630c1fdb72f9135`
- Current HEAD at cycle 24 gate: `91725b89ffac1e7b6875d4bd2344335277b76282`
- Current HEAD after cycle 24 journal handoff: `8b5df2d6f0140eae014c14d1f3a291a1aab6e98d`
- Current HEAD at cycle 25 gate: `8b5df2d6f0140eae014c14d1f3a291a1aab6e98d`
- Current HEAD after cycle 25 journal handoff: `eaf1b625e35b2e3b9d71dbd9f8a70426b651bbec`
- Current HEAD at cycle 26 gate: `eaf1b625e35b2e3b9d71dbd9f8a70426b651bbec`
- Current HEAD at cycle 27 gate: `ceb274f236881febe70aabeb534bde7f5d8babfd`
- Current HEAD after cycle 27 source fix: `c125061aa4`
- Current HEAD after cycle 27 journal handoff: `a49a0b851502138111a9007a9a86d2f4136573fe`
- Current HEAD at cycle 28 gate: `a49a0b851502138111a9007a9a86d2f4136573fe`
- Current HEAD after cycle 28 journal handoff: `177da9e873`
- Current HEAD at cycle 29 gate: `97cb20252003ac2aff08969368f3302b7824c2af`
- Current HEAD after cycle 29 validation-cache fix: `f91039eb5c`
- Current HEAD after cycle 29 mempool-limit fix: `93fef76238`
- Current HEAD at cycle 30 gate: `6ea89f6c40dd94b2e7c1524cd5573430f7e71072`
- Cycle 30 gate base: merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` is `2 832`; tracked and staged state clean; no relevant process running.
- Cycle 30 catalog SHA256: `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`; uber-protocol SHA256: `954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0`.
- Current HEAD after cycle 30 source fix: `5b2e4f5a63`.
- Cycle 31 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 834`; tracked and staged state clean; no relevant process running.
- Cycle 31 draw: `90` (`historical-knowledge-recipes`), selected on a distinct configurable-parallel-feature history cluster after excluding the cycle-4 PSBT and cycle-19 relay-accounting recipes.
- Cycle 31 verification: `validation_chainstatemanager_tests` passed 22 cases and 2068 assertions. PR `#35295` supplied the complete option/runtime/lifecycle/fuzz/documentation recipe; `c7af7477` was a parser-only negative control; `3873d90` supplied separate worker-boundary coverage. No production defect or source change was justified.
- Cycle 32 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 835` before source fixes; tracked and staged state clean; no relevant process running.
- Cycle 32 selector: first draw `48` (`property-oracle-expansion`) was rejected as the already-closed cycle-28 cell; rerun `shuf -i 0-98 -n 1` drew `3` (`current-pr-leftovers`) for the queued debug-option cells.
- Cycle 32 source fixes: `ddbbb0aadb` rejects negative and `int`-overflowing `-dbcrashratio` values before chainstate construction; `5754033019` rejects negative `-dbbatchsize` values before unsigned narrowing. Focused arguments passed 51 assertions; full chainstate, flush, and mempool suites passed 49 cases and 60,503 assertions. The existing feature functional test stopped in its MiniWallet setup with `StopIteration` before exercising database writes; all nodes were cleaned and this is recorded as a harness blocker.
- Cycle 33 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 838`; tracked and staged state was clean; catalog/protocol/TSV validation passed; no relevant process was running.
- Cycle 33 selector: `shuf -i 0-98 -n 1` drew `62` (`rejected-finding-resurrection`). The first reopened cell was the cycle-22 `CoinStatsIndex::CustomAppend` mismatch mutation.
- Cycle 33 verification: current `MakeBlockInfo`, `BaseIndex::Sync`, `BaseIndex::BlockConnected`, `CoinStatsIndex::CustomInit`, `RevertBlock`, `ProcessBlock`, `AbortNode`, and shutdown ordering were audited through callers and history. The mismatch remains behind contiguous-transition guards and a fatal shutdown path; it returns before CoinStats DB writes, best-block publication, or public lookup exposure. `coinstatsindex_tests,baseindex_tests` passed 3 cases, and the selected current index lifecycle control passed its 1 enabled case. No source change was justified; see `rejected-finding-resurrection.md`.
- Cycle 34 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 839`; tracked and staged state was clean; catalog/protocol/TSV validation passed; no relevant process was running.
- Cycle 34 selector: `shuf -i 0-98 -n 1` drew `50` (`fuzz-introspector`). The primary candidate was the high-risk `process_messages` P2P state-machine target with an empty current QA corpus.
- Cycle 34 verification: the 229-target registry, source call chain, Clang object sizes, corpus inventory, runner behavior, CI history, and libFuzzer counters were audited. Default coverage replay used `-runs=1` and reached only `cov 11470` in two empty-input executions; `--empty_min_time=2` reached `cov 11482` in 161 executions. `CoverageFuzz.cmake` now passes a configurable `FUZZ_EMPTY_MIN_TIME` defaulting to 60 seconds. CMake regeneration, the fuzz target build, Python syntax, diff checks, and targeted runner controls passed. See `fuzz-introspector.md`.
- Current HEAD after cycle 34 coverage-path fix: `7d1a7bdda64c861fdacfb18309cbc80611d097b6`.
- Current HEAD at cycle 35 gate: `722f659965ab807b7effc2b2dca20951ccf9d79f`.
- Cycle 35 selector: `shuf -i 0-98 -n 1` drew `8` (`locking-threading`). The earlier cycle-16 `DumpAddresses()` persistence race was excluded; the selected cells were transaction/inventory/mempool lock nesting, V2 transport lock order, and production scheduler lifetime.
- Cycle 35 verification: `m_tx_download_mutex` and `m_inv_to_send_mutex` retain their declared order before `m_mempool.cs`, with no nested reverse acquisition in production callers. V2 receive/send nesting matches its annotation, and normal scheduler shutdown joins before object destruction. TSan `net_tests` passed 31 cases and 135,024 assertions; TSan `scheduler_tests` passed 4 cases and 27 assertions; one real `process_message` seed passed the custom `process_messages` driver with no diagnostic. No source change was justified; see `locking-threading.md`.
- Current HEAD at cycle 36 gate: `1162eaecac8dbfc69eab31c7229c231eb1fbfdae`.
- Cycle 36 selector: `shuf -i 0-98 -n 1` drew `66` (`backport-correctness`). The earlier cycle-2 CVE-2024-52911 lifetime cell was excluded; this cycle selected the queued `#35331` 31.x backport batch.
- Cycle 36 verification: `origin/31.x` v31.1 batch `efde6234` was compared against its source-side parent and current master, including all `OpenNetworkConnection` callers, private-broadcast proxy guards, settings write checks, MuSig empty-list validation, LevelDB seek-compaction tests, and asynchronous chainstate compaction locking/destruction. The clean release build completed 496 actions; focused unit suites passed 24/1,217,435, 17/144,457, and 1/57,996; the complete unit binary exited 0 with 734/740 cases passed, 1 warning, 5 skipped, and 26,275,230 assertions; both selected functional tests and the direct LevelDB `AutoCompact` controls passed. No source change was justified; see `backport-correctness.md`.
- Current HEAD at cycle 37 gate: `ebe09a67153fe09ba67b03b0cc49f01fa84e2381`.
- Current HEAD at cycle 38 gate: `3005aa4f092d1e8234f104a869fe35c452c8b42d`.
- Current HEAD after cycle 39 source fix: `f6c37d8239` (`wallet: make address book writes transactional`).
- Cycle 40 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 847`; tracked and staged state was clean; catalog/protocol/TSV hashes validated; no relevant process was running.
- Cycle 40 selector: `shuf -i 0-98 -n 1` drew `45` (`constant-time-boundary`). The cycle selected the EllSwift `secp256k1_ellswift_create` auxiliary-input boundary after excluding the separate cycle-39 `setlabel` return-value lead.
- Cycle 40 verification: the public EllSwift header promises constant-time behavior in `seckey32` and `auxrnd32`, but the old implementation declassified the SHA-256 state before writing `auxrnd32`. A fresh Clang 19 MSan ctime run with a secret auxiliary buffer failed at the hash-derived branch in `xswiftec_inv_var`; moving declassification after the optional write made the same run pass. Normal Bitcoin Core crypto tests, libsecp256k1 regular/exhaustive tests, the rebuilt unit target, and `git diff --check` passed. See `constant-time-boundary.md`.
- Current HEAD after cycle 40 EllSwift fix: `20286797faf270438eed7824891228cba44929ca` (`secp256k1: fix EllSwift auxiliary input boundary`).
- Cycle 41 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 849`; tracked and staged state was clean; catalog/protocol/TSV hashes validated; no relevant process was running.
- Cycle 41 selector: `shuf -i 0-98 -n 1` drew `59` (`cpp-supply-chain`). The selected surface was the release binary verifier after dependency archives, vendored subtrees, Guix hashes/signatures, and compiler/tool acquisition gates showed no missing hash or provenance control.
- Cycle 41 verification: `contrib/verify-binaries/verify.py` counted valid but untrusted signatures toward `--min-good-sigs`, and also counted repeated valid packets from one trusted key as independent signers. README/history contract review and deterministic synthetic GPG-status probes independently verified both paths. The verifier now counts distinct trusted signer keys; two regression controls, Python syntax compilation, and `git diff --check` passed. The full network-backed release integration remains unrun because it depends on external release artifacts and keyserver state. See `cpp-supply-chain.md`.
- Current HEAD after cycle 41 trusted-signature fix: `eec2561d8ec33e73e6bb1e8b6a438223faad41c6` (`verify-binaries: count only trusted signatures`).
- Current HEAD after cycle 41 distinct-signer fix: `23cd641d6cc66d5d1ba73ba23c878109baf32236` (`verify-binaries: count distinct trusted signer keys`).
- Cycle 42 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 852`; tracked and staged state was clean; catalog/protocol/TSV hashes were already validated; no relevant process was running.
- Cycle 42 selector: `shuf -i 0-98 -n 1` drew `13` (`secret-lifetime-zeroization`). The cycle selected the RPC cookie raw entropy buffer, MuSig nonce-generation entropy, and MuSig partial-signing keypair after prior journal/history searches found no closed duplicate for those cells.
- Cycle 42 verification: `GenerateAuthCookie` moved its raw 32-byte cookie entropy into a secure allocator-backed vector; `CreateMuSig2Nonce` moved its secret session randomness into the same secure allocator; and `CreateMuSig2PartialSig` moved its 96-byte private-key-bearing `secp256k1_keypair` into `make_secure_unique`. The rebuilt unit target and focused RPC/key/BIP328 tests passed. The bundled libsecp tests passed with exit 0. The full unit binary completed with exit 201 because of unrelated mempool, `/tmp` filesystem, and external-signer configuration failures; the RPC functional test was blocked by the host's low-disk guard. See `secret-lifetime-zeroization.md`.
- Current HEAD after cycle 42 source fixes: `c5ac9e13dc` (`rpc: cleanse cookie entropy temporary`), `8a8da25a59` (`musig: cleanse nonce entropy temporary`), and `1b1d18722c` (`musig: cleanse partial-signing keypair`). No source, test, daemon, fuzz, sanitizer, or profiling process remains running.
- Cycle 43 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 856`; tracked and staged state was clean; catalog/protocol/TSV validation was already recorded; no relevant process was running.
- Cycle 43 selector: `shuf -i 0-98 -n 1` drew `32` (`history-incomplete-fixes`). The history campaign followed wallet migration's transaction-rewrite, external-write batching, ancient-locator, label-persistence, and duplicate-seed fixes to distinct unchecked return-value sites in current `ApplyMigrationData`.
- Cycle 43 verification: `WalletBatch` write contracts return `bool`, but migration ignored local and watch-only transaction rewrites, the watch-only order position, and all external address-book fields. A SQLite-trigger regression passed after propagation and failed under a temporary mutation restoring the old unchecked local rewrite. The rebuilt unit target and focused migration test passed; the full wallet suite exited 201 with 11 disk-space-dependent scan failures after logging `Disk space is too low!` while `/` was 100% full. The functional migration test was not run for the same host limitation. See `history-incomplete-fixes.md`.
- Current HEAD after cycle 43 source finding: `a7d70e7806c04b4f5615bd39c0efa34136ce5c4e` (`wallet: report migration write failures`). No source, test, daemon, fuzz, sanitizer, or profiling process remains running.
- Cycle 44 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 858`; tracked and staged state was clean; catalog/protocol/TSV hashes validated; no relevant process was running.
- Cycle 44 selector: `shuf -i 0-98 -n 1` drew `53` (`statistical-timing`). Earlier timing cells covered only `ec_pubkey_create` and ECDSA signing, so this cycle selected current ECDH, Schnorr, and MuSig counter callers plus their ctime boundary.
- Cycle 44 verification: Clang 19 Release assembly-auto and ASM-OFF probes used 10,000 randomized paired trials per repetition with raw traces. Welch directions varied across repetitions and backends; same-key control and equal one-bit key pairs did not establish a stable timing effect. A distinct MSan ctime check tainting the opaque MuSig keypair failed at `secp256k1_pubkey_load:261` before the fix. Declassifying the public component after `secp256k1_keypair_pub` made the same run pass; removing only that line reproduced the diagnostic. Bitcoin crypto tests (23 cases), regular/noverify secp tests, exhaustive order-13 tests, unit rebuild, and diff checks passed. See `statistical-timing.md`.
- Current HEAD after cycle 44 source finding: `8c8e8a8f9fbde5e7ecce7e9adffd030549cac0fa` (`secp256k1: declassify MuSig counter public key`). No source, test, daemon, fuzz, sanitizer, or profiling process remains running.
- Cycle 45 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 860`; tracked and staged state was clean; catalog/protocol/TSV hashes validated; no relevant process was running.
- Cycle 45 selector: `shuf -i 0-98 -n 1` drew `95` (`database-semantics-differential`). The prior LevelDB work covered read/write errors and compaction backports, so this cycle selected the distinct iterator-status propagation cell.
- Cycle 45 verification: LevelDB's iterator contract exposes read failures through `Iterator::status()`, but `CDBIterator::Valid()` discarded that status and made corrupted/unreadable iteration appear to end normally. A deterministic memenv fault after `CompactFull()` failed to raise `dbwrapper_error` against the old implementation, passed after status propagation, and failed again under a mutation restoring the old method. The unit target, full dbwrapper suite, dependent chainstate/index suites, and diff checks passed. See `database-semantics-differential.md`.
- Current HEAD after cycle 45 source finding: `2340c73ef0` (`dbwrapper: preserve iterator read errors`). No source, test, daemon, fuzz, sanitizer, or profiling process remains running.
- Cycle 57 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 882`; tracked and staged state was clean with only known agent-owned untracked artifacts; catalog, protocol, and corrected-tab TSV hashes matched; no relevant process was running.
- Cycle 57 selector: `shuf -i 0-98 -n 1` drew `7` (`resource-exhaustion-variants`). The prior global ForceRelay relay-backlog cell and the earlier integer-domain option cells are closed; this cycle starts a distinct P2P/RPC/mempool resource-accounting sweep.
- Cycle 46 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 862`; tracked and staged state was clean; catalog/protocol/TSV hashes validated; no relevant process was running.
- Cycle 46 selector: `shuf -i 0-98 -n 1` drew `53` (`statistical-timing`). The cycle-44 MuSig ctime boundary was closed; this draw selected the distinct GCC compiler/backend and `-O3` optimization cell.
- Cycle 46 verification: GCC 12.2 `-O2` and `-O3` builds with x86_64 assembly `AUTO` and `OFF` ran deterministic ECDH, Schnorr, and MuSig timing probes for both `1` versus `0x7f...7f` and `1` versus `2` classes. Across eight build/class combinations, 12 API/repetition t-statistics each, the maximum absolute t was `1.267`; directions changed across repetitions and variants, sinks matched, and official GCC AUTO/OFF tests plus noverify tests passed with the fixed seed. `objdump` showed the fixed-window multiplication call/loop structure without a source-level secret-branch finding. Valgrind, dudect, GCC 13+, LTO, and non-x86 execution remain unavailable. See `statistical-timing.md`.
- Cycle 47 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 863`; tracked and staged state was clean; catalog/protocol/TSV hashes validated; no relevant process was running.
- Cycle 47 selector: `shuf -i 0-98 -n 1` drew `98` (`float-sanitizer-fuzz-exclusions`). The selected cells were exceptional floating-point values, active sanitizer exemptions, and fuzzer guard/oracle sensitivity.
- Cycle 47 verification: removing the SHA256 SSE4 ASan guard caused the current Clang 19 sanitizer build to fail in inline assembly and caused a current-source GCC 12.2 ASan probe to SEGV in `SHA256AutoDetect()`'s self-test; compiling with the guard passed. `serfloat_tests` (3), `policyestimator_tests` (6), and `util_tests` (78) passed. The ASan/UBSan `float` target completed 100,000 runs with `cov 352`, `ft 414`, no diagnostic, and a 20-input/162-byte corpus. A temporary wrong NaN assertion was killed immediately by seed `98047` and then restored. See `float-sanitizer-fuzz-exclusions.md`.
- Cycle 48 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 863`; tracked and staged state was clean; catalog SHA-256 `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`, protocol SHA-256 `954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0`, and corrected actual-tab TSV SHA-256 `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`; no relevant process was running.
- Cycle 48 selector: `shuf -i 0-98 -n 1` drew `26` (`cross-subsystem-bug-shapes`). The historical seed was upstream merge `e3554bf361` / fix `336f5a738b`, which moved `importdescriptors` reservation before its wallet synchronization wait.
- Cycle 48 verification: the analogous `rescanblockchain` ordering admitted a forced two-success schedule when A held `cs_wallet` during scanning and B was delayed after `BlockUntilSyncedToCurrentChain()`. The old source failed the disposable control with `successes=2`; the repaired source passed it with one success and one `-4` conflict. The durable 1,000-block, ten-pair `wallet_rescan.py` functional test, existing `wallet_importdescriptors.py` suite, rebuilt wallet daemon, redirected `wallet_tests` suite (15 cases), Python syntax, and diff checks passed. Commit `9911bb75a8520990a6e9ca6405269dd634a7a50c` moved the reservation before the wait. The unredirected unit attempt exited 201 only because `/` was 100% full; `TMPDIR` on `/data` passed. See `cross-subsystem-bug-shapes.md`.
- Current HEAD after cycle 48 source/test fix: `9911bb75a8520990a6e9ca6405269dd634a7a50c` (`wallet: reserve rescan before syncing wallet`).
- Current HEAD after cycle 51 source/test fix: `a81f58f3bf` (`descriptor: reject invalid x-only keys during inference`).
- Cycle 49 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 866`; tracked and staged state was clean; catalog SHA-256 `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`, protocol SHA-256 `954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0`, and corrected actual-tab TSV SHA-256 `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`; no relevant process was running.
- Cycle 49 selector: `shuf -i 0-98 -n 1` drew `60` (`reviewer-preference-mining`). The earlier cycle-25 sample is closed; this cycle reopens only the distinct current-master review and technical-recipe cell, with the prior recipe and its hash/semantic deduplication constraints preserved.
- Cycle 49 verification: current PR review evidence from `#35690`, `#35736`, `#35746`, `#35076`, and `#35775` supported actionable error codes, minimal/extensible structured output, real externally injected build-path coverage, and scope-accurate cross-surface documentation. The isolated space/non-ASCII CMake path configured and built `bitcoin-util` in 132 actions; mainnet/regtest output fixtures matched and the extra-argument case returned the expected status/message. `WalletError` and pruning/CI source contracts were independently checked. `makensis` was unavailable, so the Windows deploy target remains a recorded limitation. No source change was justified; see `reviewer-preference-mining.md`.
- Cycle 50 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 867`; tracked and staged state was clean; catalog SHA-256 `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`, protocol SHA-256 `954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0`, and corrected actual-tab TSV SHA-256 `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`; no relevant process was running.
- Cycle 50 selector: `shuf -i 0-98 -n 1` drew `18` (`exhaustive-algebraic`). This is a new identity/property cell; prior secp field/backend parity and cross-subsystem state campaigns do not close the selected parse/arithmetic/normalization/storage/recovery identities.
- Cycle 50 verification: the disposable GCS matrix enumerated 16 included subsets and 16 query subsets of four one-byte elements under fixed `P=3,M=17`, checked `MatchAny(Q) == OR Match(q)`, singleton and empty-query behavior, and checked encoded reconstruction. With the matrix present, `blockfilter_tests` passed 9 cases and 1,555 assertions. Replacing `MatchAny` with `return false;` produced exit 201 and the first failure at `blockfilter_tests.cpp:122`; 606 of 1,056 assertions failed. After removing the disposable test and restoring production source, the original suite passed 8 cases and 499 assertions. See `exhaustive-algebraic.md`; no source change was justified.
- Cycle 51 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 868`; tracked and staged state was clean; catalog SHA-256 `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`, protocol SHA-256 `954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0`, and corrected actual-tab TSV SHA-256 `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`; no relevant process was running.
- Cycle 51 selector: `shuf -i 0-98 -n 1` drew `15` (`public-object-validation`). The prior P2PK off-curve inference defect is closed; this cycle selects the distinct x-only/taproot nested-leaf round-trip cell recorded in that journal's handoff.
- Cycle 51 verification: a tracked taproot tree containing a 32-byte all-zero tapscript key made the old inference path emit an unparseable `tr(...,pk(0000...))` descriptor; the focused test exited 201 before the fix. Adding only the validity guard exposed a null-provider segfault (exit 139), so the direct P2TR branch was also made null-aware. The final mutation removing only the guard again exited 201 with the original parse error. Commit `a81f58f3bf` makes invalid x-only providers unavailable and falls back to `rawtr(...)`. The focused regression passed 1 case/4 assertions; `descriptor_tests` passed 12/30,419; `script_tests/sign_invalid_miniscript` passed 1/1; `miniscript_tests` passed 3/10,692; rebuilt Clang 19 ASan/UBSan `FUZZ=script` completed all 2,537 corpus units with no diagnostics. See `public-object-validation.md`.
- Cycle 52 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 870`; tracked and staged state was clean with only the recorded agent-owned untracked paths; catalog SHA-256 `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`, protocol SHA-256 `954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0`, and corrected actual-tab TSV SHA-256 `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`; no relevant process was running.
- Cycle 52 selector: `shuf -i 0-98 -n 1` drew `64` (`finding-dedup-recurrence`). The fresh cell is transaction-graph/orphan deduplicated accounting; prior wallet reservation, descriptor inference, database iterator, and historical finding fingerprints are excluded.
- Cycle 52 verification: the orphanage, transaction-download, and transaction-graph suites passed with no errors. `txorphanage_sim` completed 3,000 runs (`cov 4893`, `ft 27069`, peak RSS 67 MB) and `txorphan_protected` completed 2,000 runs (`cov 3320`, `ft 17792`, peak RSS 97 MB). A temporary duplicate global-usage mutation aborted at `SanityCheck()` with `calculated_dedup_usage == m_unique_orphan_usage`; the source was restored and the clean orphanage suite passed all 12 cases. The 686-file `txorphan` replay was interrupted at the real resource boundary after 659 units, zero new units, one execution/sec, and peak RSS 127 MB, with no diagnostic. Historical commit copies were deduplicated by patch-id: the orphan-contract subjects collapse to two patch identities, and the txgraph trim-ref subject collapses to one. No new source defect was justified; see `finding-dedup-recurrence.md`.
- Cycle 53 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 872`; tracked and staged state was clean with only the recorded agent-owned untracked paths; catalog SHA-256 `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`, protocol SHA-256 `954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0`, and corrected actual-tab TSV SHA-256 `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`; no relevant process was running.
- Cycle 53 selector: `shuf -i 0-98 -n 1` drew `74` (`memory-pressure-allocator`). This is a fresh memory-lifetime, allocation-failure, and fragmentation cell; prior orphan/global accounting, wallet failure-state, and sanitizer guard cells remain excluded.
- Cycle 53 verification: `prevector` direct allocation and `realloc` growth both reproduced deterministic exit 134 under `RLIMIT_AS=128 MiB`, but the fatal behavior matches `init.cpp:new_handler_terminate()` and the documented chain-safety OOM policy. Normal `prevector`/stream tests passed 30 cases; the normal prevector fuzzer passed 5,000 runs (`cov 925`, `ft 5225`, no diagnostic); the ASan prevector fuzzer passed 3,000 runs (`cov 2883`, `ft 17535`, no diagnostic); and allocator/pool/network/mempool/coins tests passed 102 cases and 1,401,414 assertions. No source change was justified. See `memory-pressure-allocator.md`.
- Cycle 54 gate: fetched `origin/master`; HEAD is `2ca370710135304b0472ffe62bfea9ebfc1ad5d9`; `origin/master` is `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base is `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` is `2 874`; tracked and staged state is clean with only recorded agent-owned untracked paths; catalog SHA-256 is `5c847ef77405df14b7e7e8fa50430d11a71dcbac3d84df66d25a168d1e955ea8`, protocol SHA-256 is `954a67b016918eb2d71c17ae78a12b38f014bb47ed32fe45a0b6f307e5002fc0`, TSV SHA-256 is `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`, and no relevant process is running.
- Cycle 54 source fix: `4f867fc8a3` (`net: bound duplicate global relay backlog`) is the independent finding commit; the selected journal, source, regression, and exact validation are included.
- Cycle 55 gate: fetched state is at HEAD `98150b59231b0e5229a229da2c3a29abd824e5fa`; `origin/master` is `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base is `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` is `2 877`; tracked and staged state is clean with only recorded agent-owned untracked paths; catalog/protocol/TSV hashes are unchanged; no relevant process is running.
- Cycle 55 selector: `shuf -i 0-98 -n 1` -> `78` (`translation-validation`).
- Cycle 55 completion: the SipHash translation-validation cycle was closed at HEAD `dd4cac6cddbf4e6131b7be23c3d65c4bd7553705`; no source defect was justified. See `translation-validation.md`.
- Cycle 56 completion: malformed DB records changed caller outputs before returning `false` in direct lookup and iterator APIs. Commit `260e49e22d` decodes into temporaries and commits only after success; focused, full DB wrapper, dependent persistence/index, normal fuzz, ASan/UBSan, and direct-Read mutation controls passed. See `public-output-failure.md`.
- Cycle 57 completion: the resource-exhaustion sweep closed GETDATA, GETBLOCKTXN, address, block-inventory, and relay-backlog paths as bounded or already fixed. It left two inconclusive response-amplification hypotheses: `getcfilters` loads and queues up to 1,000 indexed filters without an inner send-watermark check, and BIP35 `MEMPOOL` can queue multiple 50,000-entry INV chunks in one send pass. Block-filter, BIP37/mempool, send-queue, unit, functional, fuzz, history, and upstream-tip controls supplied no failure-level reproducer. No source change was justified; see `resource-exhaustion-variants.md`. The handoff commit containing this entry is the latest journal/state commit; resolve its hash with `git rev-parse HEAD`.
- Cycle 54 selector: `shuf -i 0-98 -n 1` drew `60` (`reviewer-preference-mining`). The cycle-25 assertion/lifecycle/fuzz-review clusters and cycle-49 interface/schema/build/documentation cluster are excluded. This cycle selects the separate global-backlog memory-bound and data-structure-lifecycle review cluster from PR `#34628`.
- Cycle 37 selector: `shuf -i 0-98 -n 1` drew `62` (`rejected-finding-resurrection`). The cycle-33 CoinStats cell was excluded; the distinct cell attacked `GetNodeStateStats()` false results through the Qt peer-detail refresh path.
- Cycle 37 verification: `NodeImpl::getNodesStats()` can leave state statistics unavailable when `cs_main` is busy, and `dataChanged` immediately refreshes the selected peer details. The old Qt false path left nine state-only labels unchanged, preserving stale values after a true-to-false refresh. The new `ts.na` clearing branch is built and tested by the Qt 6 suite: AppTests 3, OptionTests 6, URITests 3, and RPCNestedTests 3 all passed. The focused `get_node_state_stats_overwrites_reused_output` control passed 1 case/4 assertions and `git diff --check` passed. No direct GUI fixture currently covers the lock-busy transition; see `rejected-finding-resurrection.md`.
- Cycle 38 selector: the first `shuf -i 0-98 -n 1` draw was `48` (`property-oracle-expansion`) and was excluded because its cycle-28 property cell was closed; rerunning the same selector drew `43` (`option-api-lifecycle`). The distinct cell audited `-txsendrate` registration, clamping, peer-manager consumption, inbound/outbound bucket observation, startup help, RPC help, and functional coverage.
- Cycle 38 verification: the unpatched parent daemon failed the corrected `rpc_net.py` peer-wide help assertion at `getnetworkinfo` with exit 1, while the current rebuilt daemon passed serial v1 and v2 `rpc_net.py` runs. The outbound relay-rate functional control passed. The two stale descriptions were changed to peer-wide wording; no runtime relay behavior changed. See `option-api-lifecycle.md`.
- Cycle 39 gate: fetched `origin/master`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; `origin/master...HEAD` was `2 845`; tracked and staged state was clean; catalog, protocol, and TSV hashes validated; no relevant process was running.
- Cycle 39 selector: `shuf -i 0-98 -n 1` drew `27` (`error-path-state`). Earlier goal-27 cells were excluded; this cycle selected `CWallet::SetAddressBookWithDB` address-book write ordering and its purpose/name transaction boundary.
- Cycle 39 verification: the pre-fix SQLite-trigger regression exited 201 with the database label unchanged but the in-memory label changed. The final regression covered both `WriteName`-after-purpose and `WritePurpose`-first failures. The wallet unit target built; the focused test passed 1 case; `scriptpubkeyman_tests,wallet_tests,wallet_rpc_tests` passed 35 cases; and `git diff --check` passed. Commit `f6c37d8239` stages the record, wraps the public operation in `RunWithinTxn`, and publishes/notifies through the commit listener. The `setlabel` RPC still ignores the bool return and is queued as a distinct error-propagation lead. See `error-path-state.md`.
- Dirty state at initialization: only agent-owned catalog artifacts existed before authoritative files were added.

## Cycle Ledger

| Cycle | Draw/seed | Goal | Status | Evidence | Findings/commits | Next action |
|---|---|---|---|---|---|---|
| 1 | `shuf -i 0-98 -n 1` -> `53` | `statistical-timing` | inconclusive; no confirmed finding | Paired Clang 19 timing probes across AUTO/Debug and ASM-OFF/Release; official secp tests passed. See `statistical-timing.md`. | None | Reopen timing after MSan/Valgrind/compiler coverage; draw the next eligible goal |
| 2 | `shuf -i 0-98 -n 1` -> `66` | `backport-correctness` | dismissed; no confirmed finding | Compared the 31.x CVE-2024-52911 lifetime backport with master; `v31.1` compiled all 496 `test_bitcoin` actions and passed all 3 `validation_block_tests` cases with Boost seed `123456`. See `backport-correctness.md`. | None | Draw the next eligible goal; reopen release backports for other branches or batches |
| 3 | `shuf -i 0-98 -n 1` -> `69` | `backend-differential` | dismissed; no confirmed finding | Fresh current-source Clang 19 x86_64 assembly and ASM-OFF builds both passed the secp tests; a 2048-vector API probe produced identical `f50ba90e4d947dde` digests. See `backend-differential.md`. | None | Draw the next eligible goal; reopen for GCC/32-bit/ECDH/sanitizer coverage |
| 4 | `shuf -i 0-98 -n 1` -> `90` | `historical-knowledge-recipes` | recipe confirmed; no new defect | Extracted a reusable presence-vs-verification-before-assertion recipe from PR `#33014`; wallet-enabled focused functional regression passed with seed `12345`. See `historical-knowledge-recipes.md`. | None | Draw the next eligible goal; deduplicate this recipe against future boundary audits |
| 5 | `shuf -i 0-98 -n 1` -> `53` | `statistical-timing` (reopened) | inconclusive; no confirmed finding | Fresh current-source Clang 19 `-O2` Release assembly and ASM-OFF builds, CPU-pinned for 3 repetitions each; t-statistics changed direction and stayed below 1.8 in absolute value. See `statistical-timing.md`. | None | Draw the next eligible goal; reopen timing only with dudect/ctime/MSan/GCC/architecture or new caller evidence |
| 6 | `shuf -i 0-98 -n 1` -> `42` | `ci-bot-followup` | dismissed; no confirmed finding | All 22 workflow-referenced setup scripts existed; shell/Python entrypoints passed syntax checks; specialized gates matched runner branches. See `ci-bot-followup.md`. | None | Draw the next eligible goal; reopen only with live bot/check-run evidence, changed CI wiring, or a reproduced job failure |
| 7 | `shuf -i 0-98 -n 1` -> `81` | `spec-vector-drift` | dismissed; no confirmed finding | Pinned BIP340 vectors matched the repository after line-ending normalization; Core's fixed-`uint256` vector case passed. See `spec-vector-drift.md`. | None | Draw the next eligible goal; reopen for BIP341/342, MuSig, transport, or changed vector-source evidence |
| 8 | `shuf -i 0-98 -n 1` -> `80` | `fuzz-engine-differential` | dismissed; no confirmed finding | Same 75-file `bech32_roundtrip` corpus under libFuzzer, AFL++ 5.03a, and Honggfuzz 2.6 for 15 seconds each; all completed without crashes or hangs. FuzzTest was unavailable locally. See `fuzz-engine-differential.md`. | None | Draw the next eligible goal; reopen engine differentials only with a distinct target, sanitizer matrix, normalized coverage, or FuzzTest integration |
| 9 | `shuf -i 0-98 -n 1` -> `39` | `deterministic-artifacts` | dismissed; no confirmed finding | Two fresh Silent Payments vector generations matched the checked-in header byte-for-byte and each other (`8d88aead...9cedbbcc`); rebuilt current libsecp256k1 consumer tests passed all 7 Silent Payments tests, including `run_silentpayments_test_vectors`. See `deterministic-artifacts.md`. | None | Draw the next eligible goal; reopen generated-artifact audit only for a distinct generator, toolchain, locale, architecture, or stale-output hypothesis |
| 10 | `shuf -i 0-98 -n 1` -> `52` | `integer-overflow` | confirmed; fixed | Clean HEAD sanitizer run showed `4294968000` narrowing to `704` at both P2P buffer assignments. Range checks, uint64 conversion, focused node-init assertions, normal-build send/receive startup rejects, and repaired sanitizer run verified. See `integer-overflow.md`. | `ad2eb8be3afe4c0c0f1a254555dcda4262dda78b` (`init: reject overflowing P2P buffer arguments`) | Draw the next eligible goal; do not reopen these buffer arguments without new evidence |
| 11 | `shuf -i 0-98 -n 1` -> `67` | `release-version-differential` | dismissed; no confirmed finding | Fresh Clang 19 v31.1 and current HEAD regtest daemons received identical deterministic RPC/parser/genesis vectors. `getblockhash`, block/header serialization, script decoding, invalid-address/transaction errors, and `getblocktemplate` taproot rules matched. Observed deployment, protocol, transaction-rate, warning, and deprecated `fullrbf` field changes were each traced to source commits and release policy. See `release-version-differential.md`. | `673e2ca63f270d83d53b0bd9f0ec793e0e62bcb4` (`journal: record release differential cycle 11`) | Draw the next eligible goal; prefer a distinct release-branch, persistence, wallet, network transcript, or consensus-vector cell |
| 12 | `shuf -i 0-98 -n 1` -> `51` | `invariant-differential` | confirmed; fixed | `bitcoin-util getchainparams` emitted raw internal genesis bytes while RPC emitted the conventional public hash. A clean Clang 19 utility build, all six corrected chain fixtures, and a fresh scratch regtest daemon proved the before/after mismatch and exact post-fix equality. Alias metamorphic probes remained equal. See `invariant-differential.md`. | `c83969dae01c70eef95cf50904e95225404fc2ed` (`bitcoin-util: use public genesis hash format`) | Draw the next eligible goal; do not reopen this output-format issue without new consumer or contract evidence |
| 13 | `shuf -i 0-98 -n 1` -> `61` | `stateful-contract-fuzzer` | confirmed; test oracle fixed; no production defect | Extended the AddrMan operation-sequence fuzzer with bounded intermediate and final serialization byte-stream checkpoints, deterministic table equality, and lookup metamorphic checks. A 2,605-run qa-assets replay, bounded controls, and ASan/UBSan replay passed; an oversized control was transparently stopped at 230 executions for resource cost. See `stateful-contract-fuzzer.md`. | `bfa5a84f0ac8a18ce73bc24c0c280040c67e2a34` (`fuzz: round-trip addrman states during sequences`) | Draw the next eligible goal; do not reopen this exact AddrMan checkpoint without new state or serialization evidence |
| 14 | `shuf -i 0-98 -n 1` -> `21` | `rebuild-recovery-profile` | dismissed; no confirmed product defect or justified optimization | Built a current RelWithDebInfo wallet-disabled `bitcoind`; profiled fixed-time 20,000-block regtest chainstate reindex at 16 MB and 256 MB caches, rebuilt `txindex` plus `coinstatsindex`, and killed/restarted reindex at height 10,000. All completed runs converged to the exact source tip; cache differences were within one-run variance, and automatic restart replayed the partial state successfully. See `rebuild-recovery-profile.md`. | `fdceb1cb047512657a4d22b3023c0e5b59cf527c` (`journal: record rebuild recovery profile cycle 14`) | Draw the next eligible goal; revisit only with an input/output-heavy index workload, repeated samples, or a distinct crash/filesystem schedule |
| 15 | `shuf -i 0-98 -n 1` -> `89` | `bitcoin-p2p-accounting` | confirmed; fixed | `CConnman::StopNodes()` and `ConnmanTestMsg::ClearTestNodes()` left `m_network_conn_counts` populated after deleting peers. A focused regression failed before the fix, then passed after clearing the aggregate under `m_nodes_mutex`; the full `net_tests` suite passed 31 cases and 167,549 assertions, and rebuilt `p2p_handshake`, `cmpctblock`, `p2p_headers_presync`, and `connman` fuzz targets each completed 100 fixed-seed runs. See `bitcoin-p2p-accounting.md`. | `1554aa0f67421f265365fa2397dab43cf8d57e4f` (`net: reset connection counts when stopping peers`) | Draw the next eligible goal; revisit P2P accounting only with sanitized full-corpus, deterministic-coverage, transport v1/v2, or new lifecycle evidence |
| 16 | `shuf -i 0-98 -n 1` -> `8` | `locking-threading` | confirmed; fixed | Scheduler `DumpAddresses()` and shutdown `StopNodes()` could concurrently write `peers.dat`; AddrMan's internal lock did not cover temporary-file creation and final rename, allowing an older periodic snapshot to replace a newer shutdown snapshot. Added a function-local dump mutex. Focused normal and TSan `net_tests` passed; the existing StopNodes regression passed under TSan. Full suite reached completion with unrelated mempool, validation-signal, and external-signer failures. See `locking-threading.md`. | `bd50f35abb99fe15197cbde78e03e9bc2ac6c90d` (`net: serialize peer address dumps`) | Draw the next eligible goal; do not reopen without a distinct persistence schedule, filesystem fault, or scheduler-lifetime reproducer |
| 17 | `shuf -i 0-98 -n 1` -> `24` | `disk-io-amplification` | dismissed for production defect; benchmark gap confirmed | Audited BaseIndex, LevelDB, chainstate, txindex, txospenderindex, and blockfilterindex contracts. Built the open PR #35827 benchmark in a detached Clang 19 RelWithDebInfo worktree. Realistic disk/memory sync pairs showed expected I/O separation: TxIndex 47.3 ms vs 6.6 ms, TxoSpenderIndex 58.7 ms vs 8.0 ms, BlockFilterIndex 53.0 ms vs 13.5 ms; sequential perf counters showed 9,789 writes and 236 fdatasync calls versus 408 and zero in memory. All sync and lookup assertions passed; no persistence-growth or recovery defect was reproduced. See `disk-io-amplification.md`. | None; journal-only handoff, preserving PR #35827 as provenance rather than duplicating it | Draw the next distinct goal; revisit storage only with a larger graph, crash/filesystem schedule, or new source evidence |
| 18 | `shuf -i 0-98 -n 1` -> `27` | `error-path-state` | confirmed; fixed | `ChangeWalletPassphrase` ignored `WriteMasterKey` failure and mutated `mapMasterKeys` before persistence. An exact SQLite master-key-row abort made the old regression return success, reject the old passphrase, and accept the new one; the fixed test returned false, preserved the old passphrase and lock state, and the 18-case scriptpubkeyman suite passed. See `error-path-state.md`. | `55eaf087c189ae871878692fb20a90ac3533084d` (`wallet: preserve passphrase state on write failure`) | Draw the next distinct goal; reopen wallet passphrase handling only with new backend, restart, or encryption-failure evidence |
| 19 | `shuf -i 0-98 -n 1` -> `90` | `historical-knowledge-recipes` (reopened on new evidence) | recipe confirmed; no new production defect | Mined the relay-budget cluster (`68b745e2`, `a13956d2`, `81518c1d`, `dccab6b2`, `ade589fd`) for a reservation-conservation/deferred-eligibility recipe. Held-out `13da611b39` independently matched disconnect/removal accounting; `a39a6d7caf` failed-spend and `4ea3901a88` atomic-dump commits were negative controls. See `historical-knowledge-recipes.md`. | Journal-only handoff; no source change justified | Draw another eligible goal; if 90 repeats, choose a different historical bug-shape cluster |
| 20 | `shuf -i 0-98 -n 1` -> `37` | `build-dead-zones` | confirmed; fixed | Clean `BUILD_FOR_FUZZING=ON`, `ENABLE_IPC=ON`, wallet-enabled GCC configuration built the 510-step fuzz target and included generated IPC plus wallet fuzz sources. An empty `wallet_bdb_parser` input failed before the fix with UBSan at `AutoFile::write`/`fwrite`; empty and non-empty replays passed after the guard. Clang 19 plus installed Cap'n Proto 0.9.2 was an explicit dependency blocker. See `build-dead-zones.md`. | `bbca305738343d518b179da413b3b4ebac0e2d5b` (`fuzz: avoid empty wallet parser write`) | Draw the next distinct goal; revisit only with a different compiler/dependency/feature combination or new sanitizer evidence |
| 21 | `shuf -i 0-98 -n 1` -> `5` | `boundary-off-by-one` | confirmed; fixed | The direct-fetch walk used `<= 16`, admitting a 17th fetchable block and issuing a partial request. The failing-before functional control exposed `getdata` for 17 headers; the final loop continues over already-requested/available ancestors while stopping before the 17th fetchable entry. Exact 16/17 coverage, the full `p2p_sendheaders` scenario, a rebuilt unit target, and all 31 `net_tests` cases passed. See `boundary-off-by-one.md`. | `ee9ad3c5f9588bc0e228c61bc0b9b682517eeee1` (`net: stop direct fetch at peer block limit`) | Draw the next distinct goal; prioritize mixed eligibility, CompactSize max edges, and 32-bit-relevant offset cells |
| 22 | `shuf -i 0-98 -n 1` -> `27` | `error-path-state` (reopened on a distinct state cell) | dismissed; no new production defect | Transaction-download response/rejection/orphan/disconnect contracts and 14 focused unit tests passed. `CoinStatsIndex::CustomAppend` increments subsidy before a mismatch return, but `BaseIndex` contiguous-transition and initialization guards make the mismatch unreachable in supported production transitions. Isolated normal and ASan/UBSan replay of the same 128 qa-assets seeds completed 129 runs each with exit code 0; see `error-path-state.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; retain only the CoinStats internal-contract question and `GetNodeStateStats` failure semantics as lower-priority future hypotheses |
| 23 | `shuf -i 0-98 -n 1` -> `58` | `exact-helper-reuse` | confirmed; fixed | `TxIndex::CustomAppend` and `TxoSpenderIndex::BuildSpenderPositions` duplicated the exact witness-inclusive transaction-size expression already named by `CTransaction::ComputeTotalSize()`. Replaced both call sites; the rebuilt index tests passed all 6 cases and the combined transaction/index run passed all 24. See `exact-helper-reuse.md`. | `8ff6f9dd279ee73e4224bdb88630c1fdb72f9135` (`index: reuse transaction total-size helper`) | Draw the next distinct goal; do not reopen this offset expression unless the transaction-size contract or index backend changes |
| 24 | `shuf -i 0-98 -n 1` -> `82` | `secp-field-scalar-matrix` | dismissed; no confirmed finding | Clean standalone Clang 19 `SECP256K1_ASM=OFF` builds forced int128 and int64 wide multiplication, and all six regular/no-VERIFY/exhaustive binaries passed. A corrected internal-header probe exercised 13 field/scalar edge vectors, inverse/half/mul/add/normalization/reduction, and emitted 754 canonical records. Both outputs matched byte-for-byte with SHA256 `315b522a08b0f7c0ab9fbf21a02c8244143c50d8ee911645f54c8e888db92828`; the two earlier VERIFY failures were harness aliasing and lazy-initialization mistakes. See `secp-field-scalar-matrix.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; reopen representation parity only for GCC, cross-architecture, assembly, sanitizer, or new source evidence |
| 25 | `shuf -i 0-98 -n 1` -> `60` | `reviewer-preference-mining` | recipe confirmed; no new production defect | Mined seven PR review clusters with local history and public GitHub API evidence. Extracted general rules for contract/lifecycle tracing, public-input validation, exact behavioral oracles, fuzz reset/realism, async race-window narrowing, portable fixtures, and dependency hygiene; classified `Assert`/`Assume`, fuzz-target scope, `max/min`/`clamp`, hasher applicability, and style suggestions as contextual. Held-out commits `e9ed898a`, `6ee05c4b`, and `6aa5d8d` independently recovered the rules; `12269112` was a same-feature fixture control. See `reviewer-preference-mining.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; reopen only with new review data, a different subsystem cluster, or a concrete recurrence |
| 26 | `shuf -i 0-98 -n 1` -> `36` | `sanitizer-analysis-matrix` | dismissed; no confirmed production defect | Clang/GCC TSan unit slices passed 63/61 cases with 173,304/174,265 assertions. Clang ASan/UBSan passed 180 focused cases with 390,283 assertions; Clang/GCC ASan fuzz slices passed identical `bech32_roundtrip`, `addrman`, and `crypto` inputs. Clang `scan-build` produced zero plist reports. GCC `-fanalyzer` emitted 29 CWE-457 warnings, all clustered in inline assembly, standard-library/static initialization, assertions, filesystem wrappers, and unreachable code; cross-tool/runtime controls classified them as analyzer artifacts or bounded contract questions. See `sanitizer-analysis-matrix.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; reopen for MSan/instrumented dependencies, Valgrind, direct TokenPipe coverage, a new analyzer warning class, or a new source/configuration path |
| 27 | `shuf -i 0-98 -n 1` -> `15` | `public-object-validation` | confirmed; fixed | A syntactically shaped but off-curve compressed P2PK key was accepted by script inference as `pk(...)`, while descriptor parsing correctly rejected the same key as invalid. The failing-before round-trip test captured the exact parse error. Adding `IsFullyValid()` to `InferPubkey()` made inference fall back to `raw(...)`; the focused regression and normal/Clang ASan/LSan/UBSan descriptor suites passed. See `public-object-validation.md`. | `c125061aa4` (`descriptor: keep inferred invalid pubkeys parseable`) | Draw the next distinct goal; keep x-only/taproot inference as a separate unchecked validation cell |
| 28 | `shuf -i 0-98 -n 1` -> `48` | `property-oracle-expansion` | dismissed; no confirmed finding | Audited transaction, block/header, Merkle, script sigop, and block-filter property surfaces. Existing fuzzers and vectors already supplied independent round-trip, mutation, failure-state, and metamorphic contracts. A temporary empty-transaction witness probe showed only invalid-domain ambiguity; `CheckTransaction` rejects the object with `bad-txns-vin-empty`. Normal script/sigop, block-filter, and transaction suites passed 29, 8, and 18 cases; ASan fuzz smokes passed 1000/323/300 runs for script ops/block filter/block header. See `property-oracle-expansion.md`. | Journal-only handoff; no source change justified | Draw a new eligible goal; revisit serialization only for an accepted-transaction boundary or new witness format, and script sigops only with an independent P2SH/witness reference |
| 29 | `shuf -i 0-98 -n 1` -> `52` | `integer-overflow` (reopened on distinct cells) | confirmed; fixed two independent findings | `-maxsigcachesize=2^44` wrapped its `uint64_t` MiB product to zero and initialized two-element caches; `-limitclustersize` accepted negative/oversized values, then overflowed downstream `*40`/`*4` calculations and terminated with `SIGILL`. Focused before/after daemon probes and unit suites proved both paths. See `integer-overflow.md`. | `f91039eb5c` (`node: reject overflowing validation cache arguments`); `93fef76238` (`mempool: reject overflowing cluster size arguments`) | Draw from the full catalog; do not reopen validation-cache, cluster-size, or prior P2P buffer cells without new evidence |
| 30 | `shuf -i 0-98 -n 1` -> `3` | `current-pr-leftovers` | confirmed; fixed | `-prune=17592186044416` wrapped the MiB-to-byte conversion to zero and started in non-prune mode; the repaired parser rejected it explicitly. The exact `2^44-1` boundary and `-prune=550` control remained valid. Focused node-init, blockmanager, validation-flush, and mempool suites plus rebuilt daemon/unit targets passed. See `current-pr-leftovers.md`. | `5b2e4f5a63` (`node: reject overflowing prune target arguments`) | Draw from the full catalog; keep debug-only `-dbbatchsize`/`-dbcrashratio` option-domain questions queued separately |
| 31 | `shuf -i 0-98 -n 1` -> `90` | `historical-knowledge-recipes` (reopened on a third distinct cluster) | recipe confirmed; no new production defect | Extracted `configurable-parallel-feature-lifecycle` from PR `#35295`: explicit option domain, serial fallback, runtime worker lifecycle, shutdown/reset ordering, fuzz boundary coverage, and performance evidence. Held-out `c7af7477` showed the parser-only incomplete stage; `3873d90` showed why boundary coverage is a separate cell. The focused chainstate option suite passed 22 cases and 2068 assertions. See `historical-knowledge-recipes.md`. | Journal-only handoff; no source change justified | Draw from the full catalog; if goal 90 repeats, choose a fourth unrelated history cluster |
| 32 | first draw `48` ineligible; rerun `shuf -i 0-98 -n 1` -> `3` | `current-pr-leftovers` (reopened on queued debug-option cells) | confirmed; fixed two independent findings | Negative `-dbcrashratio` reached `FastRandomContext::randrange` during chainstate replay and aborted status 134; negative `-dbbatchsize` narrowed to `UINT64_MAX` and suppressed partial batches. Repaired commands return explicit errors. Focused arguments passed 51 assertions; full chainstate/flush/mempool suites passed 49 cases/60,503 assertions. See `current-pr-leftovers.md`. | `ddbbb0aadb` (`node: validate chainstate debug arguments`); `5754033019` (`node: reject negative database batch sizes`) | Draw from the full catalog; do not reopen these two debug-option domains without new parser or database lifecycle evidence |
| 33 | `shuf -i 0-98 -n 1` -> `62` | `rejected-finding-resurrection` | dismissed again; no confirmed production defect | Reopened the cycle-22 `CoinStatsIndex::CustomAppend` partial-state cell. Current BaseIndex dispatch still enforces contiguous transitions or treats append failure as fatal; the subsidy mutation occurs before a non-persisting return and is destroyed during shutdown. Current index lifecycle tests passed. See `rejected-finding-resurrection.md`. | Journal-only handoff; no source change justified | Draw from the full catalog; do not reopen this CoinStats cell without a new append caller, persistence path, or nonfatal shutdown contract |
| 34 | `shuf -i 0-98 -n 1` -> `50` | `fuzz-introspector` | confirmed; fixed coverage blocker | Empty `process_messages` corpus plus default `CoverageFuzz.cmake` invocation reduced the target to an empty-input initialization pass. Passing the existing bounded empty-corpus option restored actual fuzz executions in the targeted smoke. See `fuzz-introspector.md`. | `7d1a7bdda6` (`fuzz: run empty corpora during coverage collection`) | Draw from the full catalog; do not reopen this runner/coverage cell unless corpus policy or script wiring changes |
| 35 | `shuf -i 0-98 -n 1` -> `8` | `locking-threading` | dismissed; no confirmed finding | Transaction/inventory/mempool lock scopes, V2 transport nesting, and scheduler shutdown lifetime were checked against annotations and callers. TSan `net_tests` and `scheduler_tests` passed; one real `process_message` seed passed the TSan custom fuzz driver. See `locking-threading.md`. | Journal-only handoff; no source change justified | Draw from the full catalog; exclude only the closed cycle-16 `DumpAddresses()` and `ForEachNode()` cells unless new evidence changes their contracts |
| 36 | `shuf -i 0-98 -n 1` -> `66` | `backport-correctness` (reopened on the queued #35331 release-batch cell) | dismissed; no confirmed finding | The v31.1 backport batch preserved proxy, private-broadcast, settings, MuSig, LevelDB, and background-compaction contracts. The release build and complete unit suite passed; targeted functional and direct LevelDB compaction controls passed. See `backport-correctness.md`. | Journal-only handoff; no source change justified | Draw from the full catalog; if goal 66 repeats, choose 28.x/29.x/30.x or another release batch rather than reopening #35331 |
| 37 | `shuf -i 0-98 -n 1` -> `62` | `rejected-finding-resurrection` (distinct `GetNodeStateStats()`/Qt refresh cell) | confirmed; fixed | A busy `cs_main` made state statistics unavailable during a `dataChanged` refresh, but the Qt false path retained stale state-only labels from the previous successful refresh. The `ts.na` clearing branch built successfully; the full Qt test binary passed all 15 cases and the focused API reset control passed 1 case/4 assertions. See `rejected-finding-resurrection.md`. | Source/journal/state commit recorded after verification | Draw from the full catalog; exclude this fixed Qt stale-state cell and the closed CoinStats cell unless a new caller or contract appears |
| 38 | first draw `48` ineligible; rerun `shuf -i 0-98 -n 1` -> `43` | `option-api-lifecycle` (`-txsendrate` metadata/runtime scope cell) | confirmed; fixed | The option initialized both inbound and outbound relay buckets, but startup help and `getnetworkinfo` described it as inbound-only. The corrected help regression failed against the unpatched parent and passed after the two descriptions were widened; serial v1/v2 RPC runs and the outbound relay-rate control passed. See `option-api-lifecycle.md`. | Source/journal/state commit recorded after verification | Draw from the full catalog; exclude this fixed `-txsendrate` metadata cell and choose another option/API lifecycle surface |
| 39 | `shuf -i 0-98 -n 1` -> `27` | `error-path-state` (address-book transaction/state cell) | confirmed; fixed | The old path mutated `m_address_book` before persistence and wrote purpose/name as separate transactions. An exact SQLite trigger failed the old regression with exit 201; the fixed path stages state, uses `RunWithinTxn`, and publishes/notifies on commit. The focused regression covered both write failure edges; wallet suites passed 35 cases. See `error-path-state.md`. | `f6c37d8239` (`wallet: make address book writes transactional`) | Draw from the full catalog; keep `setlabel` failure propagation as a separate queued lead and do not reopen this cell without backend/restart/commit evidence |
| 40 | `shuf -i 0-98 -n 1` -> `45` | `constant-time-boundary` (EllSwift secret auxiliary-input cell) | confirmed; fixed | The old declassification preceded the optional auxiliary hash write. A fresh Clang 19 MSan ctime harness marked a separate `auxrnd32` secret and failed at `xswiftec_inv_var:198`; the post-fix ctime run passed with no MSan diagnostic. Normal crypto, regular/exhaustive secp, unit build, and diff checks passed. See `constant-time-boundary.md`. | `20286797faf270438eed7824891228cba44929ca` (`secp256k1: fix EllSwift auxiliary input boundary`) | Draw the next distinct goal; keep other declassification sites and the cycle-39 `setlabel` lead separate |
| 41 | `shuf -i 0-98 -n 1` -> `59` | `cpp-supply-chain` (release verifier trusted-signature gate) | confirmed; fixed | `depends` archive hashes, vendored subtree checks, and Guix signatures/hashes were inventoried. The verifier accepted three valid untrusted signatures and, separately, three repeated signatures from one trusted key for a threshold of three. The post-fix controls reject both and require distinct trusted signer keys; Python syntax and diff checks passed. Full network integration is a recorded external-environment limitation. See `cpp-supply-chain.md`. | `eec2561d8ec33e73e6bb1e8b6a438223faad41c6` (`verify-binaries: count only trusted signatures`); `23cd641d6cc66d5d1ba73ba23c878109baf32236` (`verify-binaries: count distinct trusted signer keys`) | Draw the next distinct goal; continue supply-chain review only on compiler/tool downloads, generated inputs, license gates, or release-manifest evidence |
| 42 | `shuf -i 0-98 -n 1` -> `13` | `secret-lifetime-zeroization` (RPC cookie and MuSig secret temporaries) | confirmed; fixed three independent findings | Raw RPC cookie entropy, MuSig session randomness, and a MuSig partial-signing keypair were ordinary stack objects with secret-bearing lifetimes spanning error returns. Existing secure allocator/deleter contracts, source/API dataflow, and history established applicability. The rebuilt unit target, focused RPC/key/BIP328 tests, and bundled libsecp tests passed. The full unit run exited 201 on unrelated mempool, `/tmp` filesystem, and external-signer configuration failures; `rpc_users.py` was blocked by disk-space checks. See `secret-lifetime-zeroization.md`. | `c5ac9e13dc` (`rpc: cleanse cookie entropy temporary`); `8a8da25a59` (`musig: cleanse nonce entropy temporary`); `1b1d18722c` (`musig: cleanse partial-signing keypair`) | Draw the next distinct goal; revisit compiler/ctime/checkmem evidence for these buffers only after selecting the next catalog surface |
| 43 | `shuf -i 0-98 -n 1` -> `32` | `history-incomplete-fixes` (wallet migration persistence writes) | confirmed; fixed analogous unchecked write failures | History from `af041c...`, `c98fc...`, `a277f...`, and `fd44d...` established that migration writes are durable contract boundaries. Current `ApplyMigrationData` ignored local/watch-only transaction writes, watch-only order-position writes, and external address-book writes despite `bool` contracts. The SQLite-trigger regression passed after the fix and failed under a temporary mutation restoring the swallowed local rewrite. The rebuilt unit target and focused test passed; the full wallet suite exited 201 after host low-disk scan failures, and the functional migration test was not run for the same reason. See `history-incomplete-fixes.md`. | `a7d70e7806c04b4f5615bd39c0efa34136ce5c4e` (`wallet: report migration write failures`) | Draw the next distinct goal; inspect remaining migration-side writes outside `ApplyMigrationData` only if a new history cell is selected |
| 44 | `shuf -i 0-98 -n 1` -> `53` | `statistical-timing` (ECDH/Schnorr/MuSig API and ctime keypair boundary) | confirmed; fixed one constant-time boundary; timing-only screen inconclusive | Release probes across assembly-auto and ASM-OFF used paired raw traces, same-key controls, and an equal-weight key pair. No stable timing class effect survived backend/repetition comparison. The independent MSan ctime extension tainting `secp256k1_keypair` failed in MuSig counter nonce generation at `pubkey_load:261`; declassification after `keypair_pub` fixed it, and the mutation reproduced the old stack. Unit crypto tests, regular/noverify/exhaustive secp tests, and diff checks passed. See `statistical-timing.md`. | `8c8e8a8f9f` (`secp256k1: declassify MuSig counter public key`) | Draw the next distinct goal; reopen timing only for Valgrind/dudect/GCC/architecture/LTO or a new caller evidence cell |
| 45 | `shuf -i 0-98 -n 1` -> `95` | `database-semantics-differential` (LevelDB iterator-status cell) | confirmed; fixed | LevelDB requires callers to inspect `Iterator::status()` after `Valid()` becomes false, but `CDBIterator::Valid()` previously treated read errors as ordinary exhaustion. A deterministic memenv read fault failed the old implementation, passed after `HandleError(status)` propagation, and failed under a mutation restoring the old method. Unit, dbwrapper, dependent index/chainstate suites, and diff checks passed. See `database-semantics-differential.md`. | `2340c73ef0` (`dbwrapper: preserve iterator read errors`) | Draw the next distinct goal; keep iterator status closed and revisit batch/recovery/sync/comparator cells only |
| 46 | `shuf -i 0-98 -n 1` -> `53` | `statistical-timing` (GCC compiler/backend/optimization cell) | inconclusive; no confirmed finding | GCC 12.2 `-O2`/`-O3` AUTO/OFF probes covered ECDH, Schnorr, and MuSig with two key classes. No stable t-statistic direction or output divergence appeared; official GCC AUTO/OFF `tests` and `noverify_tests` passed. See `statistical-timing.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; reopen timing only for new tool/compiler/architecture/caller evidence |
| 47 | `shuf -i 0-98 -n 1` -> `98` | `float-sanitizer-fuzz-exclusions` (exceptional values, active sanitizer exemptions, and fuzzer oracle) | dismissed for new defect; existing SHA256 guard independently confirmed necessary | Removing the SHA256 SSE4 ASan guard reproduced the documented Clang inline-assembly compile failure and GCC ASan self-test SEGV; the guarded current-source probes passed. `serfloat_tests` (3), `policyestimator_tests` (6), `util_tests` (78), and a 100,000-run ASan/UBSan `float` fuzz stress passed. A wrong canonical-NaN assertion was killed by seed `98047`, then restored. See `float-sanitizer-fuzz-exclusions.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; keep this SHA256 guard closed unless compiler/runtime evidence changes and do not reopen the float oracle without a new input path |
| 48 | `shuf -i 0-98 -n 1` -> `26` | `cross-subsystem-bug-shapes` (wallet rescan reservation ordering) | confirmed; fixed | The current `rescanblockchain` RPC waited for wallet notifications before reserving. A forced lock schedule reproduced two successful concurrent rescans; moving reservation first closed it. The focused rescan test, existing import-descriptor concurrency suite, rebuilt daemon, redirected wallet unit suite (15 cases), and diff checks passed. See `cross-subsystem-bug-shapes.md`. | `9911bb75a8520990a6e9ca6405269dd634a7a50c` (`wallet: reserve rescan before syncing wallet`) | Draw the next distinct goal; keep this wallet reservation cell closed unless a new rescan caller or lifecycle path appears |
| 49 | `shuf -i 0-98 -n 1` -> `60` | `reviewer-preference-mining` (current-master technical review recipe) | recipe confirmed; no new production defect | Post-cycle-25 review evidence supported actionable interface categories, minimal/extensible structured output, real CI boundary coverage, and scope-accurate documentation. The isolated space/non-ASCII path build completed 132 actions; `bitcoin-util` fixtures and negative argument behavior passed. `makensis` was unavailable, so the Windows deploy target remains unrun. See `reviewer-preference-mining.md`. | Journal-only handoff; no source change justified | Draw the next distinct goal; preserve `actionable-interface-minimal-schema-boundary-realism` unless new evidence changes it |
| 50 | `shuf -i 0-98 -n 1` -> `18` | `exhaustive-algebraic` (bounded identities and property-oracle sensitivity) | dismissed for new defect; cell closed | The GCS `MatchAny` OR identity, singleton/empty behavior, and checked encoded reconstruction passed a 256-pair exhaustive matrix. A `return false` implementation mutation failed at the singleton oracle and aggregate checks; restored production tests passed. | Journal-only handoff; no source change justified | Draw the next distinct goal; do not reopen this cell without a new backend, parameter domain, or regression signal |
| 51 | `shuf -i 0-98 -n 1` -> `15` | `public-object-validation` (x-only/taproot nested-leaf inference) | confirmed; fixed | A tracked taproot tree with an off-curve 32-byte leaf key produced an unparseable `tr(...,pk(...))` descriptor and could wrap a null provider. `InferXOnlyPubkey` now validates curve membership, the direct P2TR branch checks the provider, and inference falls back to `rawtr(...)`. Focused, descriptor, adjacent script/miniscript, ASan/UBSan corpus, mutation, and diff checks passed. See `public-object-validation.md`. | `a81f58f3bf` (`descriptor: reject invalid x-only keys during inference`) | Draw the next distinct goal; keep this tracked-leaf cell closed unless a new caller, parser mode, backend, or regression signal appears |
| 52 | `shuf -i 0-98 -n 1` -> `64` | `finding-dedup-recurrence` (transaction-graph/orphan deduplicated accounting) | dismissed; no new source defect | Per-announcement and per-wtxid orphan accounting matched their contracts; focused suites and independent fuzz models passed. The deliberate global-usage overcount mutation aborted in `SanityCheck()`. Historical branch copies collapsed by semantic fingerprint and patch-id. The general orphan corpus was stopped at a recorded CPU/resource boundary with no diagnostic. See `finding-dedup-recurrence.md`. | Journal-only handoff; no source change justified | Cycle 53 goal 74 is active |
| 53 | `shuf -i 0-98 -n 1` -> `74` | `memory-pressure-allocator` | dismissed; no new source defect | `prevector` direct and realloc allocation failures were reproduced under a 128 MiB address-space limit and matched the deliberate fatal-OOM policy. Normal and ASan fuzz runs plus 102 allocator/accounting test cases found no new corruption, bound bypass, or accounting drift. See `memory-pressure-allocator.md`. | Journal-only handoff; no source change justified | Re-check the gate and draw cycle 54 from the full catalog |
| 54 | `shuf -i 0-98 -n 1` -> `60` | `reviewer-preference-mining` | confirmed; fixed | PR `#34628` review concern reproduced a remotely reachable duplicate-retention defect: 35 batched identical ForceRelay messages with `-txsendrate=1` left four vector entries after token exhaustion. Unique `std::set<Wtxid>` global backlogs reduce this to one pending identity while preserving the mining-order extractor. Functional, unit, normal fuzz, and ASan/UBSan controls passed; see `reviewer-preference-mining.md`. | `4f867fc8a3` (`net: bound duplicate global relay backlog`) | Draw cycle 55 from the full catalog; keep this relay cell closed unless a new caller, container/backend, or recurrence appears |
| 55 | `shuf -i 0-98 -n 1` -> `78` | `translation-validation` | dismissed; no new source defect | Supported Clang 19 and GCC 12 produced the same checksum for 100,004 fixed-width SipHash cases at `-O0/-O1/-O2/-O3/-Os`, ThinLTO, and LTO; Clang 19 IR passed `opt-19` verification at `-O0` and `-O3`; the hash unit suite passed 4 cases; and normal/ASan/UBSan probe and integer-fuzzer controls found no diagnostic. Clang 14 failed only at the documented unsupported compiler boundary. See `translation-validation.md`. | Journal-only handoff; no source change justified | Draw cycle 56 from the full catalog; reopen only with a new compiler/backend/architecture/transformation or minimized counterexample |
| 56 | `shuf -i 0-98 -n 1` -> `46` | `public-output-failure` | confirmed; fixed | A six-byte truncated record changed the first field of a caller output from its sentinel to `1` while all three DB APIs returned `false`. Commit `260e49e22d` makes decode transactional while preserving exception/configuration and non-assignable type behavior. Focused, full DB wrapper, dependent persistence/index, normal fuzz, ASan/UBSan, and direct-Read mutation controls passed. See `public-output-failure.md`. | `260e49e22d` (`dbwrapper: preserve outputs on decode failure`) | Draw cycle 57 from the full catalog; keep this persistence output cell closed unless a new serializer, caller, backend, or recurrence appears |
| 57 | `shuf -i 0-98 -n 1` -> `7` | `resource-exhaustion-variants` | inconclusive; no source change justified | GETDATA, GETBLOCKTXN, block inventory, address relay, compact-filter, mempool-response, and global relay-backlog paths were traced with explicit wire, queue, mempool, and indexed-state bounds. Block-filter and BIP37 functional tests, the send-queue contract test, block-filter unit/index tests, and the production block-filter fuzzer passed. See `resource-exhaustion-variants.md`. | Journal-only handoff; no source change justified | Cycle 58 is next |
| 58 | `shuf -i 0-98 -n 1` -> `32` | `history-incomplete-fixes` (current output-contract follow-up) | confirmed; fixed | `BlockManager::ReadBlockUndo` deserialized into caller-owned output before validating the trailing checksum. A scratch undo file with one flipped checksum byte reproduced the old loss of a pre-seeded `CBlockUndo`; the fixed local decode/publish boundary preserved both output sizes. Focused block-manager regression passed with 11 assertions; full `blockmanager_tests` passed 12 cases and 128 assertions. See `history-incomplete-fixes.md`. | `3e4ec4e7ef0f216c09c10b1d577fc1517a043434` (`blockstorage: publish undo data after checksum verification`) | Recheck the gate and draw cycle 59 from the full catalog |
| 59 | `shuf -i 0-98 -n 1` -> `45` | `constant-time-boundary` (MuSig session/keypair declassification cell) | dismissed; no new source defect | Static contract/dataflow review found session state public, secret nonce scalars retained as undefined, and only public metadata declassified. Three temporary source/harness mutations each failed with an MSan first-invalid trace; restored ctime, regular, and exhaustive tests passed. See `constant-time-boundary.md`. | Journal-only handoff; no source change justified | Recheck the gate and draw cycle 60 from the full catalog |
| 60 | `shuf -i 0-98 -n 1` -> `84` | `secp-nonce-session` (MuSig invalid-argument ordering) | confirmed; fixed | `secp256k1_musig_nonce_gen` checked all-zero session randomness before required `pubnonce`, so two invalid arguments bypassed the illegal callback. The old-source mutation failed at `_calls_to_callback == 1` with exit 134; the fixed focused API test and full 16-iteration libsecp suite passed. See `secp-nonce-session.md`. | `765e82e8ab` (`secp256k1: validate MuSig nonce output argument`) | Recheck the gate and draw cycle 61 from the full catalog |
| 61 | `shuf -i 0-98 -n 1` -> `22` | `full-sync-ibd-profile` (local-peer IBD and debug-check attribution) | dismissed; no source defect | Five fresh sinks reached the expected 20,000-block/20,001-transaction tip. Default regtest consistency checks dominated the call-stack profile; `-checkblockindex=100` and `=0` controls reproduced the documented frequency effect. 16/64/256 MB cache timings were non-monotonic within noise. See `full-sync-ibd-profile.md`. | Journal-only handoff; no source change justified | Recheck the gate and draw cycle 62 from the full catalog |
| 62 | `shuf -i 0-98 -n 1` -> `26` | `cross-subsystem-bug-shapes` (shared candidate-set mutation) | dismissed; no reachable source defect | The `b0ce659a98` fix shape resembles `PreciousBlock` because both mutate shared `nSequenceId` values used by ordered candidate sets. However, the historical chainstate's `TargetBlock` contract excludes active snapshot descendants, and `PreciousBlock` ignores lower-work blocks; a valid block cannot be in both sets in the tested configuration. The controlled active-child setup showed background counts of zero before mutation. A temporary source/test model was discarded after this reachability check. See `cross-subsystem-bug-shapes.md`. | Journal-only handoff; no source change justified | Recheck the gate and draw cycle 63 from the full catalog |
| 63 | `shuf -i 0-98 -n 1` -> `59` | `cpp-supply-chain` (Guix source and detached-signature archive provenance) | confirmed; fixed | Existing archives under the writable `DIST_ARCHIVE_BASE` were extracted without checking that their bytes matched the current repository `HEAD`; the digest was recorded only after the build. A stale archive from `HEAD^` was accepted by the old setup path, while the shared verifier now rejects stale and byte-tampered archives and accepts current-HEAD archives. `bash -n`, diff checks, and targeted source/codesign helper replays passed; `guix` and `shellcheck` were unavailable. See `cpp-supply-chain.md`. | `2ac7b583af3a8935962d75b8125fd1c67675268b` (`guix: verify cached Git archives before extraction`) | Recheck the gate and draw cycle 64 from the full catalog; keep this archive-integrity cell closed |
| 64 | `shuf -i 0-98 -n 1` -> `18` | `exhaustive-algebraic` (compact target canonicalization) | dismissed; no source defect | An independent `cpp_int` matrix passed 7,515 assertions across 1,502 boundary/generated values; arithmetic and proof-of-work suites passed 18,255 and 1,085 assertions; ASan/UBSan integer and pow smoke runs completed 1,000 and 250 runs. Sign-carry and decoder-shift mutations were killed and restored. See `exhaustive-algebraic.md`. | Journal-only handoff; no source change justified | Recheck the gate and draw cycle 65 from the full catalog |
| 65 | `shuf -i 0-98 -n 1` -> `57` | `local-reasoning-domain` (AddrMan linked-IPv4 network classification) | confirmed; fixed | `GetNetwork()` is the transport-level classifier used by AddrMan counts and `Select`, while `GetNetClass()` remains the legacy public filter for `GetAddr`. The current-branch `Select` postcondition and duplicate test/fuzzer oracles used the wrong classifier and aborted on valid linked-IPv4 selections. The retained four-form regression, focused/full suites, old-source mutation, rebuilt fuzzer, and empty-corpus smoke prove the correction; the large corpus replay stopped at a documented resource boundary after an unrelated serialization assertion. See `local-reasoning-domain.md`. | `b2d858ae4e` (`addrman: match Select contract to transport network`) | Recheck the gate and draw cycle 66 from the full catalog |
| 66 | `shuf -i 0-98 -n 1` -> `40` | `multi-agent-adjudication` (global relay queue and ForceRelay recipient transitions) | dismissed; no source change | Investigator A found a real observed late-recipient delivery gap; Investigator B found explicit original queue intent to clear work for future peers and no durable ForceRelay replay contract. Current-recipient, no-recipient, filter/fee/known, fuzzer, mutation, restored-control, and unit evidence support an intentional best-effort boundary. The broad ForceRelay documentation wording remains a future contract-review lead. See `multi-agent-adjudication.md`. | Journal-only handoff; no source change justified | Recheck the gate and draw cycle 67 from the full catalog |
| 67 | `shuf -i 0-98 -n 1` -> `66` | `backport-correctness` (29.x #34370 Berkeley wallet file ownership and cleanup) | dismissed; no confirmed finding | `7475d134f6` and the 28.x rebased `29abedc97b` have identical patch-id and semantic source hunks. BDB-enabled v29.4 built successfully; focused wallet/database/filesystem tests and corrected `tool_wallet.py --legacy-wallet` passed. `wallet_migration.py` skipped because previous-release binaries were unavailable. See `backport-correctness.md`. | Journal-only handoff; no production source change justified | Recheck the gate and draw cycle 68 from the full catalog |
| 68 | `shuf -i 0-98 -n 1` -> `2` | `assertion-invariant-audit` (release-reachable assumption and invariant cells) | confirmed; fixed | `GETBLOCKTXN` asserted that a recent block read could not fail, but local corruption/I/O/hash validation can make `ReadBlock` return false. A scratch block-header corruption probe killed the unpatched daemon; the fixed path logged the failure and disconnected only the peer. DifferenceFormatter/TransactionsRequest guards were independently covered by focused tests, mutation, and fuzz. The full compact-block functional suite passed. See `assertion-invariant-audit.md`. | `6fbcd16491` (`net: handle GETBLOCKTXN block read failures`) | Recheck the gate and draw cycle 69 from the full catalog |
| 69 | `shuf -i 0-98 -n 1` -> `27` | `error-path-state` (standalone `setlabel` RPC failure propagation) | confirmed; fixed | `SetAddressBook` returns false without publishing state after a database write failure, but `setlabel` discarded that result. A mock SQLite `NAME`-row fault preserved the old label and made the unpatched RPC return success; the fixed RPC raises `RPC_WALLET_ERROR`. Adjacent generation/import/enrichment callers remain contract-sensitive and are not assumed equivalent. See `error-path-state.md`. | `ca5f4d5279` (`wallet: report setlabel address-book write failures`) | Recheck the gate and draw cycle 70 from the full catalog |
| 70 | `shuf -i 0-98 -n 1` -> `94` | `bindings-ffi-parity` (C++ kernel wrapper opaque pointer-array boundary) | confirmed; fixed | `btck::PrecomputedTransactionData` reinterpreted `TransactionOutput` objects as `const btck_TransactionOutput**`, relying on the current object layout instead of constructing the C API's documented opaque-pointer array. The explicit pointer bridge passed the focused and full kernel suites; restoring the old cast plus a temporary one-byte layout mutation made the two-output taproot verifier exit 139. See `bindings-ffi-parity.md`. | `0ab4d16796` (`kernel: bridge spent output handles explicitly`) | Recheck the gate and draw cycle 71 from the full catalog |
| 71 | `shuf -i 0-98 -n 1` -> `53` | `statistical-timing` (ElligatorSwift XDH and Silent Payments secret callers) | dismissed; no new source defect | Clang 19 AUTO/OFF matched probes, module suites, MSan ctime, source review, and the intentional declassification-removal control found no new secret-dependent defect. The scan-only timing signal belongs to documented declassified public-output arithmetic, and the probe's fixed nonmatching output is not a valid payment-recognition oracle. See `statistical-timing.md`. | Journal/probe close snapshot; no source change justified | Recheck the gate and draw the next distinct goal |
| 72 | `shuf -i 0-98 -n 1` -> `84` | `secp-nonce-session` (failure/retry and state-binding cell) | dismissed; no new source defect | The standalone state-machine probe passed valid signing, failure-state, nonce-consumption, malformed-binding, infinity, duplicate-key, and Schnorr callback controls under normal and ASan/UBSan-linked runs. MuSig/Schnorr module tests passed 4 iterations; the full libsecp256k1 test binary passed 16 iterations. Static contracts explain the observed deliberate nonce invalidation and output behavior. See `secp-nonce-session.md`. | Journal/probe close snapshot; no source change justified | Start cycle 73 from the next draw |
| 73 | `shuf -i 0-98 -n 1` -> `54` | `raai-resource-leaks` (constructor/error/cancellation ownership cell) | confirmed; fixed | `CDBWrapper` leaked LevelDB cache, filter policy, logger, and related allocations when construction threw before its destructor became active. `LevelDBContext` now owns idempotent cleanup, `GetOptions()` is exception-safe, and the focused normal/ASan/LSan suites plus standalone negative control prove the change. The broader dependent suite was blocked by a full root filesystem. See `raai-resource-leaks.md`. | `548d8cc8f8` source/test/probe/journal; `e8db2906f7` close snapshot | Recheck the gate and draw the next distinct goal |
| 74 | `shuf -i 0-98 -n 1` -> `37` | `build-dead-zones` (wallet-off, IPC-off/monolithic, test/bench, and generated-source matrix) | dismissed; no new source defect | Excluded cycle 20's GCC wallet-parser/IPC fuzz cell. Five isolated configuration families matched their effective option contracts: wallet-off/on monolithic, wallet-off IPC-on, fuzz-only wallet-off, and GUI wallet-off. Target graphs, generated headers, test/bench/fuzz registries, focused unit/IPC/Qt tests, and `/data`-backed fuzz smokes passed. See `build-dead-zones.md`. | Journal-only close; no source change justified | Draw the next eligible goal |
| 75 | `shuf -i 0-98 -n 1` -> `15` | `public-object-validation` (nested descriptor keys and wrapper/output parity) | confirmed; fixed | Full compressed keys in P2TR expanded as x-only script keys but `ConstPubkeyProvider` looked up only the parity-specific key ID. The separate lookup flag preserves descriptor spelling while using `GetKeyByXOnly()`; direct/nested Miniscript, opposite-parity, NUMS, and script-equivalence regression controls passed. See `public-object-validation.md`. | `d02e22867b` (`descriptor: use x-only lookup for Taproot compressed keys`) | Recheck the gate and draw the next eligible goal |
| 76 | initial `37` rejected; retry `shuf -i 0-98 -n 1` -> `26` | `cross-subsystem-bug-shapes` (failure-publication and lifecycle asymmetry) | confirmed; fixed | The compact-block announcement path asserted after `ReadBlock` failed, so a reachable disk corruption/truncation/I/O failure could abort the node. The fix logs the failure, marks the peer for disconnect, and returns before compact-block publication. Candidate 1 (`dumptxoutset` rename) was dismissed because the throwing filesystem API already propagates an RPC error; candidate 2 (package fee metadata) was dismissed after all call sites were classified. The focused regression, old-source assertion mutation, full normal `net_tests`, and full TSan `net_tests` passed as documented in `cross-subsystem-bug-shapes.md`. | `ddbb88ed12` (`net: disconnect on compact block announcement read failure`) | Recheck the gate and draw the next eligible goal |
| 77 | `shuf -i 0-98 -n 1` -> `57` | `local-reasoning-domain` (relationship and state-domain follow-up) | dismissed; no source change | Four distinct cells were checked: P2P transport/public network identity, BaseIndex commit snapshot versus flushed chainstate, transaction-download peer/orphan ownership, and descriptor x-only/full-key identity. Existing contracts, lifecycle serialization, deterministic controls, and independent focused suites found no new reachable mismatch. See `local-reasoning-domain.md`. | Journal-only close snapshot; no source change justified | Recheck the gate and draw the next eligible goal |
| 78 | initial `2` rejected; retry `shuf -i 0-98 -n 1` -> `36` | `sanitizer-analysis-matrix` (distinct tool/configuration reopen) | confirmed; fixed | Cycle 26's completed matrix was not repeated. A fresh MSan build exposed an uninstrumented libstdc++ runtime boundary; a libc++/MSan TokenPipe probe independently reproduced `TokenWrite()` returning `TS_ERR` for EPIPE, while the public contract and daemon signal policy require `TS_EOS`. The minimal mapping passed the normal 6-assertion regression, full 3,991-assertion utility suite, TSan, direct MSan probe, scan-build, and GCC analyzer controls. See `sanitizer-analysis-matrix.md`. | `c28bbc9580` (`util: report broken token pipe as end-of-stream`) | Recheck the gate and draw the next distinct goal |
| 79 | `shuf -i 0-98 -n 1` -> `80` | `fuzz-engine-differential` (distinct parse_numbers engine comparison) | dismissed; no new source defect | Excluded cycle 8's completed `bech32_roundtrip` comparison. Current-head libFuzzer, AFL++, and Honggfuzz used the same 220-file corpus and 15-second controls; native metric differences were engine-specific, all retained outputs passed current Clang 19 ASan replay, and cross-engine transfer found no crash, hang, corpus corruption, or semantic mismatch. FuzzTest remained unavailable. See `fuzz-engine-differential.md`. | Journal/state-only close; no production source change justified | Recheck the gate and draw the next eligible goal |
| 80 | `shuf -i 0-98 -n 1` -> `33` | `external-vulnerability-variants` (advisory and related-project fix variants) | dismissed; no new source defect | A1-A4 were dismissed after current code/history tracing and focused cache, validation, logging, mempool, and AddrMan suites. A6's private-broadcast proxy-preservation fix passed both unit and functional regression tests. A7's wallet-tool cleanup passed; the full previous-release migration harness is unavailable because its old binaries are absent. See `external-vulnerability-variants.md`. | Journal/state-only close; no production source change justified | Recheck the gate and draw the next eligible goal |
| 81 | `shuf -i 0-98 -n 1` -> `6` | `serialization-untrusted-input` (length, encoding, parser, and failure-state audit) | in progress | Trace untrusted bytes through allocations, casts, loops, canonicalization, object mutation, and later assumptions. Exclude prior parser cells unless a distinct caller or trust boundary is proven. See `serialization-untrusted-input.md`. | Start state pending | Complete the parser/serialization cycle and then draw the next eligible goal |
| 82 | `shuf -i 0-98 -n 1` -> `7` | `resource-exhaustion-variants` (oversized locator count) | confirmed; fixed | `getblocks`/`getheaders` accepted a huge CompactSize locator count into generic vector deserialization before the later 101-entry policy check, reserving about 5 MiB. `ReadBlockLocator` now rejects the count first; normal, TSan, and focused regressions passed. See `resource-exhaustion-variants.md`. | `1e0091464c` (`net: reject oversized locators before deserialization`) | Draw the next eligible goal |
| 83 | `shuf -i 0-98 -n 1` -> `11` | `sanitizer-valgrind` | dismissed; no new source defect | ASan/UBSan, standalone and libFuzzer TSan, suppression-free TSan, and bounded standalone MSan coverage found no current source diagnostic. Valgrind was unavailable and the unbounded MSan suite was stopped at a real resource boundary; see `sanitizer-valgrind.md`. | `c79c8b401b` journal-only close | Draw the next eligible goal |
| 84 | `shuf -i 0-98 -n 1` -> `71` | `deterministic-simulation` (failed `CConnman::Start` lifecycle) | confirmed; fixed | The public start path rejected conflicting outgoing options only after creating network thread handles. The old-source focused regression failed at `!threads_started`; moving the guard before startup made it pass, and full `net_tests` passed 34 cases and 150,899 assertions. See `deterministic-simulation.md`. | `89b320fe37` (`net: reject conflicting start options before threads`) | Initialize cycle 85 goal 63 |
| 85 | `shuf -i 0-98 -n 1` -> `63` | `loupe-pipeline` (`verify-commits` ancestry error) | confirmed; fixed | The round-two scout's missing-object reproducer returned verifier status 0 because Git status 128 was treated as a normal non-ancestor result. Both ancestry checks now fail closed; the standalone regression covers statuses 0, 1, 128 and the real missing-revision path. See `loupe-pipeline.md`. | `468e4ef6fd` (`contrib: fail closed on verify-commits ancestry errors`) | Draw the next eligible goal after the close snapshot |

## Cycle 84 Completion

- Branch: `uber-cycle-84-deterministic-simulation-20260728`.
- Cycle-start HEAD: `c79c8b401b`; pre-existing branch commits `0bfcf35cf6`, `7f08743b4a`, and `55cfa7a448` were preserved before the cycle finding.
- Confirmed finding commit: `89b320fe3709b3d96d91a5a596fae40f3d71e0a5`; author and committer are `Lőrinc <pap.lorinc@gmail.com>`.
- Validation: old-source focused regression exited 201 with the joinable-thread assertion failing; repaired focused regression exited 0; full `net_tests` passed 34/34 cases and 150,899/150,899 assertions; `git diff --check` passed.
- Next draw: exact `shuf -i 0-98 -n 1` -> `63` (`loupe-pipeline`). No relevant process remains running.

## Cycle 72 Completion

- Gate: HEAD `480832aabf6e86a9750dd0a788cccc7f74d0b4ec`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `919 2`.
- Branch: `uber-cycle-72-secp-nonce-session-20260728`.
- Draw: `84` (`secp-nonce-session`).
- Excluded prior cell: cycle 60's `musig_nonce_gen` validation-order defect for null `pubnonce` plus all-zero session randomness.
- Verdict: dismissed for a new source defect. The exact pre/post probe, static contract review, normal and ASan/UBSan-linked probe runs, focused API tests, 4-iteration module matrix, and 16-iteration full libsecp256k1 suite found no stale-output, nonce-reuse, malformed-binding, duplicate-key, infinity, or custom-callback defect. No production source change was justified.
- Evidence: probe `agent-journal/secp_nonce_cycle72_probe.cpp`; raw logs `/data/my_storage/tmp/secp-nonce-cycle72-*.log`; `git diff --check` passed.
- No production, test, fuzz, sanitizer, daemon, or profiling process remains running.

## Cycle 73 Completion

- Gate: HEAD `4935b3908795d5e2196fbf2b91c667790776cffa`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 921`.
- Branch: `uber-cycle-73-raii-resource-leaks-20260728`.
- Draw: `54` (`raai-resource-leaks`).
- Source/test/probe/journal commit: `548d8cc8f8` (`dbwrapper: clean up resources on constructor failure`).
- Verdict: confirmed and fixed. A regular file used as the database parent made `TryCreateDirectories()` throw after `GetOptions()` allocated LevelDB resources. Old-source Clang 19 ASan/LSan reported 4064 bytes in 19 allocations; the fixed probe had no leak report, and removing only the new destructor restored the exact leak.
- Validation: normal `dbwrapper_tests` and the Clang ASan/LSan `dbwrapper_tests` each passed 14 cases and 2475 assertions after the commit. `git diff --check` passed. The broader block/index/flush attempt was blocked by `/` at 100% and terminated; no relevant process remains running.
- Remaining queue: socket and callback cancellation, file/mapping failure paths, database iterator/transaction ownership, and secure allocation cleanup remain distinct unchecked resource cells.

## Cycle 74 Completion

- Gate: HEAD `ddde67072a`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 925`.
- Branch: `uber-cycle-74-build-dead-zones-20260728`.
- Draw: `37` (`build-dead-zones`), with cycle 20's GCC wallet-enabled IPC fuzz cell excluded.
- Verdict: dismissed for a new source defect. Wallet-off/on monolithic Clang builds completed 651/716 steps; wallet-off IPC-on GCC completed 709; wallet-off fuzz-only Clang completed 452; and wallet-off GUI Clang completed 452. The effective target graph matched the option guards, including absent wallet targets, IPC stub/real library selection, fuzz-only target suppression, and Qt wallet exclusion.
- Validation: wallet-off/on focused DB/network/wallet suites passed; IPC tests passed 2 cases and 44 assertions; all four offscreen Qt groups passed 15 cases; fuzz-only `process_messages` and `tx_in` smokes passed with `/data` scratch state; wallet and IPC registration matched feature flags. Release-like fuzz binaries refused execution as documented. The first malformed test invocation used a missing `TMPDIR` under a full root filesystem and was discarded, then corrected.
- Evidence: `/data/my_storage/tmp/build-dead-zones-cycle74/`; `git diff --check` and process cleanup remain required before the state-only close commit.

## Cycle 78 Active State

- Gate: HEAD `b9bb18435221ef31a7e6bf9a9b761c080ea39af7`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 935`.
- Branch: `uber-cycle-78-sanitizer-analysis-matrix-20260728`.
- Draw: initial `2` rejected as the cycle-68 GETBLOCKTXN assertion cell; retry `36` (`sanitizer-analysis-matrix`).
- Excluded prior evidence: cycle 26's TSan unit slices, ASan/UBSan fuzz and unit slices, Clang `scan-build`, and the existing GCC `-fanalyzer` CWE-457 warning cluster.
- Current hypothesis: an omitted diagnostic boundary, direct TokenPipe path, or new source/configuration combination may invalidate the prior cross-tool classification without requiring broad repeated matrix work.
- Required evidence: tool/dependency inventory, exact new diagnostic or coverage path, first-invalid-operation/dataflow proof, independent contract classification, and preserved raw logs before any fix or suppression.

## Cycle 78 Completion

- Gate: HEAD `c28bbc9580717520885a15d1c5764dc3ce11159f`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 937`.
- Branch: `uber-cycle-78-sanitizer-analysis-matrix-20260728`.
- Draw: initial `2` rejected because cycle 68 closed the GETBLOCKTXN assertion cell; retry `36` (`sanitizer-analysis-matrix`) reopened only the queued MSan/instrumented-dependency, Valgrind, direct TokenPipe, new-warning, and new source/configuration cells.
- Finding: `TokenPipeEnd::TokenWrite()` returned `TS_ERR` for POSIX `EPIPE` after its last reader closed, despite `TS_EOS` being the documented end-of-stream status. `init.cpp` ignores `SIGPIPE`, and `SignalInterrupt` propagates the nonzero write result, making the distinction observable. `c28bbc9580` maps EPIPE to TS_EOS and adds a POSIX regression for normal transfer and both EOF directions.
- Verification: the parent implementation failed the new regression at `-1 != -2` with exit 201; the fixed normal focused test passed 6/6 assertions, full `util_tests` passed 79 cases and 3,991 assertions, the focused TSan/UBSan run passed 6/6, and the libc++/MSan direct probe exited 0 without diagnostics. Direct `scan-build-19` and GCC `-fanalyzer` on `tokenpipe.cpp` emitted no report. The full MSan daemon runtime stopped at uninstrumented system libstdc++ static initialization; Valgrind and other queued analyzers were unavailable.
- Evidence: `/data/my_storage/tmp/sanitizer-analysis-matrix-cycle78/`, including `msan-bitcoind-version.log`, the MSan build, direct probe binary, and scan report. `git diff --check` passed, catalog/protocol/TSV hashes matched, and no relevant process remains running.

## Cycle 79 Completion

- Gate: HEAD `42303632902bb1fb2c423b87be1c3bb2ce059bef`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 938`.
- Branch: `uber-cycle-79-fuzz-engine-differential-20260728`.
- Draw: `80` (`fuzz-engine-differential`).
- Excluded prior evidence: cycle 8's libFuzzer/AFL++/Honggfuzz `bech32_roundtrip` comparison, corpus, budget, and metric conclusions. FuzzTest remains unavailable and is not silently substituted.
- Current target: `parse_numbers`, a distinct production parser target with 220 qa-assets inputs totaling 5.2 MiB.
- Verdict: dismissed for a new source defect. libFuzzer completed 6,110 executions with 920 coverage units and 45 new units; AFL++ completed 61,202 executions with 958 edges and 28 new queue items; Honggfuzz completed 5,839 iterations with 305,559 guards and 52 new units. All reported zero crashes/hangs, and the engine-specific counters are not treated as equivalent.
- Verification: ASan replay passed 261 retained libFuzzer files, 249 AFL++ queue files, and 202 Honggfuzz `.cov` files. Cross-engine transfers passed: libFuzzer on AFL++ output (4,068 executions, 21 new units), AFL++ on Honggfuzz output (38 new queue items), and Honggfuzz on AFL++ output at 15 seconds (4,202 iterations, 34 new units). A five-second Honggfuzz transfer stopped during dry-run and is excluded from metrics.
- Tooling limitations: libFuzzer's default 2,048 MiB RSS guard rejected one `-runs=1` corpus-only control at 2,141 MiB; the same corpus passed with an explicit 8,192 MiB guard. Honggfuzz required Clang 19 and explicit build-tree runtime archives because its embedded `libhfcommon` archive was stale. Both are scratch environment results, not repository findings.
- Evidence: `/data/my_storage/tmp/fuzz-engine-differential-cycle79/`, including fixed-budget logs, AFL stats, ASan replays, transfer runs, the initial-corpus manifest, and quarantined stale-build logs. `git diff --check` passed and no relevant process remains running.

## Cycle 80 Active State

- Gate: HEAD `d2379d8892369b0b465ac9b15797309e9ee70f5d`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 940`.
- Branch: `uber-cycle-80-external-vulnerability-variants-20260728`.
- Draw: `33` (`external-vulnerability-variants`).
- Exclusions: closed TokenPipe, compact-block read-failure, Taproot compressed-key lookup, LevelDB ownership, `parse_numbers`, and other prior cells unless new advisory evidence identifies a distinct caller, precondition, trust boundary, or lifecycle path.
- Campaign: retrieve authoritative Bitcoin Core and libsecp256k1 advisory history plus related-project fixes; pin source/version context; map each defect shape into this checkout; independently verify applicability before any patch.
- Required evidence: source URL and identifier, affected versions and fix provenance, current-tree call/dataflow trace, deterministic reproducer or rigorous reachability proof, independent verification, and narrow/broad validation with explicit limitations.

## Cycle 80 Completion

- Gate: start HEAD `d2379d8892369b0b465ac9b15797309e9ee70f5d`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; start divergence `2 940`. The cycle added only its journal and state evidence; known agent-owned artifacts remain untracked and untouched.
- Branch: `uber-cycle-80-external-vulnerability-variants-20260728`.
- Draw: exact `shuf -i 0-98 -n 1` -> `33` (`external-vulnerability-variants`).
- Verdict: dismissed for a new current-tree defect. CVE-2025-46597's width/configuration path is bounded by the 32-bit mempool cap, checked multiplication, saturating dbcache, and block-file size limit; CVE-2025-46598's three validation mitigations and current mempool callers are consistent; CVE-2025-54604/54605's attacker-triggerable info paths use the default file-size limiter; and CVE-2024-52919's AddrMan identifier domain is 64-bit through persistence. The current private-broadcast retry retains its proxy override and rejects direct private-broadcast sockets. Wallet migration and `createfromdump` cleanup remove only created files and empty created directories.
- Validation: cache, maxmempool-overflow, logging, AddrMan, mempool, and private-broadcast unit suites passed. `tool_wallet.py` passed with unnamed malformed-dump cleanup and unnamed restore coverage. `p2p_private_broadcast_retry_v1.py` passed. `wallet_migration.py` was skipped on the wallet-disabled build and on the wallet-enabled build because previous-release binaries were not present; native 32-bit execution was also unavailable. These limitations are explicit and remain eligible follow-up cells.
- No production, test, fuzz, sanitizer, daemon, or profiling process remains running. No production source or test change was justified. The next run must re-check branch/base/HEAD, dirty state, process state, catalog/protocol/TSV hashes, existing journals, history, and review precedent before drawing a distinct eligible goal.

## Cycle 81 Active State

- Gate: HEAD `7918b638fd060edaa6023853038d075e798e6cb2`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 942`.
- Branch: `uber-cycle-81-serialization-untrusted-input-20260728`.
- Draw: exact `shuf -i 0-98 -n 1` -> `6` (`serialization-untrusted-input`).
- Exclusions: cycle 80's advisory variants, cycle 79's `parse_numbers` fuzz-engine comparison, cycle 75's descriptor x-only lookup, cycle 76's compact-block read-failure, cycle 78's TokenPipe status contract, cycle 73's LevelDB ownership, and earlier generic serialization/property cells unless a distinct byte boundary or caller is proven.
- Campaign: inventory parser and serializer surfaces, define valid domains before testing, and independently verify length/count arithmetic, canonical encodings, truncation, output-on-failure, partial mutation, and persisted/network parity.
- Required evidence: exact source/dataflow trace, minimized malformed fixture or rigorous domain proof, independent test/reference/mutation evidence, and narrow then broad validation before any source commit.

## Cycle 77 Initial Active State

- Gate: HEAD `76a22401689d56e337474da9003114b013e6fbd6`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 933`.
- Branch: `uber-cycle-77-local-reasoning-domain-20260728`.
- Draw: `57` (`local-reasoning-domain`).
- Excluded prior cells: cycle 65's AddrMan `GetNetwork()`/`GetNetClass()` and linked-IPv4 relationship, cycle 48's wallet-rescan reservation ordering, and cycle 76's compact-block read-failure publication path.
- Current hypothesis: a helper may combine values from different identity, snapshot, ownership, queue, or lifecycle domains without proving their relationship, causing a reachable omission, miscount, stale publication, or invalid state transition.
- Required evidence: explicit domain/invariant, callers and trust boundary, deterministic valid/invalid relationship pairs, and independent normal/mutation/sanitizer/reference validation before any source change.

## Cycle 77 Completion

- Gate: HEAD `76a22401689d56e337474da9003114b013e6fbd6`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 933`.
- Branch: `uber-cycle-77-local-reasoning-domain-20260728`.
- Draw: `57` (`local-reasoning-domain`).
- Excluded prior cells: cycle 65's AddrMan `GetNetwork()`/`GetNetClass()` and linked-IPv4 relationship, cycle 48's wallet-rescan reservation ordering, and cycle 76's compact-block read-failure publication path.
- Verdict: dismissed for a new source defect. P2P transport/public network identity is intentionally split between transport accounting and public classification; BaseIndex commit ordering is serialized by the validation callback lifecycle and guarded by the flushed tip; transaction-download cleanup preserves peer ownership and orphan/request symmetry; and descriptor x-only/full-key behavior is covered by the cycle-75 fix. No reachable relationship mismatch survived the independent review.
- Validation: corrected isolated `net_tests` passed 32 cases and 132,878 assertions; `txdownload_tests` passed 14 cases and 605 assertions; `descriptor_tests` passed 13 cases and 30,437 assertions; `baseindex_tests`, `blockfilterindex_tests`, and `coinstatsindex_tests` passed 3 cases and 26 assertions. `git diff --check` passed and no relevant process remains running. The first combined invocation was discarded as setup-only: its scratch `TMPDIR` did not exist and duplicate global argument registration caused an abort; each suite was then rerun with a dedicated scratch directory.
- No production source or test change was justified. The next run must re-check the catalog/protocol/TSV hashes, worktree, process state, and remote gate before drawing a distinct goal.

## Cycle 76 Completion

- Gate: HEAD `02e0a92ddc33af203f4204848eb6095312f052af`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 930`.
- Branch: `uber-cycle-76-cross-subsystem-bug-shapes-20260728`.
- Draw: initial `37` rejected as the closed cycle-74 build-dead-zones cell; retry `26` (`cross-subsystem-bug-shapes`).
- Excluded prior cells: cycle 48's wallet-rescan reservation ordering and cycle 62's chainstate candidate-set mutation.
- Current hypothesis: a historical failure-publication or lifecycle fix may have corrected one subsystem while an analogous caller still publishes state before validation, drops a failure return, or lacks symmetric rollback/cleanup.
- Candidate 1 was dismissed: `dumptxoutset` uses a throwing filesystem rename API, and the injected `EIO` became an RPC error with no output artifact.
- Candidate 2 was dismissed: all current `MempoolAcceptResult::Failure` call sites preserve the `TX_RECONSIDERABLE` metadata contract or are explicitly outside it.
- Finding: `PeerManagerImpl::SendMessages` asserted after a compact-block announcement `ReadBlock` failure. Removing the genesis block file reproduced the failure schedule; the new guard disconnects the peer without aborting or publishing a compact block.
- Source/test/journal commit: `ddbb88ed12` (`net: disconnect on compact block announcement read failure`).
- Validation: focused normal regression passed; the deliberate old-source mutation aborted with status 134 and the expected assertion; full normal `net_tests` passed 32 cases; the Clang 19 TSan build and focused/full TSan `net_tests` passed with no diagnostics.
- No production, test, fuzz, sanitizer, daemon, or profiling process remains running.

## Cycle 75 Completion

- Gate: HEAD `df9c8d76c7`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 927`.
- Branch: `uber-cycle-75-public-object-validation-20260728`.
- Draw: `15` (`public-object-validation`).
- Excluded prior cells: cycle 27's `InferPubkey()` full-key P2PK raw fallback and cycle 51's `InferXOnlyPubkey()` taproot leaf raw fallback, plus their regressions and source commits.
- Finding: full compressed public keys accepted in P2TR retained their full-key spelling but used parity-specific private-key lookup, while the generated Taproot script consumed only the x-only point. An even provider key and odd descriptor spelling reproduced failed private-key discovery and private-string conversion before the fix.
- Commit: `d02e22867b` separates serialization spelling from lookup mode, sets x-only lookup for full compressed P2TR keys, and preserves the mode through `Clone()`.
- Validation: normal focused/full descriptor and Miniscript suites passed; the 219/219-step Clang 19 TSan build and combined 16-case TSan suite passed; the 173/173-step Clang 19 ASan/UBSan fuzz build passed; descriptor and Miniscript corpus smokes completed without sanitizer diagnostics; the old-source mutation exited 201 with four failed assertions.
- Limitations: no additional wrapper/FFI defect was confirmed. MuSig aggregate providers and external language bindings remain separate cells without a failing reproducer. The fuzz run was a deterministic smoke, not exhaustive coverage.

## Cycle 71 Completion

- Gate: HEAD `9eb6b4ad1b66f030f0019b759d0c8017993cf63b`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 917`.
- Branch: `uber-cycle-71-statistical-timing-20260728`.
- Draw: `53` (`statistical-timing`).
- Excluded prior cells: cycles 1 and 5 basic Clang timing screens, cycle 44 ECDH/Schnorr/MuSig caller and MuSig ctime boundary, and cycle 46 GCC/backend/optimization timing matrix.
- Verdict: no new source defect. ElligatorSwift XDH uses the constant-time secret multiplication path; Silent Payments shared-secret handling passed ctime, while the scan's public-output arithmetic is explicitly declassified. Encode/decode remain excluded because the API documents them as variable-time.
- Evidence: `/data/my_storage/tmp/statistical-timing-cycle71/auto.log`, `off.log`, `msan-ctime.log`, `msan-ctime-mutated.log`, and `msan-ctime-restored.log`; probe source `agent-journal/statistical_timing_cycle71_probe.cpp`.
- No production or test process remains running. Temporary source mutation was restored before closing the cycle.

## Cycle 70 Completion

- Gate: HEAD `3fac7b6827c9a45d91d299ad717276b5539c0572`; `origin/master` `7dea464d6b51a69bd99a0451be8aaf3a26313eb6`; merge-base `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`; divergence `2 914`.
- Branch: `uber-cycle-70-bindings-ffi-parity-20260728`.
- Draw: `94` (`bindings-ffi-parity`).
- Source/test/journal commit: `0ab4d16796` (`kernel: bridge spent output handles explicitly`).
- Verification: isolated Clang 19 RelWithDebInfo kernel build; focused precomputed-data run passed with seed 7042; full 19-case `test_kernel` run passed with seed 7043; old-cast plus temporary layout mutation caused `btck_script_verify_tests` to exit 139 with seed 7044; restored fixed code passed the affected suite with seed 7045.
- No production or test process remains running. The temporary layout mutation and all other negative-control edits were restored.

## Eligibility

- Pending goals: `0..98`, subject to the catalog validation and current risk map.
- Active goals this cycle: `external-vulnerability-variants` cycle 80 only. Goal 7's response-amplification cells remain inconclusive rather than exhausted.
- Reopened goals: `external-vulnerability-variants` cycle 80; `sanitizer-analysis-matrix` cycle 78 is closed after the distinct TokenPipe finding; `statistical-timing` (cycle 5) remains reopened, with its GCC compiler/backend cell closed while database semantics remains open for distinct batch/recovery/sync/comparator cells.
- Exhausted goals: none recorded yet.

## Handoff

Cycle 79 is complete. The exact draw was `shuf -i 0-98 -n 1` -> `80`, `fuzz-engine-differential`. Cycle 8's `bech32_roundtrip` comparison was excluded; current-head builds used the distinct `parse_numbers` corpus. Native metric differences were classified as engine-specific, all retained inputs passed ASan replay, and no source change was justified. The next run must re-check the gate and draw a distinct eligible goal from the full catalog.

Cycle 80 is complete. The exact draw was `shuf -i 0-98 -n 1` -> `33`, `external-vulnerability-variants`, after the cycle-79 gate. A1-A4 were dismissed for new variants; A6 and A7 were confirmed as already fixed upstream with current regression evidence. Cycle 81 is active on goal 6, `serialization-untrusted-input`; its next handoff must preserve the byte-domain ledger, malformed fixtures, exact parser traces, independent oracle/mutation evidence, and explicit limitations without treating existing tests as proof of unexamined boundaries.

Cycle 78 is complete. The initial draw was `2`, rejected because cycle 68 closed the GETBLOCKTXN assertion cell; the retry draw was `36`, `sanitizer-analysis-matrix`. The distinct TokenPipe EPIPE contract defect is fixed in `c28bbc9580`; the parent-control failure, normal/TSan/MSan/static validation, and MSan system-library limitation are recorded in `sanitizer-analysis-matrix.md`. The next run must re-check the gate and draw a distinct goal from the full catalog.

Cycle 77 is complete. The exact draw was `shuf -i 0-98 -n 1` -> `57`, `local-reasoning-domain`, after the cycle-76 gate. It excluded cycle 65's AddrMan classifier and linked-IPv4 relationship cells, plus cycles 48 and 76's closed findings. Four relationship/state-domain cells were independently reviewed and dismissed; the corrected focused suites and evidence are recorded in `local-reasoning-domain.md`. The next run must re-check the gate and draw a distinct goal from the full catalog.

Cycle 76 is complete. The initial `shuf -i 0-98 -n 1` draw was `37` and was rejected because cycle 74 had just closed that build-dead-zones cell; the retry draw was `26`, `cross-subsystem-bug-shapes`. It excluded cycles 48 and 62's wallet-rescan and chainstate candidate-set cells. The compact-block announcement read-failure assertion was confirmed and fixed in `ddbb88ed12`; candidates involving `dumptxoutset` rename and package fee metadata were dismissed. The focused regression, old-source mutation, full normal `net_tests`, and full TSan `net_tests` evidence are recorded in `cross-subsystem-bug-shapes.md`. The next run must re-check the gate and draw a distinct goal.

Cycle 75 is complete. It selected goal 15, `public-object-validation`, by `shuf -i 0-98 -n 1` -> `15` after the cycle-74 gate. The confirmed Taproot compressed-key lookup mismatch is fixed in `d02e22867b`; the exact candidate ledger, mutation, corpus, validation, and limitations are recorded in `public-object-validation.md`. The next run must re-check the gate and draw a distinct goal from the full catalog.

Cycle 74 is complete. It selected goal 37, `build-dead-zones`, by `shuf -i 0-98 -n 1` -> `37` after the cycle-73 gate. Cycle 20's GCC wallet-enabled IPC fuzz cell was excluded; the distinct wallet-off, IPC-off/monolithic, IPC-on wallet-off, fuzz-only, and GUI wallet-off matrix found no source defect. The exact configuration ledger, runtime evidence, and limitations are recorded in `build-dead-zones.md`. The next run must re-check the gate and draw a distinct goal from the full catalog.

Cycle 73 is complete. It selected goal 54, `raai-resource-leaks`, by `shuf -i 0-98 -n 1` -> `54` after the cycle-72 gate. The confirmed `CDBWrapper` constructor-failure leak is fixed in `548d8cc8f8`; focused normal and ASan/LSan suites passed, while the broader dependent suite was blocked by the full root filesystem. The exact resource ledger and limitations are recorded in `raai-resource-leaks.md`. The next run must re-check the gate and draw a distinct goal from the full catalog.

Cycle 72 is complete. It selected goal 84, `secp-nonce-session`, by `shuf -i 0-98 -n 1` -> `84` after the cycle-71 gate. The distinct scope was MuSig/Schnorr failure, retry, nonce-consumption, malformed-binding, duplicate-key, infinity, and callback state. The probe and all relevant normal/sanitized test controls passed; no source defect was confirmed. The exact evidence and limitations are recorded in `secp-nonce-session.md`. The next run must re-check the gate and draw a distinct goal from the full catalog.

Cycle 71 is complete. It selected goal 53, `statistical-timing`, by `shuf -i 0-98 -n 1` -> `53` after the cycle-70 gate. The distinct scope was ElligatorSwift XDH and Silent Payments sender/recipient secret callers, with variable-time public-output operations excluded from the finding ledger. The exact experiment, ctime/dataflow review, raw traces, and verdict are recorded in `statistical-timing.md`.

Cycle 70 is complete. It selected goal 94, `bindings-ffi-parity`, by `shuf -i 0-98 -n 1` -> `94` after the cycle-69 gate. The distinct scope was the C/C++ `libbitcoinkernel` wrapper and its ownership, width, view, callback, and opaque-handle contracts. The confirmed defect passed `TransactionOutput` wrapper objects where the C API requires an array of opaque pointers; the explicit bridge is in `0ab4d16796`. The focused and full fixed suites passed, and the temporary layout mutation killed the old cast. The exact candidate ledger and limitations are in `bindings-ffi-parity.md`. The next run must re-check the gate and draw cycle 71 from the full catalog.

Cycle 50 selected `exhaustive-algebraic` with `shuf -i 0-98 -n 1` -> `18`. The distinct cell defined `GCSFilter::MatchAny(Q) == OR Match(q)` over all 16 filter subsets and 16 query subsets of four one-byte elements, with checked encoded reconstruction as a second path. The disposable matrix passed 1,555 assertions; a temporary `return false` mutation failed at the singleton check with exit 201 and 606 failed assertions. After restoring source and removing the disposable test, the production block-filter suite passed 8 cases and 499 assertions. No source defect was justified. The exact evidence and limits are in `exhaustive-algebraic.md`; no process remains running. The next run must re-check the gate and draw another distinct goal.

Cycle 51 selected `public-object-validation` with `shuf -i 0-98 -n 1` -> `15`. The prior cycle-27 P2PK inference issue was excluded. A tracked taproot tree containing a leaf `<32 zero bytes> OP_CHECKSIG` reproduced an unparseable inferred `tr(...,pk(0000...))` descriptor and, after a guard-only trial, a null-provider segfault. The final two-part fix validates x-only keys and skips the direct P2TR provider branch when invalid, yielding a parseable `rawtr(...)` descriptor. The pre-fix and final mutation tests exited 201; the fixed focused regression passed 4/4 assertions. `descriptor_tests` passed 12 cases/30,419 assertions, adjacent script/miniscript tests passed 1/1 and 3/10,692, and rebuilt Clang 19 ASan/UBSan `FUZZ=script` completed 2,537 corpus units with no diagnostic. Commit `a81f58f3bf` contains source, test, and per-goal journal. No process remains running. The next run must re-check the gate and draw another distinct goal.

Cycle 49 selected `reviewer-preference-mining` with `shuf -i 0-98 -n 1` -> `60`. The previous cycle-25 sample was excluded. Current review evidence from wallet error typing, `bitcoin-util getchainparams`, CI path realism, pruning help, and Guix scope wording supported the fingerprint `actionable-interface-minimal-schema-boundary-realism`. A fresh CMake configure and 132-action `bitcoin-util` build succeeded in `/data/my_storage/tmp/cycle49-build-space_ ₿🧪_`; mainnet/regtest fixtures matched byte-for-byte and the extra-argument case returned status 1 with the expected message. `makensis` was unavailable, so the Windows deploy target was not run. No source change was justified; `reviewer-preference-mining.md` contains the public review URLs, exact source tips, held-out checks, limitations, and next queue. No process remains running. The next run must re-check the gate and draw another distinct goal.

Cycle 48 selected `cross-subsystem-bug-shapes` with `shuf -i 0-98 -n 1` -> `26` after the gate recorded the corrected actual-tab TSV hash `babfb36e1a64d8b4ad310459306fa2dfdb240d644d731e2b795177f93a68f1cb`. The historical `importdescriptors` reservation race was traced to the analogous `rescanblockchain` ordering: a contender could block in `BlockUntilSyncedToCurrentChain()` on `cs_wallet`, then reserve after the first scan released its flag. A disposable forced schedule failed the old source with two successful concurrent rescans and passed after moving `WalletRescanReserver::reserve()` before the wait. The durable 1,000-block ten-pair `wallet_rescan.py` test, existing `wallet_importdescriptors.py` suite, rebuilt wallet daemon, redirected `wallet_tests` suite (15 cases), Python syntax, and diff checks passed. Commit `9911bb75a8520990a6e9ca6405269dd634a7a50c` and `cross-subsystem-bug-shapes.md` are complete. The unredirected unit attempt exited 201 only because `/` was 100% full; `TMPDIR=/data/my_storage/tmp/cycle48-wallet-unit-tmp` passed. No source, test, daemon, fuzz, sanitizer, or profiling process remains running. The next run must re-check the gate and draw another distinct goal.

Cycle 47 selected `float-sanitizer-fuzz-exclusions`. The active SHA256 SSE4 `no_sanitize("address")` guard was challenged independently: removing it failed current Clang 19 ASan compilation in inline assembly and a current-source GCC 12.2 ASan probe crashed in `SHA256AutoDetect()`'s SSE4 self-test; guarded current-source probes passed. Exceptional-value serialization, locale/money formatting, and fee-estimator unit tests passed, and the ASan/UBSan `float` target completed 100,000 deterministic runs without a diagnostic. A temporary wrong NaN assertion was killed immediately by the same fuzz target and restored. The full GCC dependency rebuild was interrupted at 21% due to resource cost, but the standalone current-source GCC object/probe supplied the relevant evidence. No production or test source change was justified. See `float-sanitizer-fuzz-exclusions.md`; draw the next goal after rechecking the gate.

Cycle 46 selected the statistical-timing GCC compiler/backend and optimization cell. GCC 12.2 `-O2` and Debug-configured `-O3` libraries with x86_64 assembly `AUTO` and `OFF` ran the preserved ECDH/Schnorr/MuSig probe at 4,000 samples per class, 16 rounds, three repetitions, and two deterministic key classes. Across the eight build/class logs, the largest absolute Welch t-statistic was `1.267`; directions changed across repetitions and variants, medians/95th percentiles tracked, and sinks matched. GCC AUTO/OFF VERIFY and noverify tests passed with seed `0123456789abcdef`; no source change was justified. Valgrind, dudect, GCC 13+, LTO, and non-x86 execution remain unavailable. The selected journal now records exact build commands, raw logs, assembly review, validation, limitations, and next queue. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, and review precedent before drawing again.

Cycle 45 selected the database-semantics-differential iterator-status cell. The production `CDBIterator::Valid()` wrapper now routes a false iterator status through `HandleError`, preserving LevelDB read failures instead of reporting normal exhaustion. The deterministic `IteratorReadErrorEnv` regression writes and compacts a real entry, injects a read error, and expects `dbwrapper_error`; the old implementation failed that test, the fix passed it, and a mutation restoring the old method failed it again. `dbwrapper_tests` (12 cases), dependent chainstate/index suites, the rebuilt unit target, and `git diff --check` passed. Source/test commit `2340c73ef0` and selected journal `database-semantics-differential.md` are complete. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, and review precedent before drawing again.

Cycle 32 used `/data/my_storage/tmp/current-pr-leftovers-dbcrash-cycle32.pMxtsM/`, `/data/my_storage/tmp/current-pr-leftovers-dbbatch-negative-cycle32.R1LzXL/`, `/data/my_storage/tmp/current-pr-leftovers-dbbatch-positive-cycle32.UhTPqC/`, and `/data/my_storage/tmp/current-pr-leftovers-feature-dbcrash-cycle32/`. The first selector draw was `48` (`property-oracle-expansion`), excluded because its cycle-28 property cell was already closed; rerunning `shuf -i 0-98 -n 1` drew `3` (`current-pr-leftovers`). Before `ddbbb0aadb`, a scratch daemon with `-dbcrashratio=-1 -dbbatchsize=1` mined one block, then restarted with `-debug=coindb`; replay reached `[coindb] Writing partial batch of 0.00 MiB` and exited status 134 at `random.h:257` with `Assertion `range > 0' failed`. After the first fix, the same command returned status 1 with `Error: -dbcrashratio must be non-negative (got -1)`, and `2147483648` returned `-dbcrashratio is too large`. Before `5754033019`, identical one-block controls with `-dbcrashratio=0` showed `-dbbatchsize=-1` logging only a final batch while `-dbbatchsize=1` logged a partial then final batch, proving signed-to-unsigned narrowing changed database write behavior. After the second fix, `-dbbatchsize=-1` returned status 1 with `Error: -dbbatchsize must be non-negative (got -1 bytes)`. `cmake --build build_unit_clang19 --target test_bitcoin -j2` and `cmake --build build_func_clang19 --target bitcoind -j2` passed. `validation_chainstatemanager_tests/chainstatemanager_args` passed 51 assertions; full `validation_chainstatemanager_tests` passed 22 cases/2,079 assertions; combined chainstate/flush/mempool passed 49 cases/60,503 assertions. The attempted `feature_dbcrash.py` run failed before database writes with `StopIteration` in `MiniWallet.get_utxo()` while preparing its first transfer; the test framework cleaned all four nodes and the artifact is retained for handoff. `git diff --check` passed and no source, test, daemon, build, or fuzz process remains. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, and review precedent before drawing again.

Cycle 33 used no new scratch data directory. The exact selector was `shuf -i 0-98 -n 1`, which drew `62` (`rejected-finding-resurrection`). The reopened finding was checked against current callers and the 2025 recovery-removal history: `kernel::MakeBlockInfo()` derives `prev_hash` from the block-index predecessor, `BaseIndex::Sync()` rewinds before append, and `BaseIndex::BlockConnected()` rejects non-ancestor notifications or rewinds to the predecessor. `BaseIndex::ProcessBlock()` is the only production `CustomAppend()` dispatch and converts false to `AbortNode()` plus immediate return. The mismatch return precedes height-index writes, `m_current_block_hash` updates, and best-block publication; `LookUpStats()` reads persisted DB entries, and shutdown unregisters/destroys indexes after queued callbacks drain. `build_unit_clang19/bin/test_bitcoin --run_test=coinstatsindex_tests,baseindex_tests --catch_system_error=no --log_level=test_suite` passed 3 cases; the selected index lifecycle command passed its 1 enabled case with no errors. No source, test, daemon, build, fuzz, sanitizer, or profiling process remains running. No source change was justified. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, and review precedent before drawing again.

Cycle 34 used `/data/my_storage/tmp/fuzz-introspector-cycle34-empty` for bounded libFuzzer smoke inputs and the existing `/data/my_storage/tmp/qa-assets/fuzz_corpora` snapshot. The exact selector was `shuf -i 0-98 -n 1`, which drew `50` (`fuzz-introspector`). `process_messages` had zero corpus files; the default runner path selected `-runs=1` and reached `cov 11470` after two executions, while `--empty_min_time=2` selected `-max_total_time=2`, completed 161 executions, and reached `cov 11482`. `test_runner.py` has supported this mode since commit `0000f55293`, and CI already passes `--empty_min_time=60`; `CoverageFuzz.cmake` did not. Static inspection ranked `process_messages_fuzz_target` at `0x69b6` bytes and traced its empty-input early exit before the 30-message loop. No Fuzz Introspector report or installation was available, so manual object/call-chain/counter evidence was used and that limitation was recorded. `cmake -S . -B build_fuzz_libfuzzer_clang19 -G Ninja`, `cmake --build build_fuzz_libfuzzer_clang19 --target fuzz -j2`, `python3 -m py_compile test/fuzz/test_runner.py`, `git diff --check`, and the targeted runner controls passed. Commit `7d1a7bdda6` adds the configurable default-60-second option and the selected journal. No source, test, fuzz, sanitizer, daemon, or profiling process remains running. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, and review precedent before drawing again.

Cycle 31 used history-only evidence and the existing `build_unit_clang19` tree. The exact selector was `shuf -i 0-98 -n 1`, which drew `90` (`historical-knowledge-recipes`). After excluding the earlier PSBT and relay-accounting recipes, the seed was PR `#35295`, merge `c0e91efdb31fa930593f61cc87464e94c9f1ac72`, whose ten-commit sequence covered `-prevoutfetchthreads` registration, typed parsing, zero/negative/max behavior, runtime worker start/ready/stop/reset/flush, unit and fuzz coverage, and release documentation. The focused command `build_unit_clang19/bin/test_bitcoin --run_test=validation_chainstatemanager_tests --report_level=short` passed 22 cases and 2068 assertions. The standalone `c7af7477b106e321d161f97c4893373426fc152b` `-inputfetchthreads` option commit was retained as a negative control because its four-file diff has no runtime consumer or tests; local follow-up `3873d90f06df3cbec47277b5c48778bdbcd03219` demonstrates separate worker-boundary coverage. No production source, test, daemon, build, or fuzz process remains running; no source change was justified. The recipe is indexed as `configurable-parallel-feature-lifecycle`. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, and review precedent before drawing again.

Cycle 30 used `/data/my_storage/tmp/current-pr-leftovers-prune-before-cycle30.ZWCHOn/`, `/data/my_storage/tmp/current-pr-leftovers-prune-after-cycle30.B8LZk1/`, `/data/my_storage/tmp/current-pr-leftovers-prune-valid-cycle30.f9SZEk/`, `/data/my_storage/tmp/current-pr-leftovers-prune-valid-after-cycle30.Ps6Bsd/`, and `/data/my_storage/tmp/current-pr-leftovers-prune-boundary-cycle30.AkQSL4/`. The exact selector was `shuf -i 0-98 -n 1`, which drew `3` (`current-pr-leftovers`). Before `5b2e4f5a63`, `-prune=17592186044416` reached `Done loading` and logged `Setting NODE_NETWORK in non-prune mode`; after the fix it exited status 1 with `-prune is too large (got 17592186044416 MiB)`. `-prune=550` reached `Done loading` and logged the 550 MiB target both before and after; `2^44-1` MiB also reached `Done loading` and logged the exact target. The focused prune test passed 1 assertion, the full node-init suite passed 3 cases/5 assertions, blockmanager passed 11/117, validation-flush passed 3/58,001, and mempool passed 24/423. `test_bitcoin` and `bitcoind` rebuilt successfully; `git diff --check` passed; no source, test, daemon, or build process remains running. The lower-priority debug-only `-dbbatchsize`/`-dbcrashratio` questions remain queued, not confirmed. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, and review precedent before drawing again.

Cycle 28 used `/data/my_storage/tmp/property-oracle-expansion-cycle28/` for dedicated empty fuzz corpora and raw smoke outputs. The exact selector was `shuf -i 0-98 -n 1`, which drew `48` (`property-oracle-expansion`). The audit found existing independent contracts for transaction serialization and hashes, block/header bytes, partial Merkle trees, script sigop accounting, and block filters. A temporary probe of default empty transaction serialization disproved the expected witness parse failure, but the object is outside the accepted transaction domain and `CheckTransaction` rejects it as `bad-txns-vin-empty`; the probe was removed and no source/test change was justified. Normal script/sigop, block-filter, and transaction suites passed 29, 8, and 18 cases. Clang ASan fuzz smokes completed 1000 `script_ops`, 323 `blockfilter`, and 300 `block_header` runs with no diagnostics. No source, test, fuzz, sanitizer, daemon, or profiling process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 29 used `/data/my_storage/tmp/integer-overflow-maxsigcache-before-cycle29b/`, `/data/my_storage/tmp/integer-overflow-maxsigcache-after-cycle29/`, `/data/my_storage/tmp/integer-overflow-maxsigcache-valid-cycle29/`, `/data/my_storage/tmp/integer-overflow-limitclustersize-before-cycle29/`, `/data/my_storage/tmp/integer-overflow-limitclustersize-after-cycle29/`, and `/data/my_storage/tmp/integer-overflow-limitclustersize-downstream-cycle29b/` for isolated regtest daemon probes. The exact selector was `shuf -i 0-98 -n 1`, which drew `52` (`integer-overflow`) again; cycle 10's P2P buffer cell was excluded and two distinct unchecked cells were selected. Before the first fix, `-maxsigcachesize=17592186044416` logged both caches as `0 MiB` and stored two elements; after `f91039eb5c`, the same command returned status 1 with an explicit size error, while `33 MiB` reached `Done loading`. Before the second fix, `-limitclustersize=9223372036854776` and the downstream boundary `230584300921370` each terminated with status 132/SIGILL; after `93fef76238`, both returned status 1 with explicit errors. The focused chainstate argument case passed 40 assertions, the focused mempool argument case passed 5 assertions, the complete mempool suite passed 24 cases and 423 assertions, and the initialization suite passed 2 cases and 4 assertions. No source, test, daemon, or build process remains running. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, review precedent, and this ledger before drawing again.

Cycle 27 used `/data/my_storage/tmp/public-object-validation-cycle27/` for a fixed malformed public-object corpus and `/data/my_storage/tmp/sanitizer-analysis-matrix-cycle26/asan-unit-clang/` for the rebuilt Clang sanitizer test target. The exact selector was `shuf -i 0-98 -n 1`, which drew `15` (`public-object-validation`). The old inference path produced `pk(020000...0000)#rxgq8p4q` for an off-curve P2PK key, and `Parse()` rejected it with `pk(): Pubkey '020000...0000' is invalid`. Commit `c125061aa4` added the full curve-validity check and the regression now requires a parseable `raw(...)` fallback. Normal and sanitizer descriptor suites each passed 11 cases and 30,415 assertions; the focused regression passed 4 assertions in both configurations. Clang and GCC parser fuzz slices passed their fixed inputs before the source edit; no sanitizer diagnostic appeared after the edit. No source, test, fuzz, sanitizer, daemon, or profiling process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 26 used `/data/my_storage/tmp/sanitizer-analysis-matrix-cycle26/`. Clang and GCC TSan unit slices passed 63/61 cases and 173,304/174,265 assertions; a fresh Clang ASan/UBSan unit build passed 180 focused cases and 390,283 assertions; and matched Clang/GCC ASan/UBSan fuzz inputs passed under their respective driver interfaces. Clang `scan-build` analyzed the core crypto/util targets with zero plist reports. GCC `-fanalyzer` completed the same targets but emitted 29 CWE-457 warnings; all were cross-tool/runtime-classified as analyzer artifacts or bounded contract questions, with no source change justified. The initial Clang IPC configure was rejected by the explicit Cap'n Proto 0.9.2/Clang 19.1.7 compatibility check, so the static and ASan unit builds used IPC off; MSan, Valgrind, clang-tidy, cppcheck, Semgrep, and CodeQL were unavailable. Raw logs and build trees remain under `/data/my_storage/tmp/sanitizer-analysis-matrix-cycle26/`; no process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 25 used local history plus unauthenticated public GitHub API review endpoints for PRs `#35692`, `#35320`, `#35639`, `#35090`, `#35681`, `#35215`, `#35616`, and `#35767`. The exact API method and source tips are recorded in `reviewer-preference-mining.md`; the extracted recipe was checked against held-out commits `e9ed898a`, `6ee05c4b`, and `6aa5d8d`, with `12269112` retained as a same-feature fixture control. Remote review visibility is incomplete and acceptance alone was not treated as a universal preference. No production source changed, no tests were needed for this history-only cycle, and no process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 16 left no fuzz, sanitizer, daemon, profiling, or test process running. Raw timing artifacts remain under `/data/my_storage/tmp/statistical-timing/` and `/data/my_storage/tmp/statistical-timing-cycle5/`; backend differential artifacts remain under `/data/my_storage/tmp/backend-differential/`; the focused PSBT log is under `/data/my_storage/tmp/rpc-psbt-recipe-wallet.log`; fuzz-engine artifacts and logs remain under `/data/my_storage/tmp/fuzz-engine-differential/`; deterministic-artifact outputs remain under `/data/my_storage/tmp/deterministic-artifacts/`; integer-overflow build and startup logs remain under `/data/my_storage/tmp/integer-overflow-*/`; cycle 11 release-differential builds, raw RPC responses, and daemon logs remain under `/data/my_storage/tmp/release-version-differential/`; cycle 12 invariant-differential builds, fixtures, and scratch daemon artifacts remain under `/data/my_storage/tmp/invariant-differential-cycle12/`; cycle 13 fuzz builds and scratch corpora remain under `/data/my_storage/tmp/stateful-contract-fuzzer-cycle13/`, with project fuzz binaries under `build_fuzz_libfuzzer_clang19/` and `build_fuzz_asan_clang19/`; cycle 14 optimized daemon, deterministic chain, profile copies, crash-recovery copies, and raw counters remain under `/data/my_storage/tmp/rebuild-recovery-profile-cycle14/`; cycle 15 rebuilt fuzz binary, four eight-input smoke corpora, and logs remain under `/data/my_storage/tmp/bitcoin-p2p-accounting-cycle15/`. Cycle 16 used the existing unit and TSan build trees; the full unit run ended with the residual failures recorded in `locking-threading.md`. Cycle 17's detached benchmark worktree, build, raw testdatadirs, and perf runs remain under `/data/my_storage/tmp/disk-io-amplification-cycle17/`. Cycle 18 used `build_unit_wallet_clang19`; the wallet unit target and selected scriptpubkeyman suite passed, and the controlled pre-fix test failure is recorded in `error-path-state.md`. Cycle 19 used history-only evidence; the relay-budget and held-out private-broadcast commits include their exact clean-master controls and mutation proofs in `historical-knowledge-recipes.md`. Cycle 20 used `/data/my_storage/tmp/build-dead-zones-cycle20/`; the GCC IPC/wallet fuzz target built all 510 steps and the post-fix empty/non-empty wallet parser and IPC smokes passed. The Clang 19 attempt stopped at the repository's explicit Cap'n Proto 0.9.2 compatibility check. Cycle 21 used `/data/my_storage/tmp/p2p-sendheaders-cycle21-before2/` for the failing-before assertion and `/data/my_storage/tmp/p2p-sendheaders-cycle21-after3/` for the passing full functional run; `build_func_clang19` rebuilt `bitcoind`, `build_unit_clang19` rebuilt `test_bitcoin`, and `net_tests` completed all 31 cases with no errors. Cycle 22 used `/data/my_storage/tmp/cycle22-fuzz-work/` and `/data/my_storage/tmp/cycle22-fuzz-asan/` for isolated 128-seed normal and ASan/UBSan replays; both completed 129 runs with exit code 0. The earlier shared-checkout parallel replay was discarded as non-authoritative after log collisions and interruption. No source process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.
Cycle 23 used the existing `build_unit_clang19` tree; rebuilding `test_bitcoin` compiled both changed index translation units. The focused `txindex_tests` plus `txospenderindex_tests` run passed all 6 cases before and after, and the combined `transaction_tests,txindex_tests,txospenderindex_tests` run passed all 24 after the fix. No source process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.
Cycle 24 used `/data/my_storage/tmp/secp-field-matrix-int128/` and `/data/my_storage/tmp/secp-field-matrix-int64/`, both with assembly disabled and explicit int128/int64 overrides. Their builds and regular/no-VERIFY/exhaustive binaries passed. The scratch probe source and outputs remain under `/data/my_storage/tmp/`; the corrected runs exited 0, emitted 754 records each, and compared with identical SHA256 `315b522a08b0f7c0ab9fbf21a02c8244143c50d8ee911645f54c8e888db92828`. The first probe failures were discarded as harness violations: restricted field-multiply aliasing and lazy reads of uninitialized pairwise entries. No production source changed and no test, fuzz, sanitizer, daemon, or profiling process remains running. The next run must re-check the worktree, branch, base/HEAD, remotes, dirty state, running jobs, this ledger, the catalog, existing findings, journals, relevant history, issues, pull requests, and review precedent before drawing. Record the exact selector command, random draw, selected slug, and cycle evidence here and in the selected per-goal journal.

Cycle 38 used `/data/my_storage/tmp/option-api-lifecycle-cycle38-before-src/` and `/data/my_storage/tmp/option-api-lifecycle-cycle38-before-build/` for the unpatched-parent control, plus separate scratch directories for serial v1/v2 RPC tests and the outbound relay-rate control. The first selector draw was `48` (`property-oracle-expansion`) and was excluded because cycle 28 closed that property cell; rerunning `shuf -i 0-98 -n 1` drew `43` (`option-api-lifecycle`). Before the source fix, the corrected `rpc_net.py` run against parent `ebe09a67153fe09ba67b03b0cc49f01fa84e2381` reached `getnetworkinfo` and exited 1 because its help lacked the peer-wide description; the old inbound-only wording remained. After changing the startup and RPC descriptions, `build_func_clang19` rebuilt `bitcoind`, serial v1 and v2 `rpc_net.py` runs exited 0, and `p2p_tx_relay_rate_limit_outbound.py` passed. Earlier parallel transport and assertion-typo runs were discarded. `python3 -m py_compile test/functional/rpc_net.py`, `git diff --check`, the standalone startup/RPC probes, and process cleanup passed. No runtime behavior changed. The next run must re-check branch/base/HEAD, dirty state, processes, catalog hashes, existing journals, history, and review precedent before drawing again; exclude this fixed `-txsendrate` metadata cell and choose another distinct option/API lifecycle surface.
