# Campaign #17 — build-matrix-modules

Base: audit/resurrection @ 78f3413021 (rotation ledger commit for #29).
Branch: audit/build-matrix. Start state: clean (untracked scratch only).
Constraint honored: disk 99% full -> cycle 1 is a text/inventory audit; no
new build directories created.

## Cycle 1 (2026-07-27): option/CI inventory + manifest drift — all clean

### Check 1: declared option surface vs CI coverage
27 options in CMakeLists.txt:103-138 (+ subtree options in crc32c/leveldb/
minisketch/secp256k1). Every ci/test/00_setup_env*.sh uses
`--preset=dev-mode`, which enables ALL features (CMakePresets.json:63-88:
WITH_ZMQ, WITH_QRENCODE, WITH_USDT, BUILD_UTIL_CHAINSTATE,
BUILD_WALLET_TOOL, ENABLE_IPC, ENABLE_EXTERNAL_SIGNER, BUILD_KERNEL_LIB,
BUILD_SHARED_LIBS, ...). Per-job deltas cover the off-combinations:
nowallet (00_setup_env_native_nowallet.sh), no_ipc (i686_no_ipc),
ENABLE_IPC=OFF+Debug, sanitizers (asan/tsan/msan/ubsan/valgrind),
fuzz+msan/valgrind, LTO (chimera), cross (win/mac/freebsd/netbsd/openbsd/
riscv/s390x/arm), REDUCE_EXPORTS=ON.
Raw greps suggesting "WITH_ZMQ: 0 CI scripts" etc. are preset artifacts —
the flags are enabled through dev-mode, not per-script.
Sole uncovered option: WITH_EXTERNAL_LIBMULTIPROCESS (default OFF
everywhere) — explicitly documented "not normally recommended" dev-only
flag. Accepted exception, not a gap.
VERDICT: CI coverage of the option surface is comprehensive. DISMISSED.

### Check 2: source-manifest drift (present-but-unlisted)
All src/**.{c,cpp} (excl. leveldb/crc32c/minisketch/secp256k1/univalue/
capnp subtrees) vs every CMakeLists.txt: 2 apparent orphans, both
intentional:
- src/crypto/ctaes/ctaes.c — unity-included: aes.cpp:13
  `#include <crypto/ctaes/ctaes.c>`.
- src/qt/bitcoinstrings.cpp — translation-pipeline input, not a compiled
  source: consumed by share/qt/translate.cmake via the `translate` custom
  target (src/qt/CMakeLists.txt:302-312); regenerated by string-freeze
  commits (ef987683dc).
VERDICT: no drift. DISMISSED.

### Check 3: test/bench/fuzz registration drift
- unit tests: 0 src/test/*.cpp missing from src/test/CMakeLists.txt
- bench: 0 src/bench/*.cpp missing from src/bench/CMakeLists.txt
- wallet tests: src/wallet/test/util.cpp flagged but compiled via
  src/test/util/CMakeLists.txt:20 (ENABLE_WALLET-conditional test-util lib)
- qt tests: 0 missing
- fuzz: 139/139 flagged by wrong-file comparison (list lives in
  src/test/fuzz/CMakeLists.txt); recheck -> only harness files fuzz.cpp/
  util.cpp, compiled into the test_fuzz lib
  (src/test/fuzz/util/CMakeLists.txt:5). 0 omitted targets.
VERDICT: no omitted tests. DISMISSED.

### Exact commands
- `grep -n "option(" CMakeLists.txt src/*/CMakeLists.txt`
- `grep -h "BITCOIN_CONFIG" ci/test/00_setup_env*.sh` (+ per-flag
  `grep -rl <FLAG> ci/test/*.sh` counts)
- python3 glob-vs-CMakeLists drift scripts (recorded in shell history)

### Limitations / leads
- No builds run (disk). Cross/emulated and pairwise flag interactions not
  exercised locally — next cycle needs either disk cleanup (~3GB) for one
  scratch build dir, or confine to static analysis.
- Stale-guard check (#if ENABLE_WALLET-style macros vs build-config
  definitions) not yet swept.
- Depends/CI package-version drift (depends/packages vs ci packages)
  unchecked.

### Next queue for this campaign
- One scratch-config build if disk frees up: e.g. ENABLE_WALLET=OFF +
  BUILD_TESTS=ON unit suite (configuration-dependent test coverage).
- depends package version vs ci/test PACKAGES drift table.

## Cycle 2 (2026-07-28): stale preprocessor guard sweep — clean

### Method
Cross-referenced build-system macro definitions (22 #cmakedefine in
cmake/bitcoin-build-config.h.in + add_compile_definitions across
CMakeLists) against all config-style guards (#if/#ifdef/defined) in
src/**.{c,cpp,h} (856 guard names), filtered out compiler/platform/
subtree macros.

### Result
- Config macros with no #if usage in src/: NONE (all 18 main + compile
  definitions are consumed).
- Guards not defined by the build system: 5 candidates, all resolved:
  - USE_POLL: defined in-tree (compat/compat.h:105), platform default.
  - HAVE_PTHREAD_GETNAME_NP: libmultiprocess subtree's own config
    (src/ipc/libmultiprocess mp/util.cpp).
  - NO_SANITIZE_MEMORY: same-header compiler-conditional definition
    (minisketch clmul_common_impl.h:23/28).
  - DISABLE_BUILTIN_BSWAPS: documented opt-out escape hatch for
    external builders (compat/byteswap.h:18).
  - NO_THREAD_SAFETY_ANALYSIS: same-header clang-conditional
    (threadsafety.h:32/51).
- ENABLE_MODULE_*/ENABLE_FIELD_INT_*/USE_ASM_X86_64/
  USE_EXTERNAL_DEFAULT_CALLBACKS: secp256k1 subtree config (own
  generated headers). ENABLE_ZMQ/ENABLE_EMBEDDED_ASMAP/
  ENABLE_ARM_SHANI/ENABLE_AVX2/DISABLE_DEFAULT_FIELDS/BUILD_GIT_TAG:
  one build-system definition each (target-level).

### Verdict
- DISMISSED (no stale guards, no dead config knobs). Journal-only.

### Exact commands
- python3 cross-reference script (cycle history);
  `grep -rn <MACRO> CMakeLists.txt cmake/ src/*/CMakeLists.txt`

### Next queue for this campaign
- One scratch-config build if disk frees up: ENABLE_WALLET=OFF +
  BUILD_TESTS=ON unit suite.
- depends package version vs ci/test PACKAGES drift table.

## Rotation note
Two bounded cycles complete; rotating per uber-goal policy. Not exhausted.

## Cycle 3 (2026-07-28): walletless matrix cells — both green; depends drift resolved

### Draw
Random draw over the 50-goal eligible pool: raw=1398496025551732756,
index 6 -> #17. Disk at ~10 GB free after #66-era cleanup — enough for
the queued scratch-config build (ccache made it cheap).

### Cell 1: depends package version vs CI drift — RESOLVED (no drift possible)
26 depends/packages/*.mk are the single source of truth for versions;
ci/test scripts reference no package versions by name (DEPENDS is a
build directory, not a version list). There is nothing to drift.
Item closed.

### Cell 2: ENABLE_WALLET=OFF + BUILD_TESTS=ON (Release)
- configure+build: clean (486 targets; one pre-existing
  -Wdeprecated-copy note in util_tests.cpp, not an error).
- bitcoin-build-config.h: `/* #undef ENABLE_WALLET */` confirmed.
- suite: `test_bitcoin` -> No errors detected.
- delta verified: walletless 1199 vs full 1285 test cases (-86
  wallet-only); zero wallet suites in the walletless binary.

### Cell 3: ENABLE_WALLET=OFF + BUILD_FOR_FUZZING=ON (RelWithDebInfo)
- fuzz binary links clean (110/110).
- targets: 227 vs 241 wallet-on (PRINT_ALL_FUZZ_TARGETS_AND_ABORT);
  zero wallet/rescan targets present.

### Structural evidence
Both wallet test trees are included only under `if(ENABLE_WALLET)` at
the inclusion points (src/test/CMakeLists.txt:175,
src/test/fuzz/CMakeLists.txt:167), so all fork-added wallet
test/fuzz files (rescan.cpp included) drop out of walletless builds
automatically. The fork did not break the walletless matrix.

### Verdict
- DISMISSED (all three cells clean). Journal-only.
- Scratch dirs removed after measurement (build-walletless 218M,
  /tmp/build-wl-fuzz 2.3G; disk back to 9.7G free).

### Exact commands
- `cmake -B build-walletless -G Ninja -DCMAKE_BUILD_TYPE=Release
  -DENABLE_WALLET=OFF -DBUILD_TESTS=ON`
- `cmake --build build-walletless -j4 --target test_bitcoin`
- `build-walletless/bin/test_bitcoin [--list_content]`
- `cmake -B /tmp/build-wl-fuzz -G Ninja -DCMAKE_BUILD_TYPE=RelWithDebInfo
  -DENABLE_WALLET=OFF -DBUILD_FOR_FUZZING=ON`
- `PRINT_ALL_FUZZ_TARGETS_AND_ABORT=1 <bin>/fuzz | wc -l`

### Next queue for this campaign
- Cross/emulated cell (armhf) — needs the arm toolchain container;
  parked (this host is already aarch64 native).
- DISABLE_OPTIMIZED_SHA256 build + SelfTest path (pairs with #69's
  backend inventory: forces the scalar reference end-to-end).

## Rotation note
Three bounded cycles complete; rotating per uber-goal policy. Not exhausted.
