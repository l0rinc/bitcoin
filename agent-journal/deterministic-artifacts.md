# Generated-artifact and test-vector determinism audit

## Cycle 160: Headersync Parameter Generator Reproducibility

### Cycle Identity

- Draw command: `shuf -i 0-98 -n 1`
- Draw: `39`
- Selected goal: `deterministic-artifacts` (generated-artifact and test-vector determinism audit)
- Worktree: `/data/my_storage/bitcoin`
- Branch: `uber-cycle-160-deterministic-artifacts-20260730`
- Start HEAD: `398766e0174d89fda80c85ef3a85ca89a63fb0f2`
- `origin/master`: `67efced1fc83a0b7215cc1513e7c4754fee0f12f`
- Merge-base: `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`
- Start divergence: `42 1101` (`origin/master...HEAD`)

### Scope and Hypothesis

Cycle 9 already closed the Silent Payments generator and is excluded. This cycle selected the distinct `contrib/devtools/headerssync-params.py` pipeline and its generated `src/kernel/chainparams.cpp` parameter blocks. The hypothesis was that the documented invocation, `pypy3 contrib/devtools/headerssync-params.py`, did not produce a reproducible transcript or copied C++ block because it used an uncontrolled random search order and `datetime.today()` in generated output.

The trust boundary is the developer generator, its stdout block copied into the four chain-parameter constructors, and the `HeadersSyncState` consumer that reads `.commitment_period` and `.redownload_buffer_size`. The intended numeric parameters must remain unchanged; only the regeneration representation and search trace should become stable.

### Evidence Before the Fix

- `contrib/devtools/README.md` documents the generator invocation but specifies no random seed, environment contract, or timestamp policy.
- `headerssync-params.py` selected each candidate period with `random.randrange(len(periods))` and emitted `datetime.today()` in the copied C++ comment. The date was not an input to the optimization and changed only provenance text.
- Two clean runs on the same source, Python 3.11, and host produced different complete outputs:

  ```text
  run1: b8be97dd5c41b865608342383806ef5e0cf5c3bf14be7896cf32b43bcf6264f1  (789 bytes)
  run2: 7877c61f076c283fe4f17c580d490462fe8cc8a55c4654da7f027d11b034dde3  (843 bytes)
  ```

  The first run reported only the initial configuration and final improvement; the second reported two intermediate improvements. Both ended with the same numeric result, `commitment_period = 641` and `redownload_buffer_size = 15218`, but both emitted `Generated by headerssync-params.py on 2026-07-30`, while the checked-in artifact still said `on 2026-02-25`.
- The changing search transcript demonstrates uncontrolled output nondeterminism, and the wall-clock comment makes the generated source differ whenever the tool is rerun on another day. Neither difference changes runtime behavior, but both violate the artifact regeneration contract and create unnecessary review/build noise.
- History traces the timestamped output to `cc5dda1de333cf7aa10e2237ee2c9221f705dbd9` (`headerssync: Make HeadersSyncState more flexible and move constants`) and shows the current generated comments updated by `58c2e23fca05d201dfcea092ab032c943c9d566` (`kernel: Update headerssync params`). No deterministic seed or timestamp override was added later.

### Fix

- Seed the generator's optimization search with `random.seed(0)` immediately before `analyze(TIME)`, preserving the selected numeric result while making the search trace repeatable.
- Replace the wall-clock comment with the stable `// Generated by headerssync-params.py.` text.
- Update the four corresponding generated comments in `src/kernel/chainparams.cpp` for mainnet, testnet3, testnet4, and signet. The four numeric parameter pairs are unchanged.

### Verification

Two post-fix runs of the documented generator invocation were byte-identical:

```text
6c904947f6dec9b4e32c792429234ffacbba832cf54ba5653d334947ca359306  /data/my_storage/tmp/cycle160-headerssync-after1.txt
6c904947f6dec9b4e32c792429234ffacbba832cf54ba5653d334947ca359306  /data/my_storage/tmp/cycle160-headerssync-after2.txt
repeated_output_cmp=0
```

The generated mainnet C++ block compares byte-for-byte with lines 223-227 of the checked-in `chainparams.cpp`. `python3 -m py_compile contrib/devtools/headerssync-params.py` passed. A fresh Clang 19 UBSan/alignment/object-size build of `test_bitcoin` completed all 198 build steps, and:

```text
TMPDIR=/data/my_storage/tmp/cycle160-test-tmp \
  /data/my_storage/tmp/cycle106-clang19-ubsan/bin/test_bitcoin \
  --run_test=headers_sync_chainwork_tests --random=160039 --log_level=test_suite
Running 7 test cases ... *** No errors detected
```

`git diff --check` passed. The source change does not alter the generated numeric parameters or the runtime headers-sync algorithm.

### Verdict and Handoff

**Confirmed generated-artifact nondeterminism; fixed.** The generator previously depended on ambient PRNG state and wall-clock date, so the documented regeneration path could not reproduce its own output text. The smallest fix controls the search seed and removes non-semantic date text, while preserving all runtime values. The source/test/journal finding commit must include this record; the next separate commit will close the uber-goal state.

### Limitations

- The script's optimization remains a simulation with `ASSUME_CONVEX = True`; this cycle audited reproducibility, not the mathematical optimality assumption.
- The script is configured for one network at a time, so this cycle compared the generated mainnet block and updated stable comments for the other three configured blocks; it did not rerun four alternate parameter configurations.
- No release, alternate architecture, locale-specific Python, PyPy, or cross-version interpreter run was available. The exact repeated Python 3.11 output and source/build validation are the current evidence.


## Cycle 9

- Selector: `shuf -i 0-98 -n 1`
- Draw: `39`
- Selected goal: `deterministic-artifacts`
- Date: 2026-07-27 UTC
- Repository HEAD: `1dcc2da988ee625fbc5d7d55eb6f894c1103ec52`
- Branch: `fuzz-contract-cluster-oracles-20260709`
- Base: `origin/master` at `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`
- Working tree: no tracked changes; only agent-owned untracked directories are present

## Scope and contract

The selected bounded surface is the libsecp256k1 Silent Payments test-vector
pipeline. The contract is that the checked-in
`src/modules/silentpayments/vectors.h` is a reproducible C-header projection of
`bip352_send_and_receive_test_vectors.json`, and that the generated data remains
valid for the consuming Silent Payments test module.

The Automake recipe lists `vectors.h` as a built test-vector source and invokes
`tools/tests_silentpayments_generate.py` with the adjacent BIP352 JSON input
(`src/secp256k1/Makefile.am:263-289`). The generated header identifies its source
script and says not to edit it. The generator imports only `hashlib`, `json`, and
`sys`; source inspection found no clock, locale, environment, UUID, or random
input. Its only order-sensitive operation is the explicit numeric sort used to
choose the smallest outpoint.

The source, generator, and generated header were introduced together by the
current repository's secp256k1 subtree update `c26d4e2d6f`. File-level history
does not exist in the Bitcoin Core superproject before that subtree snapshot, so
the subtree import is the available provenance boundary for this artifact.

## Hypothesis

The checked-in Silent Payments header might be stale or generation might depend
on an unstated runtime property such as iteration order, time, locale, or random
state. A fresh regeneration should either differ from the checked-in artifact or
produce the same bytes across independent runs. The consuming vector test must
also pass after the current build graph is refreshed.

## Evidence and exact commands

Scratch outputs were written below `/data/my_storage/tmp/deterministic-artifacts/`.
No tracked generated file was overwritten.

1. Generated twice from the checked-in JSON:

   `python3 src/secp256k1/tools/tests_silentpayments_generate.py src/secp256k1/src/modules/silentpayments/bip352_send_and_receive_test_vectors.json > /data/my_storage/tmp/deterministic-artifacts/silent-vectors-run1.h`

   `python3 src/secp256k1/tools/tests_silentpayments_generate.py src/secp256k1/src/modules/silentpayments/bip352_send_and_receive_test_vectors.json > /data/my_storage/tmp/deterministic-artifacts/silent-vectors-run2.h`

2. Byte-level result:

   ```text
   8d88aead1f2f359aca31ac8c803001c55b1231be187839cafbe9c6959cedbbcc  src/secp256k1/src/modules/silentpayments/vectors.h
   8d88aead1f2f359aca31ac8c803001c55b1231be187839cafbe9c6959cedbbcc  /data/my_storage/tmp/deterministic-artifacts/silent-vectors-run1.h
   8d88aead1f2f359aca31ac8c803001c55b1231be187839cafbe9c6959cedbbcc  /data/my_storage/tmp/deterministic-artifacts/silent-vectors-run2.h
   tracked_vs_run1_cmp_exit=0
   run1_vs_run2_cmp_exit=0
      4747  539457 src/secp256k1/src/modules/silentpayments/vectors.h
      4747  539457 /data/my_storage/tmp/deterministic-artifacts/silent-vectors-run1.h
      4747  539457 /data/my_storage/tmp/deterministic-artifacts/silent-vectors-run2.h
   ```

3. The existing `build_unit_clang19` configuration was stale relative to the
   July 23 subtree import. This refreshed its CMake graph and rebuilt the secp
   test binary:

   `cmake --build build_unit_clang19 --target src/secp256k1/bin/tests -j2`

   The refreshed configuration reported Silent Payments `ON`; it compiled
   `src/secp256k1/src/CMakeFiles/tests.dir/tests.c.o` and relinked the test binary.

4. The consumer module was run with a fixed test seed:

   `build_unit_clang19/src/secp256k1/bin/tests --target=silentpayments --iterations=1 --seed=39 --log=1`

   All seven module tests passed, including
   `run_silentpayments_test_vectors`; total execution time was `15.137 sec`.

5. `git diff --check` reported no tracked diff or whitespace error. A post-test
   process gate found no remaining Bitcoin, fuzz, sanitizer, profiler, or build
   workload.

## Verdict

The hypothesis is **dismissed**. Two fresh generator runs are byte-identical to
the checked-in header, and the rebuilt consumer test validates the generated
vectors. No stale artifact, nondeterministic output, or repository defect was
confirmed. No production change or commit is warranted.

## Limitations and next evidence

- This cycle verified the Silent Payments generator only; other generated
  artifacts such as precomputed tables, MuSig vectors, Wycheproof headers, and
  seed headers remain separate surfaces.
- The two runs used the same Python 3.11.2 interpreter. Cross-version and
  cross-locale evidence would be useful if a future cycle selects this surface
  again with a concrete toolchain-drift hypothesis.
- The refreshed test binary used the available Clang 19.1.7 Debug configuration;
  no release or alternate-architecture consumer build was required after exact
  regeneration matched.

## Artifacts

- Regenerated headers: `/data/my_storage/tmp/deterministic-artifacts/silent-vectors-run1.h` and `silent-vectors-run2.h`
- Current consuming test binary: `/data/my_storage/bitcoin/build_unit_clang19/src/secp256k1/bin/tests`

## Cycle 241: MuSig2 vector generator invocation-path determinism

### Cycle identity

- Draw command: `shuf -i 0-98 -n 1`
- Draw: `39`
- Selected goal: `deterministic-artifacts`
- Branch: `uber-cycle-241-deterministic-artifacts-20260731`
- Start HEAD: `aeddab905ea7558245ae9ce8707d576de563df58`
- `origin/master`: `67efced1fc83a0b7215cc1513e7c4754fee0f12f`
- Merge-base: `a2aab6df97d9f3e1186e8c3fc57ad909cc8aef9b`
- Start divergence: `42 1265` (`origin/master...HEAD`)

The fresh gate found stable catalog, prompt, TSV, and uber-goal protocol hashes, no tracked dirty state, the known unrelated untracked artifacts, all six protected processes alive, a full `/` filesystem, and approximately 22 GB available under `/data`. All cycle artifacts were written below `/data/my_storage/tmp/cycle241-*`.

### Scope and hypothesis

Cycles 9 and 160 are excluded: Cycle 9 covered the Silent Payments generator and Cycle 160 fixed the headersync parameter generator's random search transcript and wall-clock comment. This cycle selected the distinct MuSig2 vector generator at `src/secp256k1/tools/test_vectors_musig2_generate.py`, whose output is represented by the checked-in `src/secp256k1/src/modules/musig/vectors.h`.

The trust boundary is the developer generator invocation, the six MuSig2 JSON inputs, and the generated C header consumed by the MuSig test module. The hypothesis was that the same vector inputs could produce different generated bytes when the script was invoked through a relative path, `./tools`, or an absolute path. The generator constructed its header comment with `sys.argv[0]`, while the checked-in artifact records `./tools/test_vectors_musig2_generate.py`.

History identifies the generator body and checked-in artifact as part of the MuSig2 subtree import (`0ba680d41b`) and attributes the generator header to `2d46a89386d`. The current repository does not retain the six upstream MuSig2 JSON input files, so a schema-valid minimal fixture was used for the independent before/after proof; the fixture exercises every generator section and does not replace the missing production-vector regeneration.

### Failing-before reproduction

The scratch fixture directory was `/data/my_storage/tmp/cycle241-musig-fixtures`. It contained minimal valid `key_agg_vectors.json`, `nonce_gen_vectors.json`, `nonce_agg_vectors.json`, `sign_verify_vectors.json`, `tweak_vectors.json`, and `sig_agg_vectors.json` files. The pre-fix generator was run from `/data/my_storage/bitcoin/src/secp256k1` with these three commands:

`python3 tools/test_vectors_musig2_generate.py /data/my_storage/tmp/cycle241-musig-fixtures > /data/my_storage/tmp/cycle241-musig-output/pre-tools.h`

`./tools/test_vectors_musig2_generate.py /data/my_storage/tmp/cycle241-musig-fixtures > /data/my_storage/tmp/cycle241-musig-output/pre-dot.h`

`python3 /data/my_storage/bitcoin/src/secp256k1/tools/test_vectors_musig2_generate.py /data/my_storage/tmp/cycle241-musig-fixtures > /data/my_storage/tmp/cycle241-musig-output/pre-absolute.h`

The exact pre-fix hashes were:

```text
8815b6d2bab776ddd4a299e74497e97b51fd6edf706220dd288c374e01d45798  pre-tools.h
93012d1fb5594ddf741ba3c651b1bf92650206b8178818c5a9f02eafb865bb87  pre-dot.h
20e42ce160f8678f9eb86bea45a844b6590411470676e33e17848f96a789a02b  pre-absolute.h
tools_vs_dot=1
tools_vs_absolute=1
dot_vs_absolute=1
```

The diff contained only the generated provenance line. The `./tools` invocation emitted the same line as the checked-in header; `python3 tools/...` emitted `tools/test_vectors_musig2_generate.py`, and the absolute invocation emitted its absolute filesystem path. The body and all vector-derived values were otherwise identical. This is a deterministic, self-contained failing-before proof for generated artifact bytes.

### Fix

The generator now emits the stable repository-relative name `./tools/test_vectors_musig2_generate.py` directly and no longer formats the artifact comment with `sys.argv[0]`. The input path still comes from `sys.argv[1]`; only non-semantic provenance text is stabilized. The checked-in `vectors.h` already contains the stable line, so no generated artifact update or runtime vector change is needed.

### Passing-after validation

The same three commands were rerun after the edit into `post-tools.h`, `post-dot.h`, and `post-absolute.h`. All three output hashes were identical:

```text
93012d1fb5594ddf741ba3c651b1bf92650206b8178818c5a9f02eafb865bb87  post-tools.h
93012d1fb5594ddf741ba3c651b1bf92650206b8178818c5a9f02eafb865bb87  post-dot.h
93012d1fb5594ddf741ba3c651b1bf92650206b8178818c5a9f02eafb865bb87  post-absolute.h
post_tools_vs_dot=0
post_tools_vs_absolute=0
post_dot_vs_absolute=0
```

`python3 -m py_compile src/secp256k1/tools/test_vectors_musig2_generate.py` passed, and `git diff --check` passed. The available prebuilt MuSig consumer binaries both passed all 12 groups, including all six generated-vector consumers:

- `/data/my_storage/tmp/cycle105-clang19-release/src/secp256k1/bin/tests --target=musig --iterations=1 --seed=241039 --log=1`: exit 0, total execution time 0.042 seconds.
- `/data/my_storage/tmp/cycle105-gcc-release/src/secp256k1/bin/tests --target=musig --iterations=1 --seed=241040 --log=1`: exit 0, total execution time 0.040 seconds.

The expected `build_unit_clang19` tree was not present, so no new consumer build was attempted. The generator-only patch cannot alter the already compiled consumer or its runtime vector values; the two existing compiler builds provide compatibility coverage for the unchanged artifact.

### Verdict and handoff

**Confirmed and fixed.** The generated MuSig2 header changed whenever the same inputs were supplied through a different script path, creating unnecessary byte-level artifact drift and preventing a path-independent regeneration check. The one-line semantic fix makes the output stable while preserving every generated vector and the checked-in header content. The source and this journal must be committed together as one independent finding, authored as `Lőrinc <pap.lorinc@gmail.com>`; the separate uber-goal state close follows.

### Limitations and next queue

- The production MuSig2 JSON inputs are absent from this Bitcoin Core subtree, so the reproduction uses a schema-valid minimal fixture rather than regenerating the full 346-line production header.
- No alternate Python implementation or locale change was needed because the defect is direct path interpolation; cross-interpreter output remains a separate question.
- Remaining distinct artifact cells include `makeseeds.py`'s intentional randomized pre-filter selection and tie behavior, manpage generation under locale/tool-version changes, Wycheproof JSON-to-header regeneration, and generated minisketch/precomputation outputs. Do not reopen Silent Payments, headersync, or this fixed `sys.argv[0]` cell without new evidence.
