# PR watch 2026-07-27 (15:41 run)

## (A) New PRs (marker 35819 → 35822)

- 🟢 PR 35822 "fuzz: reset SOCKS5 interrupt between inputs" — one-line harness
  fix, correct global-state-reset pattern. No merge-blocker.
- 🟢 PR 35821 "guix: followups to #35537" — guix shell refactor consolidating
  toolchain setup into setup.sh + `--fatal-warnings` for linux/win linkers
  with documented riscv64 exception (boost fpe). Build tooling only; guix CI
  is the oracle. No merge-blocker.
- 🟢 PR 35820 "refactor: use chrono literals for durations" — reviewed in
  depth this morning (2026-07-27-pr-35820-chrono-literals.md): clean.

## (B) Known-issue spot-checks

All unchanged since last run (heads identical): PR 32387 "ipc: add windows
support", PR 26022 "Add util::ResultPtr class", PR 27409 "Make GUI and CLI
tools use the same datadir", PR 35387 "logging: make trace logging easily
usable", PR 30437 "ipc: add bitcoin-mine test program". Status as previously
classified (🟠 still open where applicable).

## (C) Involvement tracking

Head changes on tracked PRs:

- 🟢 PR 35531 "txindex: hash keys and pack positions to reduce disk usage"
  (own PR, ad725217d1 → 40c4684bda, 7 commits): rework reviewed. Reorg-skip
  in WriteTxs (`seq != height-1` gate before Exists) is sound — the
  seq-vs-height gap is permanent after any reorg, so the fast path only
  fires when no duplicate can exist. FindHashedTx iterator seek and
  per-candidate cs_main scoping are correct; legacy fallback semantics
  preserved; collision + legacy tests added. No defect found.
- 🟢 PR 35735 "Add state to HTTPRequest" (d59e63729a → 0ced8c35ae, rebase +
  new commits): chunked-trailer handling now validates but DROPS trailer
  fields (`HTTPHeaders::Read(reader, write=false)`) instead of merging them
  into the request headers — RFC 9112 §7.1.2-aligned hardening. Previously
  reported issue stays fixed. No l0rinc review comments on this PR
  (involvement via mention); no mapping owed.

Potential conflicts (file overlap with own active PRs):

- ⚠️ PR 35744 "coins: prevent DB resize from invalidating cursors" vs
  PR 34132 "coins: drop error catcher, centralize fatal read handling"
  (both: txdb.cpp/h, coins.h, coins_tests — 34132 is a 28-file refactor,
  likely rebase churn) and vs PR 34320 "coins: delegate HaveCoin to GetCoin"
  and PR 34864 "coins: tighten cache entry state invariants" (both touch
  txdb.cpp + coins_tests).
- ⚠️ PR 35531 "txindex: hash keys and pack positions" vs PR 34489 "index:
  batch db writes during initial sync" (both: index/txindex.cpp/h,
  index/base.h) and vs PR 34132 (index/txindex, dbwrapper).
- ⚠️ PR 35820 "refactor: use chrono literals" vs PR 33854 "fix assumevalid
  is ignored during reindex" — both touch the assumevalid region of
  validation.cpp (35820 edits the two-week constant 33854 logic sits next
  to). Semantic conflict unlikely, git-level conflict plausible.
- 🟢 PR 35818 "bloom: support zero-element filters" — no overlapping open PR
  found.
