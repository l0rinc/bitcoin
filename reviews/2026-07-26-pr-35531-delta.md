# Involvement-tracked delta: PR 35531 pushed (ad725217d1, 2026-07-26)

PR 35531 (txindex hashed keys) was force-pushed after my 2026-07-25 review. The user has 28 review comments on this PR.

## What changed vs the reviewed version (ce62a0f5ea series)

- **New prep commit 07bbd7a719**: moves per-tx position computation from CustomAppend into `DB::WriteTxs` (DB layer receives the whole block; "no change in data written"). Cleaner layering for the new key format.
- **Hasher switched to `SipHasher13UJ`** (the recently-merged SipHash-1-3-UJ from 2beab94896) with a **DB-persisted salt**: `ReadOrCreateTxidHasher` reads `DB_TXID_HASH_SALT`, else generates via FastRandomContext and writes with `fSync=true`. Attackers can't know the salt → can't grind prefix collisions. Salt stability across restarts is required since it's the DB key format — correctly handled.
- Legacy-fallback design now explicit via `m_has_legacy` flag: only legacy-flagged DBs probe legacy keys; new DBs skip the legacy path entirely (tests at txindex_tests.cpp:212-217 cover this).

## Your comment mapping (28 comments; newest from 2026-06-30 sampled)

- ⚠️ **Intrinsic suggestion** (`std::bit_width`/builtin for the loop-from-1): no `bit_width`/builtin appears in the new key code — either not applied or the loop moved in the restructure. Not visibly applied.
- ✅ **Malformed-new-key-falls-to-legacy concern**: substantially addressed by design — new-format keys fail strict deserialization (prefix check throws), and `m_has_legacy=false` DBs never probe the legacy path at all, so there is nothing to fall through to. The legacy-flag path remains the only place the question lives, by design.
- nit (`return {.disconnect_data = true};`): not re-checked (cosmetic).

## State of the PR

The rebase is a substantial improvement (layering + the faster hash with correct salt persistence). My earlier verdict stands (no merge-blocker); the switch to SipHasher13UJ is consistent with the primitive I verified in the 2beab94896 review series.
