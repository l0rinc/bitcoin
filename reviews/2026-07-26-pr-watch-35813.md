# pr-watch 2026-07-26 — PRs 35813, 35810, 35809, 35808

## PR 35813 (wallet: Add listrawtransactions RPC) — 🟢 clean

New read-only wallet RPC listing every known tx with net balance change — includes consolidations/self-transfers invisible in listtransactions.

Verified against the existing conventions:
- **Fee sign**: `nFee = GetValueOut - nDebit` is negative by construction; displayed via `ValueFromAmount(nFee)` — matches listtransactions' negative-fee convention (`transactions.cpp:335`).
- **Amount semantics**: `amount = nNet - nFee` = credit−debit−fee — for a funded tx equals −payment; for a pure self-transfer equals 0 (matches the documented "excluding fee" semantics).
- **Pagination**: skip/count clamping and the reverse-iteration + move_iterator slicing are the battle-tested listtransactions pattern, copied verbatim; ordering within the window is oldest-to-newest as documented.
- `abandoned` flag + WalletTxToJSON metadata; read-only, cs_wallet held during enumeration.

## PR 35810 (guix: drop unused (guix licenses) import) — 🟢 trivial one-liner.
## PR 35809 (contrib: deterministic fuzz coverage mode) — 🟢 devtool-only; new mode arg parsed with error-on-invalid, defaults preserved.
## PR 35808 (fuzz: reset connman state in p2p targets) — 🟢 fuzz-only, 2 lines.

Known issues: 32387/26022/27409/35387/30437 all unchanged (still open). Marker → 35813.
