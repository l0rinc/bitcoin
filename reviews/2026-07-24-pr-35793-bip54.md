# Review: PR 35793 — Implement BIP 54 (Consensus Cleanup) without mainnet activation (2026-07-24)

Verdict: 🟢 no merge-blocker. All five consensus rules reviewed line-by-line; gating is correct everywhere; nothing activates on mainnet.

## Per-rule findings

- **Timewarp** (d96b2c79dc): gated on `DeploymentActiveAfter(pindexPrev)`; BIP94 testnet4 takes precedence with the tighter constant (static_assert links them). Correct.
- **Murch-Zawy negative intervals** (8795625242): last-block-of-interval check; `first_height = nHeight - dai + 1` verified at both boundaries (2015, 4031); GetAncestor can't be null; compares raw header time vs stored first-block time per BIP 54.
- **Coinbase timelock** (87b003f19d): `nLockTime == nHeight - 1` is the only self-consistent value (`== nHeight` would make the coinbase non-final in its own block); `vin[0].nSequence != SEQUENCE_FINAL` requirement matches; guarding Assert safe (CheckBlock/CheckTransaction already ran).
- **64-byte txs** (b1ec5a6f4f): CRITICAL GATING QUESTION resolves cleanly — the rule lives in `ContextualCheckBlock` gated on deployment (historical validation untouched even if a 64-byte tx existed pre-deployment) plus a mempool-only TX_CONSENSUS in PreChecks that only changes the RPC error. No ungated CheckTransaction change exists.
- **Legacy sigops** (39cca6c97e + 9ccf729e75 moveonly): verbatim move of CheckSigopsBIP54 to consensus (same 2500 constant), threaded as `enforce_bip54` with `DeploymentActiveAt(*pindex)` — correct per-block gating; missing-input handling safe (empty coin → 0 sigops); error-precedence change is consensus-inert (block invalid if any rule fails regardless of order).

## Deployment
NEVER_ACTIVE everywhere except regtest (ALWAYS_ACTIVE, h=0, 108/144 = 75% threshold) — hence the included functional-test updates avoiding ≤64-byte txs. bips.md entry + e2e + JSON vectors + fuzz target for the sigops check complete the package.

## Note for the user
Your sigop-template series (32729: CountP2SHSigOps/CountSigOps helpers) touches the same counting functions this PR moves into consensus — expect a rebase interaction in whichever lands second; the counting predicates are identical (verified byte-exact in your review), so the merge is mechanical.
